# Auth & RBAC Tasks

**Design**: `.specs/features/auth-rbac/design.md`
**Testing**: `.specs/codebase/TESTING.md`
**Status**: Complete (T1-T12 verified)

**Stack for all tasks:** async FastAPI (asyncpg + SQLAlchemy 2.0 `AsyncSession`, `redis.asyncio`), httpx `AsyncClient` tests, ruff + mypy. Integration tests need `compose.test.yml` up.

---

## Execution Plan

### Phase 1 — Foundation (Sequential)
```
T1 → T2
```

### Phase 2 — Models & security (T4 parallel with T2→T3 chain)
```
T1 ──► T4 [P]        (unit — parallel-safe)
T2 ──► T3
```

### Phase 3 — Stores & guards (Sequential)
```
T4 ──► T5 ──► T7
T3 ──► T6 ──► (T7 also needs T5)
```

### Phase 4 — Services (Sequential)
```
{T4,T5,T6} ──► T8 ──► T9
```

### Phase 5 — API & bootstrap (Sequential)
```
T9,T7 ──► T10 ─┐
T8,T7 ──► T11 ─┼─► T12
               ┘
```

---

## Task Breakdown

### T1: Project skeleton, config & test infra
**What**: Create `control-plane/` package (pyproject with FastAPI/SQLAlchemy/asyncpg/redis/argon2/alembic/pydantic-settings/ruff/mypy/pytest deps), `app/main.py` app factory + lifespan stub, `app/core/config.py` `Settings`, ruff/mypy/pytest config with `unit`/`integration` markers, and `compose.test.yml` (Postgres + Redis).
**Where**: `control-plane/pyproject.toml`, `control-plane/app/main.py`, `control-plane/app/core/config.py`, `control-plane/compose.test.yml`, `control-plane/tests/conftest.py`
**Depends on**: None
**Reuses**: Establishes skeleton (design "Control-plane project skeleton")
**Requirement**: Foundation (enables AUTH-*)
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `pip install -e .` (or `uv sync`) resolves; `python -c "import app.main"` succeeds
- [x] `docker compose -f compose.test.yml up -d` brings up PG + Redis; `conftest.py` fixtures connect
- [x] `ruff check .`, `ruff format --check .`, `mypy app/` all pass on the skeleton
- [x] Gate check passes: `python -c "import app.main"` (build)
**Tests**: none
**Gate**: build
**Commit**: `chore(control-plane): scaffold FastAPI skeleton, config & test infra`

---

### T2: Async DB engine, session & Alembic harness
**What**: `app/db/session.py` (async engine, `async_sessionmaker`, `Base`, session dependency) + Alembic scaffold wired to async engine; empty baseline migration.
**Where**: `control-plane/app/db/session.py`, `control-plane/alembic.ini`, `control-plane/migrations/env.py`
**Depends on**: T1
**Reuses**: `app/core/config.py` (DSN), design "Data Models"
**Requirement**: Foundation
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `alembic upgrade head` applies cleanly against `compose.test.yml` Postgres
- [x] A connectivity integration test opens an `AsyncSession` and runs `SELECT 1`
- [x] Gate check passes: `ruff check . && ruff format --check . && mypy app/ && pytest -q` (full)
- [x] Test count: 1 test passes (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(control-plane): async DB engine, session factory & alembic harness`

---

### T3: Data models + constraints + migration
**What**: `User`, `Tenant`, `AuditEvent` SQLAlchemy models with enums, `citext`/unique-lower username, role⇔tenant `CHECK`, FKs (`AuditEvent.actor_user_id ON DELETE SET NULL`), `JSONB` metadata, `session_version`; autogenerated migration.
**Where**: `control-plane/app/db/models.py`, `control-plane/migrations/versions/*_auth_models.py`
**Depends on**: T2
**Reuses**: `Base` (T2), design "Data Models"
**Requirement**: AUTH-06, AUTH-17, AUTH-22, AUTH-23..26 (schema)
**Tools**: Bash, Write/Edit
**Done when**:
- [x] Migration creates all three tables + the `citext` extension
- [x] Integration tests prove: role⇔tenant CHECK rejects admin-with-tenant & tenant_user-without-tenant (AUTH-17); duplicate username (case-insensitive) rejected (AUTH-22); deleting a user NULLs `AuditEvent.actor_user_id` but keeps `actor_username`
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥4 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): User/Tenant/AuditEvent models, constraints & migration`

---

### T4: Security primitives [P]
**What**: `app/core/security.py` — `hash_password` (argon2id), `verify_password` (constant-time), `new_session_id` (`secrets.token_urlsafe(32)`). ⚠️ Confirm current maintenance of hash lib (argon2-cffi vs pwdlib) before pinning (design flag #1).
**Where**: `control-plane/app/core/security.py`, `control-plane/tests/unit/test_security.py`
**Depends on**: T1
**Reuses**: Establishes; design "Security primitives"
**Requirement**: AUTH-06, AUTH-07, AUTH-08
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `hash_password` returns an argon2id string with per-password salt; two hashes of same input differ
- [x] `verify_password` true on match, false on mismatch; uses library constant-time verify
- [x] `new_session_id` returns ≥32 bytes of URL-safe entropy, unique across calls
- [x] Gate check passes: `ruff check . && ruff format --check . && mypy app/ && pytest -q -m unit` (quick)
- [x] Test count: ≥4 tests pass (no silent deletions)
**Tests**: unit
**Gate**: quick
**Commit**: `feat(auth): password hashing & session-id primitives`

---

### T5: Redis session store
**What**: `app/core/sessions.py` — `create`, `get` (sliding idle TTL + absolute-expiry enforcement), `revoke`, `revoke_all`, `list_for_user`; keys `session:{sid}` + reverse index `user_sessions:{uid}`.
**Where**: `control-plane/app/core/sessions.py`, `control-plane/tests/integration/test_sessions.py`
**Depends on**: T4
**Reuses**: `new_session_id` (T4), `redis.asyncio` client (T1 config), design "Redis session store"
**Requirement**: AUTH-01, AUTH-03, AUTH-04, AUTH-19, AUTH-20, AUTH-30, AUTH-33
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `create`→`get` round-trips; `get` on unknown/expired sid → None (AUTH-04)
- [x] `revoke` kills one session; `revoke_all` kills all + clears reverse index (AUTH-19/20)
- [x] `list_for_user` returns active sessions (AUTH-33); idle TTL slides on `get`, absolute expiry still enforced
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥5 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): Redis server-side session store with mass revocation`

---

### T6: Audit service
**What**: `app/services/audit.py` — `record_event(db, *, actor, action, target_type, target_id, outcome, ip, metadata)` writing within the caller's transaction; secret-scrub helper for `metadata`.
**Where**: `control-plane/app/services/audit.py`, `control-plane/tests/integration/test_audit.py`, `control-plane/tests/unit/test_audit_scrub.py`
**Depends on**: T3
**Reuses**: `AuditEvent` model (T3), design "Audit service"
**Requirement**: AUTH-23, AUTH-24, AUTH-25, AUTH-26
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `record_event` inserts one row committed with the caller's mutation (same txn)
- [x] Scrub helper removes `password`/`token`/`secret`/`credential` keys from metadata (AUTH-26) — unit test
- [x] `actor_username` snapshot persisted even if actor later deleted
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥4 tests pass (no silent deletions)
**Tests**: integration (+ unit for scrub)
**Gate**: full
**Commit**: `feat(auth): transactional audit writer with secret scrubbing`

---

### T7: Auth dependencies & RBAC/ownership guards
**What**: `app/core/deps.py` — `Principal`, `get_current_user` (cookie→session→**fresh** user load→status/tenant/version checks), `require_admin`, `authorize_tenant_resource`, `scope_to_tenant`. Fail-closed everywhere.
**Where**: `control-plane/app/core/deps.py`, `control-plane/tests/integration/test_deps_rbac.py`
**Depends on**: T3, T5
**Reuses**: models (T3), session store (T5), design "Auth dependencies / guards" + "Error Handling"
**Requirement**: AUTH-05, AUTH-09, AUTH-10, AUTH-12, AUTH-13, AUTH-14, AUTH-38
**Tools**: Bash, Write/Edit
**Done when**:
- [x] No/expired/version-mismatch session → 401 (AUTH-05); disabled user / inactive tenant → 401 (AUTH-34 path)
- [x] `require_admin` denies tenant_user → 403 (AUTH-09); missing scope → deny (AUTH-10)
- [x] `authorize_tenant_resource`: admin allowed; tenant_user only own `tenant_id`; else deny (AUTH-12)
- [x] `scope_to_tenant` filters lists to own tenant for tenant_user, no-op for admin (AUTH-13); role/tenant change mid-session honored via fresh load (AUTH-38)
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥7 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): fail-closed RBAC + tenant-ownership guards`

---

### T8: User service
**What**: `app/services/users.py` — `create_user`, `update_user`, `set_status`, `reset_password`, `delete_user`, `change_own_password`, `get_user`, `list_users`; enforces role/tenant validity, username uniqueness, **last-admin guard**, and revoke-on-change (bump `session_version` + `sessions.revoke_all`); audits every mutation.
**Where**: `control-plane/app/services/users.py`, `control-plane/tests/integration/test_users_service.py`
**Depends on**: T4, T5, T6
**Reuses**: security (T4), sessions (T5), audit (T6), models (T3), design "User service"
**Requirement**: AUTH-16, AUTH-17, AUTH-18, AUTH-19, AUTH-20, AUTH-21, AUTH-22, AUTH-30, AUTH-31, AUTH-35
**Tools**: Bash, Write/Edit
**Done when**:
- [x] Create/edit/delete persist + audit; invalid role/tenant → error (AUTH-17); duplicate username → 409 (AUTH-22)
- [x] `set_status(disabled)`, `reset_password`, `delete_user` bump `session_version` **and** revoke_all (AUTH-19/20/21)
- [x] `change_own_password` keeps current session, invalidates others (AUTH-30); wrong current pw rejected+audited (AUTH-31)
- [x] Disabling/deleting the last active admin refused (AUTH-35)
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥9 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): user lifecycle service with revoke-on-change & last-admin guard`

---

### T9: Auth service (login/logout/bootstrap)
**What**: `app/services/auth.py` — `login` (generic failure, disabled-user/inactive-tenant reject, `last_login_at`, audit), `logout` (revoke + audit), `bootstrap_admin` (idempotent create-if-none from env/secret).
**Where**: `control-plane/app/services/auth.py`, `control-plane/tests/integration/test_auth_service.py`
**Depends on**: T4, T5, T6
**Reuses**: security (T4), sessions (T5), audit (T6), models (T3), design "Auth service"
**Requirement**: AUTH-01, AUTH-02, AUTH-03, AUTH-24, AUTH-27, AUTH-28, AUTH-29, AUTH-34
**Tools**: Bash, Write/Edit
**Done when**:
- [x] Valid login → session id + `last_login_at` updated + success audit (AUTH-01/24)
- [x] Invalid creds → single generic failure, no user-exists signal, failed-login audit (AUTH-02)
- [x] Disabled user / inactive tenant login refused even with correct pw (AUTH-34)
- [x] `bootstrap_admin` creates exactly one admin when none exists, idempotent on rerun, creds from env (AUTH-27/28/29)
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥6 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): login/logout & idempotent first-admin bootstrap`

---

### T10: Auth router
**What**: `app/api/routers/auth.py` — `POST /auth/login` (set HttpOnly/Secure/SameSite=Lax cookie), `POST /auth/logout` (clear cookie), `GET /auth/me`, `POST /auth/password` (self-change). Wire `get_current_user`.
**Where**: `control-plane/app/api/routers/auth.py`, `control-plane/tests/integration/test_auth_api.py`
**Depends on**: T7, T9
**Reuses**: auth service (T9), guards (T7), design "API routers"
**Requirement**: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-24, AUTH-30, AUTH-31
**Tools**: Bash, Write/Edit
**Done when**:
- [x] Login sets cookie; `/auth/me` returns principal; logout clears + session dead (AUTH-03)
- [x] Wrong creds → 401 generic; no session → 401 on `/auth/me` (AUTH-02/05)
- [x] Self password change invalidates other sessions (AUTH-30); **log-capture asserts no plaintext credential emitted** (Success Criteria)
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥6 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): auth API endpoints (login/logout/me/password)`

---

### T11: Users router (admin) + isolation & RBAC coverage
**What**: `app/api/routers/users.py` — admin-guarded `POST/GET/PATCH/DELETE /users` + `POST /users/{id}/reset-password`; list annotated with tenant/creator for admin. Includes the **isolation-pair** and RBAC-denial API tests.
**Where**: `control-plane/app/api/routers/users.py`, `control-plane/tests/integration/test_users_api.py`
**Depends on**: T7, T8
**Reuses**: user service (T8), guards (T7)
**Requirement**: AUTH-09, AUTH-11, AUTH-12, AUTH-13, AUTH-15, AUTH-16, AUTH-18, AUTH-19, AUTH-20, AUTH-21, AUTH-22
**Tools**: Bash, Write/Edit
**Done when**:
- [x] tenant_user → any `/users` endpoint = 403 (AUTH-09); admin CRUD works + audited
- [x] **Isolation pair**: tenant A session cannot read/modify a resource scoped to B (404, zero leak); admin lists both with tenant labels (AUTH-12/13/15, Success Criteria)
- [x] Admin reset-password endpoint invalidates target sessions (AUTH-20)
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥7 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): users admin API with tenant-isolation enforcement`

---

### T12: Bootstrap CLI & app assembly
**What**: `app/cli.py` bootstrap-admin command; mount auth+users routers and finalize lifespan (DB/Redis pools) in `app/main.py`; end-to-end smoke: fresh DB → bootstrap → admin login → create tenant_user.
**Where**: `control-plane/app/cli.py`, `control-plane/app/main.py` (modify), `control-plane/tests/integration/test_bootstrap_e2e.py`
**Depends on**: T9, T10, T11
**Reuses**: auth service (T9), routers (T10/T11)
**Requirement**: AUTH-27, AUTH-28, AUTH-29 (+ end-to-end Success Criteria)
**Tools**: Bash, Write/Edit
**Done when**:
- [x] `python -m app.cli bootstrap-admin` creates first admin from env; rerun is idempotent (AUTH-27/28)
- [x] App boots with routers mounted; e2e: bootstrap → login → create tenant_user → that user logs in
- [x] Gate check passes: `... && pytest -q` (full)
- [x] Test count: ≥3 tests pass (no silent deletions)
**Tests**: integration
**Gate**: full
**Commit**: `feat(auth): first-admin bootstrap CLI & control-plane assembly`

---

## Parallel Execution Map
```
Phase 1 (Sequential):     T1 ──► T2
Phase 2 (Parallel):       after T1:  T4 [P]   ‖   T2 ──► T3
Phase 3 (Sequential):     T4 ──► T5 ;  T3 ──► T6 ;  (T3,T5) ──► T7
Phase 4 (Sequential):     (T4,T5,T6) ──► T8 ──► T9
Phase 5 (Sequential):     T10 (T7,T9) ──► T11 (T7,T8) ──► T12 (T9,T10,T11)
```
Only **T4** is `[P]` — it is the sole unit-tested task (parallel-safe per TESTING.md). All other tasks carry integration tests that share the single `compose.test.yml` Postgres/Redis and therefore run sequentially.

---

## Validation — Check 1: Task Granularity

| Task | Scope | Status |
| --- | --- | --- |
| T1 | skeleton/config/infra (cohesive bootstrap) | ✅ Granular |
| T2 | DB engine+session+alembic (1 layer) | ✅ Granular |
| T3 | 3 cohesive models + 1 migration | ✅ Granular |
| T4 | 1 module, 3 pure fns | ✅ Granular |
| T5 | 1 module (session store) | ✅ Granular |
| T6 | 1 service (audit) | ✅ Granular |
| T7 | 1 module (guards) | ✅ Granular |
| T8 | 1 service (users) | ✅ Granular |
| T9 | 1 service (auth) | ✅ Granular |
| T10 | 1 router (auth) | ✅ Granular |
| T11 | 1 router (users) | ✅ Granular |
| T12 | CLI + wiring (cohesive assembly) | ✅ Granular |

## Validation — Check 2: Diagram ↔ Definition Cross-Check

| Task | Depends on (body) | Diagram arrows | Status |
| --- | --- | --- | --- |
| T1 | None | (root) | ✅ Match |
| T2 | T1 | T1→T2 | ✅ Match |
| T3 | T2 | T2→T3 | ✅ Match |
| T4 | T1 | T1→T4 [P] | ✅ Match |
| T5 | T4 | T4→T5 | ✅ Match |
| T6 | T3 | T3→T6 | ✅ Match |
| T7 | T3, T5 | T3→T7, T5→T7 | ✅ Match |
| T8 | T4, T5, T6 | {T4,T5,T6}→T8 | ✅ Match |
| T9 | T4, T5, T6 (via T8 chain) | T8→T9 | ✅ Match |
| T10 | T7, T9 | {T7,T9}→T10 | ✅ Match |
| T11 | T7, T8 | {T7,T8}→T11 | ✅ Match |
| T12 | T9, T10, T11 | {T9,T10,T11}→T12 | ✅ Match |

Parallel check: T4 `[P]` shares no dependency with T2/T3 in its phase ✅.

## Validation — Check 3: Test Co-location

| Task | Code layer | Matrix requires | Task says | Status |
| --- | --- | --- | --- | --- |
| T1 | config/app factory | none | none | ✅ OK |
| T2 | db engine/alembic | integration | integration | ✅ OK |
| T3 | models | integration | integration | ✅ OK |
| T4 | security primitives | unit | unit | ✅ OK |
| T5 | session store | integration | integration | ✅ OK |
| T6 | audit service | integration (+unit scrub) | integration (+unit) | ✅ OK |
| T7 | deps/guards | integration | integration | ✅ OK |
| T8 | user service | integration | integration | ✅ OK |
| T9 | auth service | integration | integration | ✅ OK |
| T10 | api router | integration | integration | ✅ OK |
| T11 | api router | integration | integration | ✅ OK |
| T12 | cli + wiring | integration | integration | ✅ OK |

All three checks pass — no restructuring required.

---

## Requirement Coverage

39 requirements (AUTH-01..39) map to tasks as follows; unmapped items are tracked below.

- **Mapped:** AUTH-01 (T5,T9,T10) · 02 (T9,T10) · 03 (T5,T9,T10) · 04 (T5,T7,T10) · 05 (T7,T10) · 06 (T3,T4) · 07 (T4) · 08 (T4) · 09 (T7,T11) · 10 (T7) · 11 (T11) · 12 (T7,T11) · 13 (T7,T11) · 14 (T7) · 15 (T11) · 16 (T8,T11) · 17 (T3,T8) · 18 (T8,T11) · 19 (T5,T8,T11) · 20 (T5,T8,T11) · 21 (T8,T11) · 22 (T3,T8) · 23–26 (T3,T6) · 27–29 (T9,T12) · 30 (T8,T10) · 31 (T8,T10) · 34 (T7,T9) · 35 (T8) · 38 (T7).
- **Deferred to P2/edge (not in this task set):** AUTH-32, AUTH-33 (session inventory/password-policy — P2; AUTH-33 store support lands in T5, UI later), AUTH-36 (delete-tenant rule — owned by Tenant/CIDR feature), AUTH-37 (username race — covered implicitly by the unique constraint in T3), AUTH-39 (failed-login audit — covered by T9/T10 audit).

---

## Tooling note (Execute phase)
Uniform greenfield Python project → built-in **Bash / Write / Edit** + `pytest`; apply the `coding-guidelines` skill during implementation. No external MCPs required. Diagrams already handled via `mermaid-studio`. Execution will delegate each task to a sub-agent (sequential; T4 may run parallel), per the skill's Sub-Agent Delegation model — **only when you approve and start Execute**.
