# State

**Last Updated:** 2026-07-22

**Current Work:** **UDP amplification config & DDoS Protection tab (`udp-amplification-config`, AMP-01..21) — P1 EXECUTED + VERIFIED** (2026-07-22). Reviewed via `/tlc-spec-driven` validate. **16 atomic commits `181223a..80a67ff`** landed all of Phase 0–4: CT1 `BlockedUdpPort` model+migration (pinned live to **`20260722_0013_blocked_udp_port`** — real head at Execute was service-ratelimit's `_0012`, not the drafted `_0011_alerting`) → CT2 `services/ddos_amplification.py` (`HARDCODED_AMP_PORTS` 12-const + `list/add/remove_blocked_port` + `record_event ddos.amp_port.added/removed`) → CT3 `/ddos` admin router+schemas+`main.py` register → CT4 `worker/blocked_port_reconciler.py` (`BlockedPortReconciler`+`DpstatBlockedPortsWriter`+`FakeBlockedPortsWriter`, 1:1 clone of `NodeControlReconciler`) → CT5 `worker.py` wiring + `worker_blocked_port_enabled/_interval` settings; **DP** DT1 `dpstat set-blocked-ports` + DT2 `smoke_blocked_port.sh`; **FE** FT1 types+`useAmplificationConfig` hook + FT2 `DdosProtectionPage`+`BlockedPortForm`+Sidebar nav+`/admin/ddos` route; DOC1 docs (data-plane/frontend README + TESTING.md). **All gates re-run green at review:** **CP** `ruff`/`ruff format --check`/`mypy app/` clean + feature's **33 integration tests** pass (`test_blocked_udp_port_model` 6, `test_ddos_amplification_service` 6, `test_ddos_router` 3, `test_blocked_port_reconciler` 4, `test_worker_runtime` +2 lane, `test_udp_amplification_e2e` 1); **FE** lint/typecheck/**220 tests (50 files; B_fe 213→220, +7)**/build; **DP** `make bpf skel loader apply dpstat` + `make test` **137** + DT1 behavior verified (usage lists subcommand; unloaded→friendly exit 1; port>65535→exit 2) + **DT2 privileged veth smoke PASSED** — src-port 9999 drops `udp_amplification_drop` (idx 7) / 9998 delivered, **survives `xdpgw-apply` slot-swap (carry-forward-safe, AMP-11 proof)**, empty set clears. **Correctness confirmed:** reconciler drift-only on in-memory `asserted_ports` (None→re-assert on restart), **fail-safe on write failure (never clears asserted → AMP-10)**; `add` 409 on dup / `remove` 404 on absent / both audited. **P1 = AMP-01..19 all ✅ Verified; P2 = AMP-20..21 (PT1 `dpstat snapshot` read-back + PT2 effective-state panel) intentionally deferred.** ⚠️ **Whole-repo `pytest -q` = 610 passed / 6 FAILED — all 6 pre-existing & unrelated** (feature touches zero alert/user code, `git diff --name-only` confirmed): 2 are M6-Alerting (`test_alert_models`/`test_alerts_api`; Alerting still IN-PROGRESS) + 4 user-deletion tests that **pass in isolation** but fail under full-suite ordering pollution (`delete_user` coroutine-never-awaited leak). Memory `[[udp-amp-config-verified]]` + `[[cp-suite-preexisting-failures]]`. Minor: CT3 router test = 3 fns vs drafted ≥5 floor but all scenarios (GET/201/409/422/204/404/tenant-403) covered — coverage complete. *Independent of the service-ratelimit execution thread below.*

**Prior sub-step (superseded above — planning artifacts, now executed):** **UDP amplification config — SPEC + CONTEXT + DESIGN (AD-036) + TASKS DRAFTED** (2026-07-21). **`tasks.md`** (CT1–CT5 ∥ DT1–DT2 ∥ FT1–FT2 + DOC1 + P2 PT1–PT2; all 21 reqs mapped): **CP chain (integration, serial on `compose.test.yml`):** CT1 `BlockedUdpPort` model+migration → CT2 `services/ddos_amplification.py` (HARDCODED_AMP_PORTS + CRUD + audit) → CT3 `/ddos` router+schemas+register → CT4 `blocked_port_reconciler.py` (Reconciler+`DpstatBlockedPortsWriter`+`FakeBlockedPortsWriter`) → CT5 `worker.py` wiring + `worker_blocked_port_enabled/_interval` settings. **DP:** DT1 `dpstat set-blocked-ports` (build) → DT2 `smoke_blocked_port.sh` (dp-integration, into `make smoke`). **FE `[P]`:** FT1 types+`useAmplificationConfig` hook → FT2 `DdosProtectionPage`+Sidebar nav+`/admin/ddos` route. **DOC1 `[P]`** (README×2 + TESTING deny-stage). **P2 (optional):** PT1 `dpstat snapshot` blocked_ports read-back → PT2 effective-state panel. **All 3 mandatory pre-approval checks pass** (tables in `tasks.md`): granularity (CT4 lane / FT2 tab flagged cohesive-not-split w/ nexthop-CT2 / global-blacklist-page precedent); diagram↔`Depends on` (CT1→CT2→{CT3,CT4→CT5}; DT1→DT2; FT1→FT2; DOC1←{DT1,CT5,FT2}; PT1←DT1; PT2←{FT2,PT1}); test co-location (only `Tests: none` = DT1/PT1 userspace `dpstat` tooling **covered by the privileged DT2/snapshot smoke** = DP-established `set-bypass`/`set-nexthop` pattern, **not** deferral, + DOC1 docs). **Parallelism:** per TESTING.md only unit/fe-unit are parallel-safe → all CP tasks integration = **one serial chain**; FE (`fe` gate) + DP (`make`) tracks run **concurrently** with the CP chain (disjoint infra); `[P]` = DT1/FT1/FT2/DOC1/PT2; DT2 dp-integration not parallel-safe within DP but infra-disjoint from CP compose. Baselines pinned live at Execute (`B_cp`=`pytest -q` head ≥507, `B_dp`=`make test` head 130, `B_fe`=Vitest head ≥213); each task states its added-test floor + expected count (no silent deletions). Tools: `coding-guidelines` (all code), `docs-writer` (DOC1), `mermaid-studio` (diagrams done), **no MCPs** (Context7 unavailable). **Next:** approve tasks → **Execute** (Phase 0: CT1 ∥ DT1 ∥ FT1).

**Prior sub-step (superseded above):** **SPEC + CONTEXT + DESIGN (AD-036) DRAFTED** (2026-07-21). **Design AD-036** + 2 rendered diagrams (`diagrams/amp-config-architecture.{mmd,svg}` component/data-flow, `diagrams/reconcile-sequence.{mmd,svg}` add→reconcile→enforce; both mmdc-rendered green). **4 thin tracks over verified infra, cloning the present-in-code bypass/next-hop pattern 1:1:** (1) CP model `blocked_udp_port` (`port` natural PK `Integer`+CheckConstraint 0..65535 — **T3**, SmallInteger max 32767 too small; `note` String(256); `created_by` FK SET NULL; `created_at`) + migration (# pinned live at Execute) · (2) `services/ddos_amplification.py` (`HARDCODED_AMP_PORTS` mirror constant + `list/add/remove_blocked_port` + `record_event` `ddos.amp_port.added/removed`) + `/ddos` admin router (clone `global_blacklist.py`: `GET /ddos/amplification`→`{hardcoded_ports, dynamic_ports}`, `POST .../ports` 201/409/422, `DELETE .../ports/{port}` 204/404) + schemas · (3) `worker/blocked_port_reconciler.py` (`BlockedPortReconciler`+`DpstatBlockedPortsWriter`+`FakeBlockedPortsWriter`, clone `NodeControlReconciler`/`DpstatBypassWriter`; drift on `asserted_ports: frozenset|None`) + `worker.py` wiring + settings `worker_blocked_port_enabled`/`_interval_seconds` (reuse dpstat binary+timeout) · (4) DP `dpstat set-blocked-ports [port...]` (build 1024-word bitmap, write **BOTH** slots' inner maps via `apply_inner_fd` idiom `bpf_map_lookup_elem(outer,&slot,&id)`→`bpf_map_get_fd_by_id`; +usage/dispatch/`UDP_BLOCKED_PORT_BITMAP_PIN_PATH`) · (FE) `features/config/ddos/DdosProtectionPage.tsx`+`useAmplificationConfig.ts`+types+Sidebar NavLink+`/admin/ddos` route+Vitest (clone `global-blacklist/*`). **Load-bearing correctness (T1/T2):** **T1** API & worker are **separate processes** → propagation = PG desired-state + worker poll (~1s ≪ 5s SLA), **not** a cross-process event (corrects `context.md` A-AMP-2); **T2** writing **both** double-buffer slots is **carry-forward-safe** because `xdpgw-apply` `carry_forward_feed`/`carry_forward_service_config` use `apply_copy_outer_inner` = **pointer-install** of the active inner into inactive + **never clear content**, and the bitmap inner is **never** recreated by an apply (unlike service-scoped outers) → this is the AMP-11 proof. **T8:** zero hot-path C edit, **reuses** `DR_UDP_AMPLIFICATION_DROP=7` (no ABI append), no apply-snapshot wire change, no Redis `JobType`, no new map/pin (loader already pins `udp_blocked_port_bitmap`). **7 flags for Tasks:** F1 router/route naming (`/ddos`+`/admin/ddos` proposed); F2 periodic force-reassert (default drift-only, loader-reload self-heal optional knob); F3 P2 effective-state read-back (`dpstat snapshot` decoded set + `udp_amplification_drop` counter, AMP-20/21); F4 overlap-with-hardcoded = accept-with-note; F5 single `GET /ddos/amplification`; F6 migration#/down_revision pinned live; F7 lane wired in `worker.py` (like node_control/nexthop) not `__main__`. All 21 reqs mapped to components (design.md traceability). **Tracks:** CP-API↔CP-worker serialize on `compose.test.yml`; DP + FE run own toolchains in parallel; contract = `dpstat set-blocked-ports` argv+exit (CP tests use `FakeBlockedPortsWriter`, no hard cross-track gate). Baselines (`B_cp`=`pytest -q` head, `B_dp`=`make test` head, `B_fe`=Vitest head) pinned live at Execute. Tools: `coding-guidelines` (code), `docs-writer` (docs task), no MCPs; diagrams via `mermaid-studio`. **Next:** approve design → **Tasks**.

**Prior sub-step (superseded above):** **SPEC + CONTEXT DRAFTED** (2026-07-21, awaiting approval → Design). New feature dir `.specs/features/udp-amplification-config/` (`spec.md` AMP-01..21; P1=01..19 MVP, P2=20..21; `context.md` D-AMP-1..3, A-AMP-1..8). **Realizes the deferred D-BLK-2 / AD-022 item** (STATE deferred-ideas line ~607): the **control-plane writer** for the dynamic `udp_blocked_port_bitmap` that *Blacklist & deny filters* shipped with **enforcement only** (v1 writer = loader env `XDPGW_SEED_BLOCKED_PORT`). User request: "Thêm tab DDoS Protection cho dashboard admin: Quản lý cấu hình UDP amplification." **Spans CP (new admin model + API + audit) + worker (new reconcile lane) + DP (`dpstat set-blocked-ports` subcommand) + FE (new admin tab)** — but **zero hot-path change, zero new drop reason, zero apply-snapshot wire change, no new `JobType`** (it's a writer/UI over the existing verified BLK enforcement, ABI idx 7). **3 gray areas resolved via AskUserQuestion (all recommended options, `context.md`):** **D-AMP-1** scope = dynamic port-list CRUD + read-only view of the always-on hardcoded set (17/19/53/111/123/137/161/389/520/1900/5353/11211) — **no** node-wide toggle, **no** per-built-in override (both → Out of Scope/deferred) · **D-AMP-2** propagation = **worker reconcile lane + `dpstat set-blocked-ports`** (1:1 clone of `NodeControlReconciler`+`DpstatBypassWriter` / `NextHopResolver`+`DpstatNextHopWriter`, **present in code**) — **not** an apply-snapshot wire-format extension (option b rejected: bumps wire v3→v4 mid-flight against service-ratelimit, and the bitmap is node-global A-BLK-7 so it doesn't belong in a per-service snapshot; `xdpgw-apply` already carries the bitmap forward so a direct writer + carry-forward keeps ports enforced across swaps) · **D-AMP-3** entry model = single UDP port (0..65535) + optional note, node-global, admin-only, no expiry/ranges. **Grounding verified live in-tree:** `blacklist.h::amp_port_hardcoded` + `udp_blocked_port_bitmap` `ARRAY_OF_MAPS[SERVICE_SLOTS=2]`×inner `ARRAY[BLOCKED_PORT_WORDS=1024]×u64` (`key=port>>6`,`bit=1<<(port&63)`); `loader.c::seed_blocked_port_from_env` write idiom; `xdpgw-apply.c` `carry_forward_feed`/`carry_forward_service_config` both `apply_copy_outer_inner` the bitmap; `dpstat.c` `set-bypass`/`set-nexthop` + `worker/node_control_reconciler.py`/`nexthop_resolver.py` reconcile-lane precedents; `require_admin`/`AuditEvent`/`scrub_metadata`; SPA role-filtered `Sidebar` *Manage* group + `src/features/config/*` + `apiClient` `fieldErrorsFrom422`; **no CP model/API for blocked ports exists** (deferred D-BLK-2 confirmed). **8 assumptions (A-AMP-1..8):** new `BlockedUdpPort` model + migration (number pinned live at Execute, head `20260714_0011_alerting`); new `worker/blocked_port_reconciler.py` mirroring bypass/next-hop + jump-tick request event + `worker_blocked_port_*` settings; `dpstat set-blocked-ports` writes **both** slots (carry-forward-safe, A-AMP-3); built-in set = CP constant mirroring the authoritative DP switch (drift caveat); overlap-with-hardcoded accepted-with-a-note; router/route names = Design calls; loader env seed retained as bootstrap; ≤5 s SLA measured in live smoke. **Next:** approve spec + context → **Design** (AD-0xx: model+migration; router/route naming; reconcile lane vs fold + settings + jump-tick; `dpstat set-blocked-ports` write-both-slots; built-in-set exposure; SPA tab + Vitest). ROADMAP entry added under *Frontend / operability* (cross-cutting). *Independent of the service-ratelimit execution thread below — this is a planning-only track; no execution started.*

**Prior current work (independent active track — NOT superseded, still awaiting approval → Execute):** **Service-level rate-limit refactor (`service-ratelimit`, SVR-01..11) — SPEC + CONTEXT + DESIGN (AD-SVR-1..6) + TASKS DRAFTED** (2026-07-21, awaiting approval → Execute). **Motivation:** per-rule `allow_rule.pps/bps` (ARL) was never plumbed through the M4 apply wire format (`APPLY_SNAPSHOT_RULE_SIZE == 10` carries only ports/proto/flags) — `_rule_flags` stamping `RULE_F_PPS_SET/_BPS_SET` seeded a **zero-token bucket** → 100% of a rule's traffic dropped as `rate_limit_drop`. Root-caused live 2026-07-20 on `118.107.78.137:2283` (browser hung; `clean_pkts=0`, `rate_limit` climbing while service/rule/plan/nexthop all looked correct); **hotfix `49b7c91`** made `_rule_flags` emit `RULE_F_ENABLED` only (per-rule RL = no-op) + doc/wire-note + memory `[[allow-rule-pps-bps-flags-blackhole]]`. This feature **removes** the dead per-rule knob and adds a **single aggregate rate-limit per service**. **3 gray areas resolved via AskUserQuestion (context.md D-SVR-1..3):** D-SVR-1 identity = **new `ProtectedService.service_pps/service_bps`** (not a VIP-ceiling overload, not Plan-only) — keeps the 3 bandwidth mechanisms orthogonal (Plan=SLA fairness, VIP=bypass ceiling, SVR=clean-path rate-limit); D-SVR-2 = **both pps+bps** (each nullable); D-SVR-3 scope = **clean (non-VIP) traffic only** (VIP keeps its ceiling). **Design (AD-SVR):** clone the **VIP ceiling** machinery — `struct svc_rl_config` (=`vip_config` layout, slotted `svc_rl_config_map`) + unslotted per-CPU `svc_rl_state` (`rl_bucket`) + `svc_rl_admit()` (clone `vip_bucket_admit`, reuse `rl_burst`); wire into `rules.h::allow_rule_stage` at the matched→`admit_clean` seam, **before** the fairness ladder; **reuse `DR_RATE_LIMIT_DROP=10`** (no drop-reason ABI append); **delete** `rule_entry.pps/bps` + `RULE_F_*_SET` + `rate_limit_state`/`rl_key`/`rl_config`/`rl_bucket_admit/consume/refill/reset` (keep `rl_bucket`+`rl_burst`, shared w/ VIP). **Wire v3:** bump `APPLY_SNAPSHOT_SCHEMA_VERSION 2→3`, service record += `service_pps le64/service_bps le64/svc_rl_flags u8` (after vip_flags), `SERVICE_FIXED_SIZE 50→67`, **rule record byte-identical (10 B)**. **Migration:** add `protected_service.service_pps/bps`, drop `allow_rule.pps/bps` (old values discarded — were no-ops). **Tasks:** 2 tracks **DP∥CP**, contract = wire v3 (DT1). Phase0 `DT1`(header v3)∥`CT1`(models+migration); Phase1 `DT2`(maps+hot-path+per-rule removal, BPF_PROG_TEST_RUN)∥`CT2`(applier v3 + regen golden)∥`CT3`(API schemas+service svc); Phase2 `DT3`(`xdpgw-apply` v3 read + loader pin/seed)∥`CT4`(SPA forms)[P]∥`CT5`(docs)[P]; Phase3 `DT4`(live veth smoke: over/under-budget + whitelist-unaffected + both-unset=0). Deploy = single maintenance step (migrate+worker+apply+loader together, no v2/v3 mix). New feature dir `.specs/features/service-ratelimit/` (spec+context+design+tasks). **Next:** approve spec+context → confirm design → approve tasks → **Execute** (Phase 0: DT1∥CT1). *Independent of the next-hop thread below.*

**Prior current work (superseded above):** **Static next-hop L2 (MAC) rewrite from a BPF map (`nexthop-mac-rewrite`, `NHR-01..21`) — SPEC + CONTEXT + DESIGN (AD-035) + TASKS DRAFTED** (2026-07-20, awaiting approval → Execute). `tasks.md` (DT1–DT3 ∥ CT1–CT6; all 21 reqs mapped) landed: **2 tracks DP∥CP** run concurrently (independent toolchains/infra; CP tests use `FakeNextHopWriter`/fake snapshot so **no hard cross-track gate** — the DP↔CP contract = dpstat subcommand args + snapshot `"nexthop"` JSON, fixed in AD-035). **DP** (mirrors M6 bypass DP track 1:1): **DT1** `nexthop.h`+ABI append+`redirect_out` resolve-first/fail-closed+delete `l3_rewrite_nexthop`+bypass-verbatim+dp-unit (quick) · **DT2** loader pin+`dpstat resolve/evict/set-nexthop`+snapshot `"nexthop"` block (build; covered by DT3 smoke) · **DT3** live veth smoke: `set-nexthop` rewrite + `resolve-nexthop` ARP against a veth peer + unresolved drop (dp-integration, full). **CP** (serial on shared `compose.test.yml`): **CT1** single-IPv4-host validation+report · **CT2** `NextHopResolver` lane+`Dpstat/FakeNextHopWriter` (mirrors `NodeControlReconciler`) · **CT3** worker wiring+`worker_nexthop_*` settings+post-apply `request_resolve`/`request_evict` hook · **CT4** (P2) `/node/health` unresolved count · **CT5** (P3) manual resolve endpoint+metrics · **CT6** `[P]` docs. Only **CT6** parallel (integration = not parallel-safe per TESTING.md; DP dp-unit serial on shared C files). **All 3 pre-approval checks pass** (granularity — DT1/DT2 flagged cohesive-not-split w/ M6 precedent; diagram↔`Depends on`; test co-location — DT2 build-gated-tooling covered by the privileged DT3 smoke = DP-established pattern, not deferral). Baselines pinned live at Execute: `B_dp`=`make test` head (TESTING.md=130; AD-DP-01 +3 uncommitted), `B_cp`=`pytest -q` head; each task states its added-test floor. Tools: `coding-guidelines` (code), `docs-writer` (CT6), no MCPs. **Next:** approve tasks → **Execute** (Phase 1: DT1 ∥ CT1). New feature dir `.specs/features/nexthop-mac-rewrite/` with `spec.md` + `context.md` + `design.md` + 2 rendered diagrams (`diagrams/nexthop-architecture.{mmd,svg}` component/data-flow, `diagrams/resolve-and-rewrite-sequence.{mmd,svg}` declare→resolve + hot-path). **Design AD-035 (2 flagged items now decided):** **D-035-B bypass = verbatim** (no rewrite — bypass has no per-service context to key the map; keeps `bpf_fib_lookup` fully removed; tradeoff: routed multi-backend bypass carries ingress dst MAC = documented emergency-mode limit) · **D-035-M existing non-`/32` rows** = validate single-host on create/update + **report** existing, **no auto-convert**. Key decisions: **D-035-1** `nexthop_map` = `HASH`+`NO_PREALLOC`, unslotted runtime state keyed by `dp_id`, value `{dst_mac[6], src_mac[6], resolved, _pad, last_resolved_ns}` — **web-verified** RCU-atomic `hlist_nulls_replace_rcu` replace = tear-free whole-value hot-path read; **NOT** rebuilt by M4 #2 `xdpgw-apply` swap · **D-035-2** store both MACs per entry (one hot-path lookup) · **D-035-3** read OUT src MAC/IP **live** from `tx_devmap[0]`→ifindex via `ioctl(SIOCGIFHWADDR/SIOCGIFADDR)` — **no new seed map**, auto-tracks OUT MAC changes · **D-035-4** ARP probe **+** map write both in the C `dpstat` tool (`resolve-nexthop`/`evict-nexthop`/`set-nexthop`/`nexthop` dump; **web-verified** `AF_PACKET`/`SOCK_RAW`/`ETH_P_ARP`+`sockaddr_ll`), worker execs it unprivileged (mirrors `set-bypass`; `CAP_NET_RAW`+`CAP_BPF` isolated in the binary) · **D-035-5** new `NextHopResolver` worker lane (30-min tick + resolve queue, mirrors `NodeControlReconciler`/`DpstatBypassWriter`) · **D-035-6** immediate resolve via post-apply hook `request_resolve(dp_id, ip)` · **D-035-7** hot path checks only `resolved` (lane owns staleness). `redirect_out` reordered **resolve-first→fail-closed** (`record_drop(DR_NEXTHOP_UNRESOLVED)` before `svc_stat_clean`). **Zero new DB table** (single-host = service-layer validation; resolution state lives in the BPF map, surfaced via `dpstat` snapshot `"nexthop"` block → `/node/health` unresolved count). De-risk: T1 load+verifier probe (fallback `ARRAY`-by-`dp_id`), privileged ARP smoke before lane wiring. All 21 reqs mapped to components. **Next:** approve design → **Tasks** (DP∥CP tracks: `nexthop.h`+hot-path+ABI+loader pin+`dpstat` subcommands+`BPF_PROG_TEST_RUN`; CP single-host validation + `NextHopResolver` lane + post-apply hook + settings + `/node/health`). **Replaces the packet-time `bpf_fib_lookup` mechanism** shipped as AD-DP-01 / SLRD-27..29 with a **static, pre-resolved map lookup**: control-plane/agent resolves each single-IP service's backend MAC out of band and stores `{dst_mac, src_mac}` in a pinned BPF map; the XDP hot path does a pure lookup + `memcpy` with **no `bpf_fib_lookup` and no verbatim fallback**. Spans DP hot-path + new next-hop map, the frozen drop-reason ABI (append `DR_NEXTHOP_UNRESOLVED = 16`, `DROP_REASON_COUNT` 16→17, cap 32), the CP `ProtectedService` single-IPv4-host constraint (was `cidr_or_ip` CIDR), and a new worker **ARP-probe resolution lane**. **4 gray areas resolved via AskUserQuestion (all locked in `context.md` D-NHR-1..4):** **D-NHR-1** missing/stale MAC → **fail-closed drop** (`nexthop_unresolved` + exact per-CPU counter; last-known **not** retained; bounded retry-before-invalidate to avoid single-lost-ARP flapping) · **D-NHR-2** resolve the **service IP itself** on `OUT` (directly-connected backend, matches the verified `cyberrange02` deploy) → `dst_mac` per-service, `src_mac` = **node-global** OUT port MAC · **D-NHR-3** **active ARP probes** (raw socket, self-contained; fail → unresolved) · **D-NHR-4** **immediate resolve at apply** + 30-min (1800 s, configurable) periodic refresh. **Map placement locked as behavior:** next-hop map is **unslotted runtime state** keyed by `dp_id`, pinned under `/sys/fs/bpf/xdp_gateway/`, **NOT** rebuilt by the M4 #2 `xdpgw-apply` swap (resolution survives config apply); writer = a **`dpstat`-family privileged subcommand** the worker execs (mirrors M6 `dpstat set-bypass` / `NodeControlReconciler`). Grounded live in-tree: `l3_rewrite_nexthop()` in `xdp_gateway.bpf.c:148` (clean via `redirect_out`, bypass via `node_control.h` `redirect_out_bypass`); `service_val` **frozen 8 B** (`_Static_assert`) → MACs need a new map not a field; `drop_reason.h` 16-reason append-only ABI; `ProtectedService.cidr_or_ip` (models.py:498) `CIDR`; **no MAC/ARP/neighbor concept exists in control-plane yet**. **3 items flagged for Design (context.md):** (1) **bypass-mode L2 behavior** (no per-service context → can't key the map: verbatim vs node-global bypass next-hop vs retain fib on bypass path only) · (2) existing non-`/32` service rows at rollout (reject-at-migrate vs flag vs first-address) · (3) `ixgbe` OUT XDP-TX-ring loader auto-attach dependency (AD-DP-01 open item #1). **Next:** approve spec+context → **Design** (map value layout, worker lane vs `NodeControlReconciler` fold, `dpstat` subcommand + `/node/health` shape, bypass + migration decisions). *This feature supersedes the mechanism of the prior current-work entry below.*

**Prior current work (superseded above):** **Data-plane L2 next-hop rewrite (AD-DP-01) — IMPLEMENTED + VERIFIED** (2026-07-20). First real (non-veth) deployment on `cyberrange02` is **L3-routed**, not a pure L2 transparent bridge (gateway has IPs IN `118.107.64.250/29` + OUT `118.107.78.250/24`, `ip_forward=1`, upstream routes the protected `/24` to it). The verbatim redirect (SLRD-09) forwarded clean frames out OUT still bearing the **gateway's own IN MAC** (`00:1b:21:be:40:6e`) instead of the backend MAC (`fa:16:3e:ce:1f:88`), so the backend VM dropped them at L2 — `clean` counter rose (verdict=REDIRECT) but browser to `http://118.107.78.137:2283/` got no response. **Fix:** added `l3_rewrite_nexthop()` in `xdp_gateway.bpf.c` — `bpf_fib_lookup(BPF_FIB_LOOKUP_DIRECT)` then rewrite `eth` dst→next-hop MAC / src→OUT MAC before `bpf_redirect_map`; non-SUCCESS (transparent bridge / ARP / NO_NEIGH) = **verbatim fallback**; `ETH_P_IP` only; **TTL never decremented** (SLRD-08 holds). `ctx` threaded through `redirect_out`/`redirect_out_bypass` (`whitelist.h`/`fairness.h`/`node_control.h`). **Second gotcha found + worked around:** OUT is **ixgbe**, which needs an attached XDP prog to alloc XDP TX rings — loader attaches IN only, so devmap `ndo_xdp_xmit` failed silently (`clean` counted, `xdp_redirect_err`); manual `XDP_PASS` on OUT unblocks it (loader auto-attach = tracked follow-up). **Verified:** 133 dp-unit pass, program loads native + verifier-clean, `BPF_PROG_TEST_RUN` proves dst MAC `00:1b:21:be:40:6e→fa:16:3e:ce:1f:88` + src→OUT + IP/port/TTL intact, live `xdp_redirect`/`xdp_devmap_xmit` with `err=0`, backend serves HTTP 200. **Docs updated (this task):** PRD §2 + §8.2 note, `data-plane/README.md` (new "Forwarding: transparent bridge vs. routed next-hop rewrite" + OUT-XDP-TX section), `service-lookup-redirect/spec.md` (Amendment SLRD-27..29, amends SLRD-09) + `design.md` (implementation + tech decisions). Project memory `[[redirect-no-mac-rewrite-l3-deploy]]`. **Open follow-ups:** (1) loader auto-attach `XDP_PASS` on OUT; (2) live loader reloaded with `SERVICE_DEST=118.107.78.137` seeding a **match-all** allow rule — control-plane must re-apply to restore the precise port-2283 rule (version reset to 1); (3) code + docs uncommitted (branch `feature/dashboard`). *Independent of the config-management-SPA thread below.*

**Prior current work (superseded above):** **Configuration management SPA (admin &
tenant)** — **EXECUTE IN PROGRESS** (2026-07-15). **T19 (docs) DONE** —
`control-plane/frontend/README.md` rewritten (design system, nav IA,
apply-status UX, screen→endpoint map, links both rendered diagrams), grounded in
the real routes/sidebar/`ui` barrel/`hooks/resources`/`features/config` on disk.
**T1–T18 code present; a fix pass landed and the full `fe` gate is now GREEN**
(lint PASS · typecheck PASS · `npm run test -- --run` **48 files / 213 passed** ·
build PASS). Two defects were found during T19 and **both fixed 2026-07-15**:
(1) `npm run lint` had **64 problems** — not just `any`: it surfaced **3 real
`react-hooks/rules-of-hooks` bugs** in `AlertingPage.tsx` (delete/update/test
channel mutation hooks called *inside* handlers) + **3 `set-state-in-effect`**
(`AllocationForm`/`AllocationsPage`/`UserForm`) + 56 `no-explicit-any` + 2
unused-vars. Fixed by lifting AlertingPage mutation hooks to top level (FeedsPage
`?.id ?? ''` pattern), moving the per-row test mutation into `ChannelRowActions`
(`onTestStart`/`onTestResult`), deriving `effectiveTenantId` instead of an
auto-select effect, handling UserForm's admin tenant-reset in the role handler,
moving AllocationForm's debounce guard inside the timer, and typing the `any`s
with real DTOs (added `WebhookChannelConfig`/`EmailChannelConfig`, exported
`FeedFormPayload` — which also closed a real credential-omit type hole — typed
admin form payloads; test-double `any`s → typed mock props / `as never`).
(2) `vite.config.ts` dev proxy extended to every SPA prefix
(`/tenants`/`/users`/`/allocations`/`/me`/`/feeds`/`/blacklist`/`/alerts`/`/jobs`
added to the existing four); README dev section updated. Both logged
RESOLVED in `tasks.md` §Findings. `B_fe` now **213** across 48 files. **Nothing
committed yet (awaiting user).** Feature is functionally complete + gate-green
pending the commit decision. *Independent of the M6 #3 SLA/OLA backend thread
below.*

**Prior sub-step (superseded above):** **SPEC + DESIGN (AD-034) +
TASKS DRAFTED** (2026-07-15). `tasks.md` (T1–T19; all
53 reqs mapped) landed: single **frontend** track, **fe-unit** tests beside source
+ **fe** gate (`npm run lint && typecheck && test --run && build`) for every code
task. **T1** tokens+base+theme+`radix-ui` dep · **T2/T3/T4** primitive families
(form / overlay-nav / data-display, serialize on shared `ui/index.ts` barrel) ·
**T5** `[P]` apiClient `{detail}` parse + `fieldErrorsFrom422` + DTO `types.ts` ·
**T6** `useApplyStatus`(1 s→30 s soft-timeout)+`ApplyStatusIndicator` · **T7**
app-shell rebuild (Sidebar role-filtered + Topbar + role-routing; rehome existing
dashboards, keep tests green) · **T8** `[P]` tenant resource hooks
(services/rules/lists) · **T9→T10→T11** tenant services/detail-tabs/lists (P1
demoable slice, serial on shared service dir) · **T12–T17** `[P]` admin console
pages (tenants+users · allocations · services+plan · feeds+global-BL · alerting ·
node) · **T18** `[P]` account+job-backlog (P3) · **T19** `[P]` docs. **Parallelism
basis:** fe-unit parallel-safe only across **disjoint source files** (fe gate
independent of `compose.test.yml`); foundation/shell/tenant tasks serialize on
shared files, admin pages fan out `[P]` (run each in an isolated worktree since
`typecheck`/`build` compile all of `src/`). **All 3 mandatory pre-approval checks
pass** (granularity — T2/T3/T4/T7/T12/T15 flagged cohesive-not-split with
justification; diagram↔`Depends on`; test co-location = fe-unit everywhere, zero
deferral). **P2 endpoints verified present in-tree** (`/alerts/rules|channels|test`,
`/node/bypass|maintenance|health`) → admin screens buildable **now**, no hard M6
gate. **Radix React 19 + unified `radix-ui` package web-verified**; T1 pins exact
version + verifies peer range. Tools: `coding-guidelines` (code T1–T18),
`docs-writer` (T19), **no MCPs**. `B_fe`=`npm run test` head total (≥34) pinned
live at Execute; each task keeps the total monotonic + states its added-test floor.
**Next:** approve tasks → **Execute** (Phase 1: T1 first, then T2→T3→T4 with T5
`[P]`). *Independent of the M6 #3 SLA/OLA backend thread below.* `design.md` + 2
rendered
diagrams (`diagrams/config-architecture.{mmd,svg}` component,
`diagrams/apply-status-ux.{mmd,svg}` async-apply sequence) landed. **UI/UX
directive resolved via AskUserQuestion:** the SPA has **zero CSS today** (verified
in-tree — no stylesheet, `main.tsx` imports none, only 9 files use inline
`style={}` for chart colors), so "hiện đại, best UI/UX" means **introducing a real
design system from scratch** → user chose **"Token CSS + Radix primitives"**.
**Radix React 19 compat + the unified `radix-ui` package web-verified** (Knowledge
Chain step 4). AD-034 key decisions: **D-034-1** unified `radix-ui` + CSS Modules
(Vite-native) + CSS-variable tokens (light+dark); **D-034-2** rebuild shell
(Sidebar+Topbar) app-wide, rehome + base-restyle existing observability panels
(visual-only, behavior/tests preserved); **D-034-3** enhance `apiClient` to parse
FastAPI `{detail}` + `fieldErrorsFrom422` helper (current client throws
status-only); **D-034-4** async-apply UX = `useApplyStatus` (1 s poll while
`pending|queued|applying`, 30 s soft-timeout → slow poll + "taking longer" state,
stop at `active|failed`) + `StatusBadge` + toasts + Topbar `ApplyStatusIndicator`
(rule/list mutations reuse parent service apply-status); **D-034-5** dep-free
forms (`Field` + client validators + 422 merge, **no** react-hook-form/zod);
**D-034-6** Sidebar IA (Overview / Manage[role-filtered] / Observe) + service-detail
Radix Tabs (Overview·Rules·Whitelist·Blacklist); **D-034-7** tenant plan
display-only (`/plan` admin-only), admin plan-at-create; **D-034-8** zero backend
change, missing endpoint → `SPEC_DEVIATION`; **D-034-9** dual-theme via
`prefers-color-scheme` + `[data-theme]` toggle, fold `thresholds.ts` into tokens;
**D-034-10** Vitest primitives(a11y/keyboard)+screens(mocked apiClient/QueryClient),
existing panel tests stay green. **Async-apply contract grounded live in-tree**:
`ApplyMutationResponse{apply_status,version,active_version}` (every 202),
`ApplyStatusView{...,last_error,latest_job}` (`GET /services/{id}/apply-status`),
`ServiceResponse` carries `apply_status`/`version`/`active_version`/`plan`/`warnings`.
All 53 reqs mapped to components (Foundation→primitives→shell→data-layer =
P1 critical path, then tenant screens = demoable P1 slice, then admin console,
then P3). No `CONCERNS.md` (nothing to mitigate); `.specs/codebase/TESTING.md`
present for Tasks. **Next:** approve design → **Tasks** (single FE track;
`B_fe`=current Vitest total ≥34 pinned live at Execute; pin exact `radix-ui`
version + React 19 peer range at Execute). *Independent of the M6 #3 SLA/OLA
backend thread below.*

**Prior sub-step (superseded above):** **SPEC DRAFTED** (2026-07-15,
awaiting approval → Design). New feature dir
`.specs/features/config-management-spa/` with `spec.md` (`CFG-01..53`; P1=01..24
MVP, P2=25..50, P3=51..53). Resolves the intent fork via AskUserQuestion:
user chose **"Config-management SPA"** (the deferred CRUD screens), not a redesign
of the existing read-only observability dashboards. **Pure frontend, zero new
backend endpoint/model/migration** — surfaces the already-shipped, tested APIs.
Scope: **P1 tenant self-service** (services CRUD + enable/disable via
`load_service_for_principal` ownership guard; allow-rules ≤16/unique-priority +
overlap-check; whitelist/VIP + service blacklist) + **shell/nav** (role-aware,
reuses `AuthContext`/`ProtectedRoute`/`AppLayout`; reusable async-apply-status
primitive polling `/services/{id}/apply-status`); **P2 admin console** (tenants/
users, CIDR allocation + overlap-check, service oversight + **admin-only** plan
sizing `PATCH /services/{id}/plan`, threat feeds + `/feeds/{id}/sync` + global
blacklist, alert rules/channels + test-send, node bypass/maintenance); **P3**
account change-password (`POST /auth/password`) + admin job backlog (`GET /jobs`).
**Grounding verified live in-tree** by enumerating every router
(`app/api/routers/*`): tenant self-creates services (`_tenant_for_create` scopes to
own tenant), `/plan` is admin-only, every service/rule/list write returns `202
{apply_status, version, active_version}`. **No backend milestone gate** — P1
buildable today; P2 alerting + node-control stories **soft-depend** on M6
*Alerting* + *Bypass & maintenance* executed (config/control endpoints must exist).
Reuses the M5 SPA (Vite+React+TS, TanStack Query, existing `apiClient`). Out of
scope: any backend change, telemetry/billing/alert-history **views** (already
shipped), audit-log viewer (M6 SLA/OLA owns `/audit`), SSE/WebSocket, SSO/MFA.
ROADMAP gains a new "Frontend / operability" cross-cutting section. **5 open gray
areas flagged for Design** (async-apply UX depth; form/validation library vs plain
React+TanStack; tenant plan display-only; admin plan-at-create; navigation IA).
**Next:** approve spec → **Design** (or discuss the 5 gray areas first). *This
feature is independent of the M6 #3 SLA/OLA work below, which remains the active
backend thread.*

**Prior current work (superseded above):** M6 #3 → **SLA/OLA reporting & audit**
(final M6 feature) — **SPEC + CONTEXT DRAFTED** (2026-07-14, awaiting approval →
Design). New feature dir `.specs/features/sla-ola-reporting/` with `spec.md`
(SLA-01..36; P1=01..27 MVP, P2=28..33, P3=34..36) and `context.md` (D-SLA-1..4,
A-SLA-1..8). **Control-plane/worker only, zero hot-path change, no new DP
surface/counter, no fabricated numbers** — reads only already-persisted evidence.
Scope: a worker background lane materializes a per-(tenant, period) **SLA report**
(met/missed per dimension, `open`→`final` immutable, unique `(tenant,period_start)`,
tied to the `BillingUsage` UTC calendar-month period reusing `billing_period.py`)
+ an admin **OLA** operational summary; plus an **admin-only** queryable/exportable
view over the **already-complete** audit write path. **4 gray areas resolved via
AskUserQuestion (all recommended options):** **D-SLA-1** compute/storage = new
worker lane → immutable period rows (running `open` finalized to `final`; API/export
read stored rows, never recompute; not a Redis `JobType`); **D-SLA-2** dimensions =
**measurable-only from persisted evidence** — committed-clean-bandwidth honored (from
persisted fairness-breach `Alert` durations, Alerting M6 #2), billing/chargeback
(finalized `BillingUsage`, never recompute p95), OLA apply-reliability
(`AgentJob failed`)/feed-health (`FeedSyncRun`)/bypass-maintenance windows
(`node.bypass.*`/`node.maintenance.*` `AuditEvent` + `NodeControl`); **Availability
+ added-latency-p99 disclosed `best_effort`/not-measured (AD-007 / A-TEL-8), never
scored or fabricated**; absent source → `insufficient_data` (fail-safe); **D-SLA-3**
audit half = **admin-only read/query/export** (`GET /audit` newest-first via
`ix_audit_events_created_at` + filters + CSV/JSON export) over the finished write
path (**41 `record_event` sites**, full dangerous-action taxonomy incl.
`tenant.delete`/`service.delete`/`feed.delete`/`node.bypass.*`/`node.maintenance.*`;
`scrub_metadata` strips secrets) — **no write-path change**; admin-only is structural
(`AuditEvent` has **no `tenant_id`**, mirrors Alerting D-033-2 "no email column";
tenant self-audit deferred); **D-SLA-4** delivery = API + CSV/JSON export + **P2 SPA
panel**, SLA (tenant-facing) vs OLA (admin-scoped) split (tenant never sees OLA/
another tenant, §5.2), **no auto-email in v1** (scheduled email = P3 hook reusing
Alerting SMTP, SLA-36). **Execute readiness:** all evidence sources are **present
in-tree** (verified live: `BillingUsage`+migration `_0009` = chargeback executed,
`NodeControl` `_0010`, Alerting models `_0011` with the evaluator lane landing now)
→ **no hard external gate**; committed-honored soft-coordinates on *Alerting*'s
fairness-breach `Alert` rows (degrades to `insufficient_data` until present). New
models + migration are **`_0012`** (after head `20260714_0011_alerting`). Grounding
verified live in-tree: `AuditEvent` fields + `record_event`/`scrub_metadata` + 41
action taxonomy (**no `audit.py` router exists yet**), `BillingUsage` open→final at
`models.py:611`, `billing_period.py` UTC-month, billing/telemetry export+retention
patterns, M5 SPA shell. **8 assumptions (A-SLA-1..8)** flagged (new in-worker lane
not a JobType; zero DP; additive models + migration `_0012` pinned live; sources
present now; never fabricate; committed-honored via `Alert` not a new counter; audit
admin-only read-only secrets-scrubbed; RBAC reuse + tenant scope via denormalized
`tenant_id`).
ROADMAP entry PLANNED→IN PROGRESS. **Next:** approve spec+context → **Design**
(AD-0xx: report lane + `SlaReport`(+dimension detail / target-override) models +
migration + `/sla` & `/audit` routers + P2 SPA panel; resolve model shape /
committed-honored math / tick cadence at Design). M6 #1 bypass-maintenance + M6 #2
alerting remain drafted/awaiting Execute.

**Prior current work (superseded above):** M6 #2 → **Alerting** — **SPEC + CONTEXT
+ DESIGN (AD-033) + TASKS DRAFTED** (2026-07-14, awaiting approval → Execute).
`tasks.md` (T1–T12; all 42 reqs mapped) landed: **T1** extract `telemetry_math`
committed-bps helper (re-point telemetry router) · **T2** `[P]` `alert_rules`
§9.3 catalog + pure predicates · **T3** 4 models + 5 enums + migration
`20260714_0011_alerting` · **T4** `AlertSources`→`AlertInputs` reader · **T5**
`AlertEvaluator` lifecycle (pending→firing→resolved debounce/hysteresis/dedup/
re-notify/auto-resolve + run_loop + critical→audit + maintenance-silence gate) ·
**T6** `NotificationDispatcher` + email(stdlib smtplib/to_thread)/webhook(httpx)
channels + scoped routing/isolation + min-severity + test-send · **T7** worker
wiring + `worker_alert_*` settings + history retention prune · **T8** `/alerts`
tenant-scoped history read · **T9** `/alerts/rules`+`/alerts/channels` admin
config (secret write-only, audited) + test-send · **T10** SPA `AlertsPanel`
(P2, fe, gated telemetry FE) · **T11** P3 ack + export · **T12** `[P]` docs.
Single CP/worker track, **zero DP work**; only **T2** (unit) + **T12** (docs) +
**T10** (fe toolchain) are `[P]`, all other tasks are integration → serialize on
`compose.test.yml`. All 3 mandatory pre-approval checks pass (granularity,
diagram↔`Depends on`, test co-location per TESTING.md matrix). Tools:
`coding-guidelines` (code), `docs-writer` (T12), no MCPs. Baselines pinned live at
Execute: `B_cp` = `pytest -q` head total (≥507), `B_fe` = telemetry Vitest total
(≥34). Execute is CP/worker-only and buildable now (telemetry executed;
`NodeControl` table exists at `_0010`; fixture source rows for tests). **Next:**
approve tasks → **Execute** (Phase 1: T2 `[P]` alongside T1 then T3).

**Prior sub-step (superseded above):** SPEC + CONTEXT + DESIGN (AD-033) drafted
earlier this session (awaiting approval → Tasks) — now advanced to tasks. `design.md`
+ 2 rendered diagrams (`diagrams/alerting-architecture.{mmd,svg}`,
`diagrams/alerting-sequence.{mmd,svg}`) landed. **Control-plane/worker only, zero
DP surface** — reads existing persisted sources. AD-033 key decisions: **D-033-1**
rules bind to structured source rows (not audit-log parsing; `AuditEvent` = the
critical-alert *write* target); **D-033-2** routing/isolation keyed by
`NotificationChannel.tenant_id` (**no email column exists** on User/Tenant — node
alerts→NULL-scope channels, service→owner+NULL; §5.2 structural); **D-033-3**
single `Alert` row per `(rule,scope)` walking `pending→firing→resolved` with all
streak/notify state in-row (restart-safe) + partial-unique dedup index;
**D-033-4** new `AlertEvaluator` worker lane (15 s tick, `fire_ticks`/`clear_ticks`
= 2), not a Redis `JobType`; **D-033-5** webhook=httpx (existing dep), email=stdlib
`smtplib`/`to_thread` (no new dep); **D-033-6** thresholds mirror
`frontend/theme/thresholds.ts` §9.1 + per-rule admin override; **D-033-7** in-worker
lane detects backlog/stuck-applying/telemetry-staleness — **full worker-process
death is a documented external-monitor gap** (can't self-detect); **D-033-8** 4
new tables (`alert_rule`/`notification_channel`/`alert`/`alert_notification`) +
migration `20260714_0011_alerting` (down_revision = current head `_0010_node_control`),
SET-NULL durable history + name snapshots. Refactor: extract
`_committed_clean_bps` (telemetry router L460) → `services/telemetry_math.py`
(import-only, no logic fork). **8 open flags for Tasks** (tick cadence, email
transport, helper extraction, attack-onset metric, rule-catalog upsert, config-UI
scope, worker-death gap, migration pin). All 42 reqs mapped to components. Grounding
verified live in-tree (worker lane Protocol, `record_event`/`scrub_metadata`,
source model fields, RBAC guards, httpx dep, no SMTP lib, migration head `_0010`).
ROADMAP updated. **Next:** approve design → **Tasks** (single CP/worker track:
models → `alert_rules` unit `[P]` → `AlertSources` → `AlertEvaluator` → dispatcher
→ `/alerts` API → worker wiring; P2 SPA panel gated on telemetry FE; DP work = zero).

**Prior sub-step (superseded above):** SPEC + CONTEXT drafted earlier this session
(awaiting approval → Design) — now advanced to design. New feature dir
`.specs/features/alerting/` with `spec.md` (ALRT-01..42; P1=01..34 MVP, P2=35..39,
P3=40..42) and `context.md` (D-ALRT-1..4, A-ALRT-1..8). Four gray areas resolved
via AskUserQuestion (all recommended options): **D-ALRT-1** evaluation = a new
dedicated `AlertEvaluator` worker background lane (so counter-derived alerts —
attack onset / near-capacity / bloom-FP / fairness — exist, not event-only);
**D-ALRT-2** stateful lifecycle tables `AlertRule`/`Alert`/`AlertNotification`/
`NotificationChannel` (dedup key `(rule,scope)`, firing→resolved, restart-safe —
supersedes bypass-maintenance's "no alert table" note; critical alerts *also*
write `AuditEvent`); **D-ALRT-3** admin-global channels (SMTP + generic webhook) +
§9.1-seeded tunable thresholds (mirror `frontend/theme/thresholds.ts`) + ownership
routing (service→owning tenant+admin, node→admin only, §5.2); **D-ALRT-4**
for-duration (N ticks) firing + separate lower clear-threshold hysteresis band +
dedup + re-notify window + auto-resolve (M ticks). Whole feature is **worker/
control-plane only, zero hot-path change** — reads existing `NodeHealthSnapshot`/
`TelemetryCounter`/`AgentJob`/`FeedSyncRun`/`NodeControl`/`AuditEvent`. **Execute
gate = Telemetry & dashboards executed (satisfied — VERIFIED 2026-07-14)** +
agent-worker (satisfied); bypass-maintenance = soft coordination only (ALRT-19
fires once M6 #1 lands). ROADMAP Alerting entry updated PLANNED→IN PROGRESS.
**Next:** approve spec+context → **Design** (AD-0xx: lane + 4 models + migration +
`/alerts` & `/alert-channels` routers + SPA panel P2). Other M6 #1
bypass-maintenance tasks remain drafted/awaiting Execute.

**Prior current work:** M5 → **Telemetry & dashboards** —
**FULLY EXECUTED / VERIFIED** (P1 2026-07-13; P2/P3 2026-07-14). All 40 TEL
requirements are verified. This 2026-07-14 run closed the feature: **T14** full
CP gate re-run green after billing landed (`pytest -q` → 507 passed), **T15**
`20c304b` added the P2 SPA panels (`TopTalkersPanel`, `BloomFpPanel`,
`CommittedHonoredPanel`, `FeedStatusPanel`) + `theme/thresholds.ts` §9.1
display-only severity coloring, and **T16** `46bdd9b` added the telemetry
history/export endpoints (`GET …/telemetry/history` + `/telemetry/export?format=csv|json`)
and the `TrendChart` wired into both dashboards. Final 2026-07-14 gates:
control-plane `ruff`/format/`mypy` clean and `pytest -q` → **507 passed** (18
pre-existing Pydantic warnings); frontend lint/typecheck/Vitest → **17 files /
34 tests** plus production build. New panels/endpoints are covered by unit +
integration gates; a live browser walkthrough of the P2/P3 surfaces was not
re-run (the P1 SPA serving path was already browser-validated 2026-07-13).
**Next phase:** M5 *Chargeback metering* is executed; resume M6 (bypass &
maintenance — tasks drafted).

**Prior current work (P1):** M5 → **Telemetry & dashboards P1** —
**EXECUTED / VERIFIED** (2026-07-13). T1–T12 and T17 are complete. This run
landed T7 `8efba3b`, T9 `c2d4239`, T8 `17f0aa9`, T11 `77753e4`, and T12
`6e335e8`; T17 records the operational and testing conventions. Final gates:
control-plane `ruff check`/format/mypy clean and `pytest -q` → **450 passed**
(18 existing Pydantic deprecation warnings); frontend lint/typecheck/Vitest →
**9 files / 13 tests** plus production build; data-plane build/quick →
**130 passed** and privileged redirect/fairness/apply smoke passed. Browser
validation against a FastAPI-served production bundle confirmed `/tenant` and
`/admin` history routes, unauthenticated-session redirect to Login, a missing
asset's 404, tenant-only service selection, two-second polling, and the
critical `generic` XDP health treatment.

**P1 defaults and deferral:** Telemetry is a worker background lane, not a
`JobType`; `worker_telemetry_interval_seconds` is an integer 1–2 (default 2).
`CONTROL_PLANE_FRONTEND_STATIC_DIR` opts FastAPI into serving the built SPA with
an HTML-only browser-history fallback while preserving API and missing-asset
404s. TEL-01–30 are verified. T13–T15/TEL-31–38 remain deferred to P2, and
T16/TEL-39–40 remain deferred to P3; alerting, chargeback, and configuration
management screens remain out of scope for this P1 release.

**Prior current work:** M4 #3 → **Threat intelligence feed sync** — **EXECUTED / VERIFIED** (2026-07-13). All of T1–T15 landed. T1–T13 committed earlier (`021f567..ae33648`); **T14** (`xdpgw-apply` `GLOBAL_DENY` mode + shared pin-dir lock + inverse carry-forward + global-apply smoke/scale) and **T15** (operations docs + final traceability) executed and committed this session. **Gates re-run green this session:** control-plane `ruff check`/`ruff format --check` (128 files)/`mypy app/` (60 files) clean, `pytest -q` → **435 passed** (100 unit + 335 integration, 89 s); data-plane `make bpf skel loader dpstat apply` + `make test` green (test_parse 130 + `test_snapshot` service+global golden self-tests); `make smoke` green (redirect TTL/csum, fairness, apply `active_config` flip in 78 ms — no regression); **`make globalapplysmoke`** → fake feed snapshot reached `blacklist_drop` via the real helper while an unlisted source stayed delivered; **`make globalapplyscale`** → **1,048,576** distinct entries loaded in **6925 ms**, **1,048,577** rejected before flip (helper peak RSS ~17.6 MB; kernel BPF-map footprint via cgroup = n/a in this container). **External gate resolved:** M4 #2 double-buffer (`7fcfb1b..86120bc`) is executed, which unblocked T12–T14. `spec.md` moves all 40 FEED reqs to **Verified**; `tasks.md` Execution Results + Done-when boxes back-filled; ROADMAP marks the feature VERIFIED. **Deviations:** none material — one pre-existing Pydantic `__fields_set__` deprecation warning in `services/feeds.py:407` (non-blocking, fallback branch only). **Next phase:** M4 is complete (agent-worker VERIFIED, double-buffer executed, threat-feed-sync VERIFIED) → resume **M5** Execute (Telemetry & dashboards / Chargeback metering are design+tasks-drafted).

**Prior current work:** M4 #2 → **Double-buffer map build/swap** — Execute underway. **T1 landed (uncommitted)**: `apply_snapshot.h` + `fair_budget.h` (new) + `loader.c` 14 config-map pins; DP quick gate green at **112** (telemetry DP tests raised the live baseline from the stale tasks.md `B_dp=91`). **T2 (helper scaffold/parser/golden fixture) is next**, blocked pending a **contract correction now applied (2026-07-13)** for two T2-blocking seams found in T1's uncommitted wire header — fixed *before* the parser/fixture bake the layout in: **(1) VIP is service-level** — `vip_pps/vip_bps/vip_flags` moved from the per-whitelist record up to the service fixed record (one `vip_config` keyed by `dp_id`, matching `struct vip_config` in `whitelist.h` + `ServiceConfig.vip_pps/vip_bps`); `wl[]` entry is now `{prefixlen, src_be32}` only. Constants `APPLY_SNAPSHOT_SERVICE_FIXED_SIZE` 33→**50**, `WHITELIST_ENTRY_SIZE` 25→**8**. **(2) `dp_id` surrogate** — wire `service_id`→`dp_id` (the AD-030 D-030-4 `u32` surrogate = `ProtectedService.dp_id`, **not** the UUID); `ServiceConfig` gained additive `dp_id: int` populated in `load_service_config` (T6 serializer sources it). Schema stays **v1** (nothing consumed it pre-correction; no bump). Edited: `data-plane/src/apply_snapshot.h`, `design.md` §Data Models (AD-028), `control-plane/app/worker/applier.py`, `tasks.md` T2 note. Verified: DP `make test` **112** unchanged (header inert until T2); `ServiceConfig` imports/parses OK, `dp_id` at field index 1; sole construction site (`load_service_config`) updated, no test constructs it directly. Next: **T2** — parse + `create_inner_like` fresh-inner de-risk + golden fixture against the corrected layout.

**Prior current work:** M5 → **Telemetry & dashboards** — Execute was partially
complete. This historical note predates T5 verification; see the authoritative
update above for the current P1 status.

**Previous current work:** M4 → **Threat intelligence feed sync (M4 #3)** — spec
**APPROVED**, AD-029 Design **APPROVED**, and `tasks.md` drafted (T1–T15), with 40/40 requirements mapped
to both Design and Tasks and two rendered diagrams. Design resolves the five open questions with:
run-linked `AgentJob` lifecycle adapters;
many-to-many `FeedBlacklistAssertion` provenance + manual promotion/demotion; PostgreSQL `cidr && cidr`
overlap with GiST; desired-vs-active global-deny digest/revision tracking; and a `GLOBAL_DENY` mode on the
same planned `xdpgw-apply` binary with inverse carry-forward + shared helper lock. Fetch defaults are
5 s connect / 10 s read inactivity / 30 s wall / 32 MiB, interval range 300..604800 s, and network wait
runs in one bounded background lane so ordinary service applies keep the foreground worker lane. M4 #1
agent-worker is **EXECUTED** (262 tests); only T12–T14/real DP propagation remain hard-gated on M4 #2.
Mandatory Tasks checks all pass; only T2/T3 are parallel. Next: **approve Tasks → Execute T1** (the
control-plane track can start before M4 #2).

> This authoritative update supersedes stale prerequisite/status wording in the historical planning
> snapshot retained below.

**Current Work (planning track):** M4 → **Threat intelligence feed sync (M4 #3)** — **spec + context drafted, awaiting approval** (2026-07-10): `.specs/features/threat-feed-sync/` spec.md (FEED-01..40) + context.md (D-FEED-1..3, A-FEED-1..8). The **authoritative writer** of `source=feed`/`scope=global` `BlacklistEntry` — fulfils AD-011's feed-population deferral and M4 #2's explicit "feed sync must be slot-aware — its problem" punt. Owns: `ThreatFeedSource` model + `/feeds` CRUD + `POST /feeds/{id}/sync`; `JobType.feed_sync` + handler (fetch→parse→validate→normalize→dedup→reconcile into global blacklist); in-worker due-time scheduler; whitelist-overlap flag/alert; `FeedSyncRun` stats; and the **slot-aware global-deny map rebuild + atomic swap** reusing M4 #2's `xdpgw-apply` helper. **3 gray areas resolved via discuss:** **D-FEED-1** DP scope = **included** — a successful `FEED_SYNC` drives a slot-aware rebuild of the feed-owned maps (`global_blacklist_bloom`/`lpm`, `gbl_meta`) into the inactive slot via the M4 #2 helper, **carrying forward all service-scoped maps unchanged** (inverse of M4 #2's carry-forward), single atomic `active_config` flip, fail-closed abort-before-flip → **Execute hard-gated on M4 #2 executed**; **D-FEED-2** feed format = **plain IPv4/CIDR line lists only** (comment/blank tolerant, reuses AD-010 `ipaddress` canonical validation; JSON/CSV/STIX deferred, `format` column kept forward-compat); **D-FEED-3** scheduling = **in-worker per-source due-time scheduler** (agent-worker tick enqueues `FEED_SYNC` where `next_sync_at≤now`; `next_sync_at=finished_at+interval`; no double-enqueue; restart catch-up; no external cron). **Resilience:** per-source isolation (one bad feed never affects another or the active DP version); keep-last-active on fetch/timeout/oversize/non-2xx/zero-valid (empty 200 = failed); manual>feed precedence, multi-source dedup collapses to one global row (preserves `uq_blacklist_global_source_cidr`). **8 assumptions flagged** (A-FEED-1..8): feeds node-global/admin-only; global-deny only; `BlacklistEntry` gains nullable `feed_source_id` FK for provenance/per-source removal; "alert"=audit+event now, M6 delivers; HTTPS GET capped, credential=env-ref secret name never logged (§7.2); invalid-line policy (partial vs failed); **`AgentJob.target_id` is NOT-NULL FK to protected_service → `FEED_SYNC` needs ledger generalization (polymorphic target) or a dedicated path — Design call**; 1M envelope bounds feed size (reuses AD-023 `blbulk`). **5 open questions for Design** (ledger shape, global-deny apply channel / inverse helper mode + shared `active_config.version`, provenance model vs partial-unique index, overlap detection SQL-vs-worker, fetch-limit defaults). **Requires agent-worker (M4 #1) AND double-buffer (M4 #2) executed** — both currently Tasks-approved/drafted, not executed; CP sync core buildable ahead of the DP path (agent-worker placeholder precedent), gate localizes to the P1 DP-propagation story. Reuses AD-023 global-blacklist build contract + `BlacklistEntry` consumption (blacklist-filters VERIFIED), audit + `AgentJob` ledger infra (M1). Next: **Design** (resolve the 5 open questions → AD-029). — Prior current work: M4 **Double-buffer map build/swap (M4 #2)** — **Tasks drafted** (2026-07-10, awaiting approval → Execute): tasks.md (T1–T8, all 28 DBS reqs mapped; **HARD GATE: agent-worker M4 #1 must be executed first** — it's Tasks APPROVED, not executed). Baselines pinned live at Execute: **dp `make test` B_dp=91**, **CP `pytest` B_cp = agent-worker's final count** (AD-027 static 209 + its tests). **Structural key:** `xdpgw-apply` core factored into fd-taking fns (`build_inactive_slot`/`carry_forward_feed`/`verify_slot`/`commit`) so the whole build→verify→flip→verdict path runs in-harness under `make test` (skel fds, `BPF_PROG_TEST_RUN`); only pin-open + subprocess `main()` needs privileged smoke. **Tasks:** T1 contracts (`apply_snapshot.h`+`fair_budget.h`) + loader pins 14 config maps + fair-math extract (verdict-neutral, build+91) · T2 `xdpgw-apply` scaffold: parser + `create_inner_like` + **fresh-inner de-risk dp-unit** (the fail-fast novel composition) + `make apply`/`applybulk` + golden fixture C-parse (make test 92) · T3 build/carry-forward/verify/single-write-flip **core** + verdict dp-unit (≥97) · T4 fail-closed rollback + version/idempotency dp-unit (≥101) · T5 `main()` pin-open + subprocess CLI + privileged smoke + `applybulk` ≤5 s/scale measure (full+scale) · T6 `[P]` `DoubleBufferApplier`+`load_node_config`+`serialize_node_snapshot`+`__main__` DI swap+`Settings` fields, fake-helper integration + serialize-round-trip unit (CP full) · T7 `[P]` dpstat `active_config` slot/version (build) · T8 `[P]` docs (`docs-writer`). C-track T1→T5 **serial** (shared `xdpgw-apply.c`/`test_parse.c`/`loader.c` + privileged smoke); only T6/T7/T8 `[P]` (disjoint files/infra). All 3 pre-approval checks pass (granularity, diagram↔def, test co-location). Snapshot round-trip co-located across languages: C-parse of golden fixture in T2 build self-test, Python-emit-identical-bytes in T6 unit — same committed `apply_snapshot_golden.bin`. Tools: `coding-guidelines` T1–T7, `docs-writer` T8; no MCPs (Context7 unavailable). Next: **Execute** (blocked on agent-worker; then T1 first — T2 de-risk = the fail-fast moment). **Design drafted** (2026-07-10): design.md (**AD-028**) + 2 rendered diagrams (`diagrams/apply-dataflow.{mmd,svg}`, `diagrams/build-verify-swap.{mmd,svg}`). Spec + context complete: `.specs/features/double-buffer-swap/` spec.md (DBS-01..28) + context.md (D-DBS-1..3, A-DBS-1..8). **Design summary (AD-028):** `DoubleBufferApplier` (new class in `app/worker/applier.py`, swapped in at `__main__` DI for `PlaceholderApplier`, boundary `apply(ServiceConfig)` unchanged — A-DBS-1) does **zero BPF in Python** — it (1) loads a consistent full-node config snapshot from PG (all enabled services + plan/rules/wl/service-bl via `load_node_config`, reusing AD-027 `ServiceConfig`), (2) serializes it to a temp file in the new **`apply_snapshot.h` versioned explicit-LE binary wire format** (first CP↔DP data crossing; golden-fixture round-tripped), (3) `asyncio.create_subprocess_exec`s a **new C helper `data-plane/tools/xdpgw-apply.c`** with a timeout, mapping exit 0→success / nonzero→`ApplyError(stderr)`→`mark_failed`. **Grounding discovery:** outers are `ARRAY_OF_MAPS[2]` with **static** inner_0/inner_1 (BTF `.values`); only observability maps are pinned today; headers **already document** the M4 contract ("build fresh inners, swap into the inactive slot, keep value_size/max_entries/map_extra equal"). Helper algorithm: read `active_config{A,V}` fresh → `inactive=1-A` → for each service-scoped outer (`service_map`/`rule_block_map`/`whitelist_bloom`+`lpm`/`vip_config_map`/`service_blacklist_bloom`+`lpm`/`fair_config_map`) **create a fresh inner meta-equal to the active inner via `bpf_map_get_info_by_fd`→`bpf_map_create`(btf_fd=0)**, populate from snapshot (loader leaf-writer idioms + shared fair math + env node knobs), `bpf_map_update_elem(outer,inactive,fresh_fd)` install → **carry-forward feed-owned deny maps** (`global_blacklist_bloom`/`lpm`, `udp_blocked_port_bitmap` pointer-copy the active inner fd into inactive; `gbl_meta` row copy — O(1), 1M list never rebuilt per job) → **structural read-back `verify_slot(inactive)`** → **single `bpf_map_update_elem(active_config,0,{inactive,V+1})` COMMIT**. Abort-before-flip is the entire rollback (only ever writes the inactive slot; flip = one aligned `__u32` → no torn slot read; crash mid-build → live slot untouched, next apply overwrites inactive). **Loader change (additive):** pin the 14 config maps (mirrors observability pin/unpin/rollback), extract fair budget math to shared `fair_budget.h`; **dpstat** gains an `active_config` slot/version section (DBS-27); env node knobs (capacity/k/ref_pkt) stay loader-env (D-FAIR-2), per-service committed/ceiling from snapshot. `Settings` +`worker_apply_binary_path`/`worker_apply_timeout_seconds` (default 5 s). **No CP schema change/migration; no new `JobType`** (full-rebuild-per-`SERVICE_UPDATE` subsumes `MAP_REBUILD`/`ACTIVE_SLOT_SWAP`, A-AGW-4). **1 load-bearing new fact web-verified:** userspace `bpf_map_create`+`bpf_map_update_elem(outer,slot,inner_fd)` runtime inner replacement; inner must match template meta (BTF not compared → btf_fd=0 ok); BPF progs can't modify outer entries (userspace-only) — kernel map_of_maps.html + eBPF-docs + libbpf#834. The one novel composition (separate-process fresh-inner install into a **pinned** outer) is **de-risked fail-fast at the first Execute gate** with a 3-rung fallback ladder (pin fresh inners / loader pre-pins static inners → loader hosts apply via IPC). 6 flags for Tasks (loader-refactor depth = minimal fair_budget.h extraction; binary snapshot vs JSON; ≤5 s test placement gated/manual; fresh-inner de-risk rung; helper computes Σcommitted headroom; apply timeout default+attempts). Original spec/context notes below. The **authoritative writer** that fills the agent-worker's `Applier` boundary — replaces `PlaceholderApplier` with `DoubleBufferApplier` (impl swapped behind the boundary, not the boundary — D-AGW-1), making a committed `SERVICE_UPDATE` actually reach the XDP hot path. 3 gray areas resolved via discuss: **D-DBS-1** write mechanism = a new **C apply-helper binary** the worker shells out to (reuses loader `seed_*`/inner-map-creation/`bpf_map_update_elem` routines; blooms replace-only + LPM inner creation stays in audited C; `bpftool`/Python-native rejected) → loader must now **pin** the ~11 slotted config maps + `active_config` (A-DBS-3, additive to observability pins); **D-DBS-2** **full-node rebuild every job** (all active services' service-scoped maps: `service_map`/`rule_block`/`whitelist`+`vip`/`service_blacklist`/`fair_config`) into the inactive slot from PG, with feed-owned global deny maps (`global_blacklist_bloom`/`lpm`, `udp_blocked_port_bitmap`, `gbl_meta`) **carried forward** (M4 #3 owns their content) so a per-service swap never drops the global blacklist — the 1M global blacklist is NOT rebuilt per job (keeps ≤5 s); on flip all services go live together, `mark_active` advances only the triggering service's per-service `active_version` (state machine per-service, slot = physical carrier); **D-DBS-3** verify = **structural read-back** of the inactive slot before the flip (every enabled svc present, `rule_block` version/count match, inner fds installed) — live-`BPF_PROG_TEST_RUN`-probe deferred. Swap point = single `active_config[0]={active_slot,version}` write (grounded: hot path reads `active_slot` at ingress via AD-015); rollback = **abort before flip** (never flip-then-flip-back — nothing became live); no new `JobType` (A-AGW-4 preserved, full-rebuild-per-`SERVICE_UPDATE` subsumes `MAP_REBUILD`/`ACTIVE_SLOT_SWAP`); `active_config.version` = node-global map version (distinct from per-service `active_version`); re-validates ≤5 s (A-AGW-7) with real builds + gated scale-envelope rebuild-cost measurement. 8 flags for Design (A-DBS-1 boundary unchanged/impl swapped; A-DBS-2 helper input = worker-serialized node snapshot vs helper reads PG; A-DBS-3 config-map pinning additive; A-DBS-4 carry-forward preserves live global deny maps; A-DBS-5 node map version vs per-service `active_version`; A-DBS-6 no unsolicited startup swap AGW-21; A-DBS-7 ≤5 s measured not asserted; A-DBS-8 single writer). **Requires agent-worker executed first** (M4 #1; currently Tasks APPROVED — Execute gated on it). Reuses frozen M4 build contracts (AD-015/019/021/023/025) + loader pin pattern (AD-017); D-SLRD-1 loader env seed downgrades to initial-slot bootstrap only. Next session: **Design** — resolve helper input channel + carry-forward mechanics + boundary DI shape.

**Prior planning (chargeback-metering):** M5 → **Chargeback metering** (second/last M5 feature; sibling = *Telemetry & dashboards*) — **spec + context + design APPROVED + tasks drafted, awaiting Execute** (2026-07-10). **Tasks drafted (2026-07-10):** tasks.md (T1–T10; all 33 reqs mapped; **single control-plane/worker track, zero DP work**; baseline `B_cp = pytest -q` pinned live at Execute ≥ 262 agent-worker + feed + telemetry). **Phase 1 (unit `[P]`):** T1 `services/billing_period.py` UTC calendar-month `month_period`/`previous_period` · T2 `services/billing_metrics.py` `p95_nearest_rank` + `bps_to_gbps` (×8 bytes→bits asserted). **Phase 2 (integration):** T3 `BillingSample`(CASCADE, `UNIQUE(service_id,sample_ts)`)+`BillingUsage`(SET NULL, `UNIQUE(service_id,period_start)`)+`BillingStatus` enum + migration `..._0009_billing` (down_revision = telemetry models migration, pinned live). **Phase 3 (integration, two serialized branches):** T4 `BillingMeter.sample_once` (reader delta/reset/idempotent upsert, `FakeTelemetryReader`) → T5 rollup (`refresh_open_periods` p95→`billed=max(committed,p95)`+`finalize_due_periods` open→final + orphan NULL-service finalize; deps T1,T2,T4) → T6 `prune_samples`+`run_loop`+`Worker` lane wiring+`worker_billing_*` settings; and T3→T7 `/billing` usage+export router (tenant `tenant_id` filter+`load_service_for_principal` 404, admin all, CSV/JSON finalized) → T8 history endpoint (P3). **Phase 4:** T9 SPA showback panel (**P2, fe gate, gated on telemetry FE shell executed**), T10 docs `[P]` (`docs-writer`; TESTING.md billing layers + p95/units/period conventions). **Parallelism:** only T1/T2 (unit) + T10 (docs) `[P]`; T3–T8 serialize on shared `compose.test.yml` even where code-independent (T7-chain ⊥ T4-chain but not parallel-safe). **All 3 mandatory pre-approval checks pass** (granularity — T3/T6/T7 flagged cohesive-not-split; diagram↔`Depends on`; test co-location — every layer's tests co-located, FE = telemetry-established `fe` layer). Tools: `coding-guidelines` all code, `docs-writer` T10, **no MCPs** (Context7 recorded unavailable; nothing needs external lookup). The 7 design flags are resolved by the chosen task shapes (reader injected/shared; nearest-rank p95; retention 400 d setting; `tenant_id` FK SET NULL; single 5-min tick; export CSV columns in T7 done-when; `period=YYYY-MM`). Next: **approve tasks → Execute** (Phase 1 T1+T2 `[P]`), **hard-gated on telemetry executed** — CP slice (T1–T8) buildable/testable now vs `FakeTelemetryReader`; T9 waits on the telemetry SPA. **Design drafted (AD-031, 2026-07-10):** design.md **Design drafted (AD-031, 2026-07-10):** design.md + 2 rendered diagrams (`diagrams/billing-architecture.{mmd,svg}` data-flow, `diagrams/metering-cycle.{mmd,svg}` sequence), grounded by reading the real worker lane/settings/deps/models + the approved AD-030 sibling. **Control-plane-only, zero new data-plane surface** — reuses telemetry's exact `clean_bytes` counter + `dpstat snapshot --json`/`TelemetryReader` + `ProtectedService.dp_id`. **Decisions (AD-031):** D-031-1 reuse the telemetry reader (inject `TelemetryReader`; `snapshot()` reads never consume, so billing keeps its **own** `prev[dp_id]` cumulative baseline, independent of the 2 s aggregator); D-031-2 **nearest-rank p95** (pure Python, no numpy) over period `clean_bps` + `bps_to_gbps` = bytes/sec **×8 / 1e9** (load-bearing bytes→**bits** fix — DP counter is bytes/sec, `committed_clean_gbps` is gigabits; grounded in ARL/fairness "bps map = bytes/sec", `FAIR_RATE_MAX 16e9 B/s = ~128 Gbit`); D-031-3 **one worker background asyncio lane** `BillingMeter.run_loop(stop)` (spawn/await/cancel like the telemetry/feed lanes) doing `sample_once`→`refresh_open_periods`→`finalize_due_periods`→`prune_samples` each 5-min tick, catch-log-continue, **not** a Redis `JobType`/`AgentJob`; D-031-4 `BillingUsage` `open` running estimate → `final` immutable at boundary, `UNIQUE(service_id, period_start)` (idempotent close, NULLs distinct in PG); D-031-5 deletion = FK `SET NULL` + `finalize_due_periods` also closes rows where `service_id IS NULL` → **no reach into M1 `delete_service`** (contrast: telemetry touches `create_service` for `dp_id`; chargeback only **reads** `dp_id`); D-031-6 UTC calendar-month arithmetic in pure `services/billing_period.py`, `worker_billing_period` fixed `"monthly"` fwd-compat, no proration; D-031-7 `BillingSample` FK **CASCADE** (transient p95 inputs) vs `BillingUsage` **SET NULL** + denormalized `tenant_id`(FK SET NULL)/`service_name` snapshot (durable chargeback history); D-031-8 new `/billing` router — tenant filtered by denormalized `tenant_id` (+`load_service_for_principal`→404 when `service_id` given), admin all, export admin-only CSV/JSON of finalized rows; reuses session/RBAC verbatim. **New models:** `BillingSample`(service_id CASCADE, sample_ts, clean_bps bytes/sec, window_seconds, is_reset; `UNIQUE(service_id,sample_ts)`) + `BillingUsage`(service_id SET NULL, tenant_id, service_name, period_start/end, billing_metric, committed/p95/billed/overage_gbps `Numeric(10,2)`, overage_policy, sample_count, status open|final, finalized_at) + `BillingStatus` enum + `billing_status_enum`; migration `..._0009_billing` (down_revision = telemetry models migration, pinned at Execute). **New settings** `worker_billing_{enabled=True,interval_seconds=300,sample_retention_days=400,period="monthly"}`; **reuses** telemetry reader knobs (`worker_telemetry_binary_path/ifindex/timeout_seconds`). **Reuse verified in-tree:** `Worker.run` lane lifecycle (`FeedCoordinator`), `session_scope` (`db/session.py:62`), `core/deps.py` (`load_service_for_principal`→404, `require_admin`, `Principal.tenant_id`), `TimestampMixin`/`SAEnum(native_enum=False)`/`Numeric(10,2)`/FK-`ondelete` idioms, `ServicePlan`(committed/billing_metric/overage_policy already on `models.py:443`, `OveragePolicy` enum), `main.py::create_app` router registration. **No Context7/web needed** (all grounded in repo + approved AD-030). **Execute-gated on telemetry executed** (byte source/reader/`dp_id`); CP models+`BillingMeter`(vs `FakeTelemetryReader`)+rollup+API build+test now; P2 SPA showback gated on the telemetry FE shell; M6 bypass exclusion is a documented fwd dep (A-CHG-8). **7 flags for Tasks** (reader sharing vs dedicated; nearest-rank vs interpolation p95; retention horizon 400 d vs keep-N-periods; `tenant_id` FK SET NULL vs plain UUID; single 5-min tick vs separate finalize timer; export CSV column contract; `period` param `YYYY-MM` vs ISO). All 33 reqs mapped in the coverage table; expected as a **single CP/worker track** (models → meter unit-`[P]` → rollup → API → export), zero DP work. Next: **approve design → Tasks**. — Original spec/context notes below.

**Prior planning (chargeback-metering, spec):** M5 → **Chargeback metering** — spec + context (2026-07-10): `.specs/features/chargeback-metering/` spec.md (CHG-01..33; P1=01..25 MVP sample-series→p95-rollup→API, P2=26..31 export+SPA showback, P3=32..33 history/trend) + context.md (D-CHG-1..4, A-CHG-1..8). Closes M5 by metering clean throughput into immutable per-period `BillingUsage` for internal chargeback. Authoritative shape fixed by TDD §4.8/§4.7: unit = clean Gbps (`IN→OUT`, drops excluded), `billing_metric` default `p95_clean_bps`, **`billed_gbps = max(committed_clean_gbps, p95_clean_gbps)`**, `overage_gbps = max(0, p95−committed)` per `overage_policy` (`billed`=charge overage / `capped`=enforcement bounds p95≤ceiling), written per `billing_period` (monthly), `GET /billing/usage`. **Reuses** `ServicePlan.committed_clean_gbps`/`billing_metric`/`overage_policy` + `OveragePolicy` enum (all already in `models.py:443`); **new** models `BillingSample`+`BillingUsage` + `/billing` router (BillingUsage does NOT exist yet). **4 gray areas resolved via AskUserQuestion:** **D-CHG-1** p95 source = dedicated per-service **`BillingSample` 5-min series** (worker records one exact clean-bps sample/service/300 s, retained full period; decoupled from telemetry's ≤2 s windows+short retention; ~8,640 rows/svc/month; industry-standard 95th-pct burstable billing) reading the **D-TEL-1 exact per-service `clean_bytes` counter** (no new DP counter); **D-CHG-2** materialize = worker sampler + scheduled in-worker **period-close rollup** → **immutable** `BillingUsage` (`status` open/final, unique `(service_id,period_start)`, idempotent) + running current-period estimate for showback — background asyncio lane like the telemetry aggregator, **not** a Redis `JobType`/`AgentJob`; API reads stored rows (never recomputes p95 on read); **D-CHG-3** period = **UTC calendar month, no proration** (mid-period create/delete → fewer samples, `sample_count` exposes confidence; `billing_period` knob forward-compat, fixed monthly v1); **D-CHG-4** granularity = **per-service** (1:1 `ServicePlan`), **VIP/whitelist clean included** (delivered bytes, counted at `redirect_out`), **bypass excluded** (M6 fwd dep, TDD §4.9); `BillingUsage.service_id` FK `SET NULL`+denormalized `tenant_id`/name snapshot survives service deletion. **8 assumptions (A-CHG-1..8):** reuses D-TEL-1 counter → **Execute-gated on telemetry-dashboards executed** (CP model/API/rollup buildable ahead vs a fake reader — plan-ahead posture like telemetry vs M4 #2); sampler+rollup = new in-worker background tasks; reader = telemetry's C-helper/`dpstat snapshot --json` (no new privileged surface); additive `BillingSample`+`BillingUsage` models+migration (no M1–M4 schema change), Gbps `Numeric(10,2)` matches `ServicePlan`; per-service counter unslotted (swap-safe), stale-`dp_id` reconcile like telemetry; `/billing/usage`+export+history reuse session/RBAC+`load_service_for_principal` verbatim (tenant own→404 cross-tenant, admin node-wide); v1 = `p95_clean_bps` only (`billing_metric` fwd-compat), `overage_policy` snapshotted per period; bypass "count separately" deferred M6. **Note:** M5 sits behind the M4 execution front (threat-feed-sync is the authoritative current work); plan-ahead artifact, not an execution-order change. Next: **approve spec → Design** (`BillingSample`/`BillingUsage` schema, period-boundary+close-job mechanics, running-estimate refresh, reader reuse, `/billing` router shape → AD-03x).

**Prior planning (bypass-maintenance):** M6 → **Bypass & maintenance mode** (first M6 feature) — **spec + context + design drafted (AD-032), awaiting design approval** (2026-07-13): `.specs/features/bypass-maintenance/` spec.md (BYP-01..33; P1=01..27 MVP, P2=28..30 banner, P3=31..33 runbook/history) + context.md (D-BYP-1..4, A-BYP-1..8) + design.md (**AD-032**) + 2 rendered diagrams (`diagrams/bypass-architecture.{mmd,svg}` component, `diagrams/bypass-sequence.{mmd,svg}` toggle-propagation + maintenance hold/drain, mmdc-rendered). **Design grounded against the live tree (svc_stat.h/telemetry_reader.py/worker.py/applier.py/loader.c/xdp_gateway.bpf.c all read); `PlaceholderApplier` still live → M4 #2 NOT executed; TelemetryReader.snapshot() already exposes active_slot/version/xdp_mode.** **Gating correction (the big design finding):** **D-032-1** stores the bypass indicator in a **dedicated `node_control` BPF `ARRAY[1]` map** (`{__u32 bypass}`) written by `dpstat`, **not** a field in `active_config` written by the M4 #2 applier — this **decouples the whole feature from double-buffer** (buildable on the executed M1+M2+M4 #1+in-progress M5 base) and **removes A-BYP-3's field-preserving contract** on the applier (two writers RMW'ing one struct value = lost-update race avoided). Decisions: **D-032-2** hot-path = post-parse `if (node_control_bypass()) return redirect_out_bypass(&meta);` before `service_lookup_redirect` (one-line seam; ARP unchanged; IPv6/malformed/fragment already dropped upstream) · **D-032-3** `bypass_counter` `PERCPU_ARRAY[1]` outside frozen `counter_map` (mirrors `bloom_stats`/`bloom_hit_lpm_miss`), `svc_stat` untouched (bypass runs `service_id==0` → `svc_stat_clean` no-ops anyway) · **D-032-4** maintenance = worker **apply-dispatch gate** (`_maintenance_active(db)` before `mark_applying` in `process_job`/`reconcile_once` → SERVICE_UPDATE jobs stay `queued`, M1 auto-enqueue still 202s, drain+flip on clear via reconciler kick; applier stays maintenance-agnostic; **works today with `PlaceholderApplier`**, the real slot-swap it defers arrives with M4 #2 with no new code — refines D-BYP-2's "build but hold flip" to the safest mechanism: full-node rebuild-per-apply makes pre-building moot, holding before dispatch avoids wrong `active_version`/illegal transitions, identical observable contract) · **D-032-5** propagation = new `NodeControlReconciler` background asyncio lane spawned in `Worker.run` (feed-lane pattern, **separate task** so a long `process_job` never delays it → jumps the backlog, D-BYP-3), 1 s tick, execs the DP writer on bypass drift + kicks a reconcile on maintenance-clear edge, re-asserts from the row on restart (BYP-05); **no `NODE_CONTROL` JobType** · **D-032-6** DP writer = extend **`dpstat`** with a privileged `set-bypass 0|1` subcommand (opens pinned `node_control`), reconciler execs it like `TelemetryReader` — decouples bypass from the M4 #2 `xdpgw-apply` binary; not-loaded → friendly error → effective=off · **D-032-7** `NodeControl` = **singleton** table (`SmallInteger PK CheckConstraint id=1`, get-or-create) desired-state {bypass_enabled, maintenance_enabled, bypass_reason, *_activated_at, *_actor_user_id}; migration `..._00NN_node_control` down_revision pinned live; forward-compat multi-node via later `node_id` · **D-032-8** `/node/health` reuses **`TelemetryReader.snapshot()`** extended with a bypass block (`dpstat snapshot --json` gains `node_control.bypass` + `bypass{pkts,bytes}`), handler merges desired (row) ⊕ effective (snapshot) + activated_at/active_seconds; **alerts = `AuditEvent`** (distinct `action` per toggle) + health state the M6 *Alerting* feature evaluates — **no dedicated alert table** here (A-BYP-2, feed-sync A-FEED-4 precedent). New files: DP `src/node_control.h` (+2 maps, loader pins/seed, dpstat set-bypass+snapshot); CP `NodeControl` model+migration, `services/node_control.py` (set_bypass/set_maintenance idempotent+audit, maintenance_active), `api/routers/node.py`+`schemas/node.py`, `worker/node_control_reconciler.py` (+`BypassWriter`/`DpstatBypassWriter`/`FakeBypassWriter`), processor gate, `worker.py` lane spawn, `config.py` `worker_node_control_{enabled,interval_seconds=1.0}` (reuse `worker_telemetry_binary_path/ifindex`); P2 banner on M5 SPA shell. **Soft coordination (NOT hard gates):** M4 #2 applier (the flip maintenance defers), M5 telemetry `/node/health`+`TelemetryReader` (extend not duplicate — flag #1), M5 chargeback (A-CHG-8 reads the counter), M5 SPA shell (banner). All 33 reqs mapped in the coverage table. **6 flags for Tasks:** node-health router ownership (extend telemetry vs new `routers/node.py`); dpstat set-bypass privilege envelope; singleton enforcement idiom; maintenance-gate FEED_SYNC swap scope (v1 SERVICE_UPDATE, feed-swap same gate = decide at Tasks); DP-unit bypass seam (harness writes real `node_control` map, no new PKT_TEST_HOOKS); migration down_revision pinned live (after telemetry 0008/billing 0009). No Context7/web needed (all mechanisms proven in-repo; BPF aligned-`__u32` no-torn-read grounded in AD-028 slot-flip atomicity). **Tasks drafted** (2026-07-13): tasks.md (T1–T11; all 33 reqs mapped; 3 toolchain tracks DP∥CP∥FE per TESTING.md). **DP** (`make test`): T1 `node_control.h` (2 maps+`node_control_bypass`/`bypass_count`/`redirect_out_bypass`)+post-parse hot-path branch+dp-unit (bypass on→would-be-`service_miss` redirects+`bypass_counter`↑+`svc_stat` untouched+IPv6/frag still drop; harness writes real `node_control` map, no new PKT_TEST_HOOKS) · T2 loader pins/seed+dpstat `set-bypass 0|1`/snapshot bypass block (build) · T3 live veth bypass smoke (dp-integration, privileged). **CP** (`pytest`): T4 `NodeControl` singleton model+migration (integration) · T5 `services/node_control.py` set_bypass/set_maintenance idempotent+audit+maintenance_active (integration) · T6 `TelemetryReader` bypass ext (**unit `[P]`**, parses AD-032 JSON contract) · T7 `api/routers/node.py`+schemas+main.py (`/node/bypass`|`/node/maintenance`|`/node/health` desired⊕effective, require_admin, integration) · T8 maintenance apply-dispatch gate in processor.py (`_maintenance_active` before `mark_applying`→jobs stay `queued`, integration) · T9 `NodeControlReconciler` lane+`DpstatBypassWriter`/`FakeBypassWriter`+`worker_node_control_*` settings+`Worker.run` spawn (integration). **FE** (`npm`): T10 SPA BYPASS/maintenance banner (P2, fe gate, **gated on M5 telemetry SPA shell**) `[P]`. **Docs:** T11 TESTING.md conventions+OLA runbook `[P]`. Serial within CP (shared `compose.test.yml`): T4→T5→T8→T7→T9; only **T6 (unit)/T10 (fe, separate toolchain)/T11 (docs)** `[P]`; DP T1→T2→T3 serial (shared bpf/loader/dpstat files+privileged smoke). **All 3 mandatory pre-approval checks pass:** granularity (T2 loader+dpstat, T7 3-endpoint `/node` resource, T9 lane+writer+settings+spawn flagged cohesive-not-split); diagram↔`Depends on` (11/11 match, no `[P]` inter-dep); test co-location (T2 the only non-obvious — loader/dpstat C tooling can't be unit-tested w/o privilege, build-gated + covered end-to-end by the privileged **T3 dp-integration smoke**, the DP-established "tooling→build, smoke→separate full task" pattern, not deferral). Tools: `coding-guidelines` all code, `docs-writer` T11, **no MCPs**. Baselines pinned live at Execute (B_dp≥91+telemetry svc_stat, B_cp≥262+feed+telemetry, B_fe=telemetry shell). Next: **approve tasks → Execute** (Phase 1: T1 DP lead + T4 CP lead + T6 `[P]`; the feature builds on the executed M1+M2+M4 #1 base — **not** hard-gated on M4 #2). — Original spec/context notes below.

**Prior planning (bypass-maintenance, spec):** M6 → **Bypass & maintenance mode** (first M6 feature) — **spec + context drafted** (2026-07-10): `.specs/features/bypass-maintenance/` spec.md (BYP-01..33; P1=01..27 MVP, P2=28..30 banner, P3=31..33 runbook/history) + context.md (D-BYP-1..4, A-BYP-1..8). Two node-global admin-only operator safety controls the Pilot OLA depends on: a **global soft-bypass** (emergency "stop dropping" — hot path pass-throughs clean traffic, skipping the verdict pipeline) and **per-node maintenance mode** (holds config swaps during a window); both audited + alert-worthy + banner-surfaced, bypass separately accounted. **4 gray areas resolved via AskUserQuestion:** **D-BYP-1** bypass scope = **parsed IPv4 + ARP only** (short-circuit right after parse → `redirect_out()` skipping service-lookup/cap/whitelist/blacklist/rules/fairness; IPv6/malformed/fragment still fail-fast drop — v1 IPv4-only forwarding commitment unchanged; distinct from device-level link bypass M7/OP-03, §11.5.1); **D-BYP-2** maintenance = **queue-and-apply-on-exit** (worker keeps building the inactive slot but **holds the `active_config` flip**; mutations still accepted/queued 202; clear → latest good config swaps in one flip; window survives worker restart); **D-BYP-3** toggle path = **immediate control channel** (toggle writes DB desired-state, audited, + worker **fast node-control reconcile lane** asserts into DP **ahead of the service-apply backlog** ~1 tick; persists across restart via re-assert; **no `NODE_CONTROL` JobType**); **D-BYP-4** accounting = **add node-global exact per-CPU bypass pkts/bytes counter now** (separate from telemetry `svc_stat` clean; outside frozen `counter_map` like `bloom_hit_lpm_miss`; `dpstat` + `/node/health`; chargeback exclusion = A-CHG-8/M5). **Owns:** new `NodeControl` desired-state model + `/node/bypass`|`/node/maintenance`|`/node/health` API; worker node-control reconcile lane (assert bypass flag + gate the M4 #2 `DoubleBufferApplier` flip for maintenance); hot-path bypass short-circuit branch before `service_lookup_redirect()`; node-global bypass counter; audit + critical alert-worthy event per toggle; OLA runbook. **8 assumptions (A-BYP-1..8):** node-global admin-only (`require_admin`); "alert"=audit+emitted event, delivery=sibling *Alerting*; **`active_config` gains a bypass indicator — storage (field-in-`active_config` RMW-safe vs dedicated `node_control` map) = Design call, but the M4 #2 flip MUST preserve it (shared contract, like telemetry `dp_id`)**; new additive `NodeControl` model+migration; worker gains a fast reconcile background lane (`worker_node_control_*` settings); bypass counter outside `counter_map`; bypass forward reuses `redirect_out()`/`tx_devmap` verbatim; bypass⊥maintenance independent, maintenance gates only the flip not the emergency bypass channel, `/node/health` reports desired-vs-effective for each. **6 open questions for Design** (bypass-indicator storage; maintenance swap-hold mechanism in the applier; node-control assert path/tick default; alert-event shape vs sibling Alerting; `NodeControl` shape singleton-vs-node-keyed; `/node/health` reader = reuse telemetry `TelemetryReader`/`dpstat snapshot --json`). **Execute-gated on M4 #2 executed** for real DP effects (hot-path pass-through, bypass counter, maintenance swap-hold); CP control surface + audit + state + reconcile-lane logic buildable ahead on the executed M4 #1 worker (feed-sync DP-gate precedent). Reuses M1 auth/`AuditEvent`/`record_event`; M2 `redirect_out`/`tx_devmap`/loader pins/`dpstat`; M4 #1 worker + background-lane pattern + `Applier` boundary; M4 #2 `active_config`/`xdpgw-apply`/pins; M5 `svc_stat` + SPA shell. **Note:** plan-ahead M6 artifact — current execution front is M4 (threat-feed-sync authoritative). Next: **approve spec → Design** (resolve the 6 open questions → AD-03x).

**Prior planning (telemetry-dashboards):** M5 → **Telemetry & dashboards** (first of two M5 features; sibling = *Chargeback metering*) — **spec + context drafted, awaiting approval** (2026-07-10): `.specs/features/telemetry-dashboards/` spec.md (TEL-01..40; P1=01..30 MVP vertical slice, P2=31..38, P3=39..40) + context.md (D-TEL-1..4, A-TEL-1..8). First M5 feature + **first frontend in the repo**. Makes the data-plane observable end-to-end: **new exact per-CPU per-service counters on the XDP hot path** (clean pkts/bytes + drop pkts/bytes + per-service per-§9.2-reason; node-global `counter_map` stays authoritative for pre-service/unmatched drops) → worker **`TELEMETRY_AGGREGATE`** (new `JobType`+handler on the executed AD-027 `HANDLERS` registry, in-worker ≤2 s scheduler à la feed-sync due-time, reads pinned maps via the established C-helper subprocess pattern, windowed-delta with reset detection) → windowed `TelemetryCounter(service_id|node, window_ts)` Postgres rows → telemetry/health REST API (`/services/{id}/telemetry` tenant-scoped + admin node/health; reuses session/RBAC + tenant-ownership guard verbatim) → **first React SPA** (shell: session login/role routing/layout + tenant per-service view + admin node/health view; **REST polling ≤2 s**). **4 gray areas resolved via AskUserQuestion discuss:** **D-TEL-1** telemetry **introduces the per-service hot-path counters** here (not chargeback) — the exact `clean_bytes` counter becomes the billing source of truth chargeback reuses, satisfying the PROJECT exact-per-CPU-billing constraint; **D-TEL-2** frontend scope = **SPA shell + telemetry/health views only** (config CRUD screens = separate later effort); **D-TEL-3** realtime = **REST polling** (worker windowed rows + SPA ≤2 s poll; SSE/WebSocket rejected as unjustified fan-out complexity); **D-TEL-4** **include sampled top dst-port + top src IP** from `drop_ringbuf` (approximate; `top_src` PII pilot-accepted CM-08, GA adds retention). **8 assumptions (A-TEL-1..8):** worker colocated on gateway node reads `/sys/fs/bpf/xdp_gateway/` via C helper (dpstat/`xdpgw-apply` precedent, no new privileged surface); `TELEMETRY_AGGREGATE` is scheduler-driven not mutation-enqueued; new additive `TelemetryCounter` model + retention prune (no M1–M4 schema change); node-health fields sourced XDP-mode/map-version (loader/`active_config` via C reader), apply-status/backlog (CP `AgentJob`/`apply_status`), feed status (`FeedSyncRun` M4#3), `map_error` (counter_map idx 15), throughput-vs-capacity (clean-bps vs `node_clean_capacity` fairness seed); per-service counters **unslotted** (untouched by double-buffer swap, aggregator reconciles stale keys); P2 feed/fairness panels depend on those features executed but P1 does not; **added-latency p99 excluded** (no in-band measurement path in v1). **Out of scope:** p95/`BillingUsage` (sibling chargeback), alert firing (M6), bypass/maintenance banner (M6), config CRUD UI, SSE/WebSocket, structured-log pipeline (M6). Reuses executed DP counter/pin patterns (AD-017) + worker `HANDLERS`/`Applier` boundary (AD-027). **Note:** M5 sits after the M4 execution front (threat-feed-sync is the authoritative current work); this spec is a plan-ahead artifact, not a change to the execution order. **Design drafted (AD-030, 2026-07-10):** design.md + 2 rendered diagrams (`diagrams/telemetry-architecture.{mmd,svg}`, `diagrams/aggregation-tick.{mmd,svg}`); grounded by reading the actual hot path, worker, deps, models. **Central finding + fix:** `service_val.service_id` is `__u32` but `ProtectedService.id` is UUID with no numeric surrogate → **D-030-4 introduces `ProtectedService.dp_id`** (monotonic `service_dp_id_seq`, ≥1, `0`=sentinel) written into `service_val.service_id` by the M4 build path — **a shared contract the M4 #2 double-buffer applier must adopt** (loader env-seed adopts it immediately). **Decisions (AD-030):** D-030-1 `svc_stat_map`=prealloc `PERCPU_HASH[dp_id]` value `{clean_pkts,clean_bytes,drop_pkts,drop_bytes,drop_by_reason[DROP_REASON_CAP=32]}` (~19 MB@64CPU, mirrors `rate_limit_state`), incremented via `svc_stat_clean`/`svc_stat_drop` at the **single choke points** `redirect_out()`/`record_drop()` gated on `service_id!=0` (new header `src/svc_stat.h`); D-030-2 `pkt_meta.frame_len` (repurpose 2 pad bytes, size stays 40 B, set at ingress) for exact bytes; D-030-3 `drop_by_reason[CAP=32]` for ABI-stable value width; D-030-5 reader = **extend `dpstat`** with `snapshot --json` (reuses `open_pinned_map`/`read_percpu_u64`/`libbpf_num_possible_cpus`; iterate hash via `bpf_map_get_next_key`; XDP mode via `bpf_xdp_query`), P2 top-talkers reuse `dpstat tail --json` streaming; D-030-6 aggregation = **worker background asyncio task** (`TelemetryAggregator.run_loop`, spawned/cancelled like the feed lane) computing windowed deltas + reset detection + `dp_id→UUID` map — **NOT a Redis `JobType`/AgentJob** (supersedes A-TEL-2; periodic maintenance like `reconcile_once`, no version/idempotency/durability, avoids ledger writes at 2 s); D-030-7 node health = DP-derived persisted (`NodeHealthSnapshot`) + **live** `AgentJob` backlog/`FeedSyncRun` status read by API; D-030-8 REST polling; D-030-9 FE = Vite+React+TS+React Router+TanStack Query (`refetchInterval:2000`)+Recharts, httpOnly-cookie auth, `/auth/me` for role routing; D-030-10 P2 ringbuf isolated to streaming lane. **New models:** `TelemetryCounter` (scope service|node, `service_id` FK SET NULL, `window_start`/`window_seconds`, clean/drop pkts+bytes deltas, `drop_by_reason` JSONB, pps/bps, `top_*` JSONB P2, `is_baseline`) + `NodeHealthSnapshot` (xdp_mode/slot/map_version/map_error/clean_bps/capacity), migration `20260710_0007`. **API** (new `routers/telemetry.py`): `GET /services/{id}/telemetry` (`load_service_for_principal`→**404 cross-tenant**), `GET /node/telemetry` + `/node/health` (`require_admin`); reuses session/RBAC verbatim. **New settings** `worker_telemetry_*` (enabled/interval 2 s/retention/binary_path/ifindex/timeout); capacity from existing `node_clean_capacity_gbps`. **Reuse verified:** deps `load_service_for_principal`/`require_admin`/`get_current_user`, `session_scope`, `/auth/me`, feed background-lane + `next_reconcile` timer, dpstat readers, loader observability pin group, alembic head `20260710_0006`. **2 facts flagged web-verify@Execute** (`bpf_xdp_query` mode; ringbuf consumer-pos across processes — P2 only, streaming fallback). **P1 buildable now** vs loader-seed/`FakeTelemetryReader`; true multi-service end-to-end **gated on M4 #2 executed** (dp_id contract) — mirrors feed-sync's DP gate. **8 flags for Tasks** (dp_id seq vs identity; create_service reach into M1; reader home; xdp-mode source/ifindex setting; FE build/serve+chart lib; svc_stat value width 32 vs 16; confirm drop TELEMETRY_AGGREGATE JobType; P2 ringbuf lane). **Tasks drafted (2026-07-10):** tasks.md (T1–T17; all 40 reqs mapped). **3 consequential Design flags confirmed as recommended:** no `TELEMETRY_AGGREGATE` JobType (background task), `dp_id` assigned in `create_service`, `drop_by_reason[DROP_REASON_CAP=32]`. **P1** = T1 DP core (svc_stat.h+frame_len+choke-point wiring, dp-unit) → T2 loader pin+env-seed dp_id (dp-integration) → T3 `dpstat snapshot --json`+golden fixture (build+manual) · T4 `dp_id` col+`service_dp_id_seq`+create_service+backfill migration `20260710_0007` → T5 `TelemetryCounter`+`NodeHealthSnapshot`+migration `20260710_0008` · T6 `TelemetryReader`+`FakeTelemetryReader` (**unit `[P]`**) · T7 `TelemetryAggregator` (delta/reset/dp_id-map/persist/prune) · T8 worker lane spawn+`worker_telemetry_*` settings · T9 telemetry/health API router · T10 SPA scaffold+auth shell (**FE `[P]`**) · T11 tenant dashboard · T12 admin dashboard. **P2** T13 top-talkers (dpstat tail --json + rolling top-N), T14 richer health (bloom FP/committed-honored/backlog/feed), T15 P2 FE panels+threshold coloring. **P3** T16 trend+export. **Docs** T17 `[P]` (TESTING.md dp `svc_stat`/`snapshot` section + new **FE `fe` gate** convention). **Parallelism:** DP ∥ CP ∥ FE (separate dirs/gates); within CP only unit-typed `[P]` (integration shares `compose.test.yml`); FE = separate Vitest toolchain so scaffold `[P]` but FE view tasks serialize on the shared project tree. **Gates:** DP build=`make bpf skel loader dpstat`/quick=`make test`/full=`+sudo make smoke`; CP quick/full/build per TESTING.md; **new FE `fe`**=`npm run lint && typecheck && test --run && build`. Baselines pinned live at Execute (`B_dp`≥91, `B_cp`≥262+feed, `B_fe`=0). All 3 mandatory pre-approval checks pass (granularity; diagram↔`Depends on`; test co-location — FE flagged as new layer T17 documents, not a deferral). **5 Execute flags** (dp_id seq; ifindex setting vs pinned mode byte; FE serve StaticFiles vs static host; web-verify `bpf_xdp_query`@T3 + ringbuf-consumer-pos@T13 w/ streaming fallback; confirm executing ahead of M4 #2). Tools: `coding-guidelines` all code, `docs-writer` T17, `context7`/web T3/T10/T13. Next: **Execute** (Phase 1: T1+T4 track leads, T6+T10 `[P]`) — still a plan-ahead artifact behind the M4 execution front.

**Prior planning (agent-worker):** M4 → **Agent worker & job pipeline** — **spec APPROVED + context complete** (2026-07-10): `.specs/features/agent-worker/` spec.md (AGW-01..30) + context.md (**D-AGW-1** v1 applier = succeeding placeholder — full `queued→applying→active` lifecycle runs end-to-end now, `active` = "acknowledged by worker" until M4#2 fills the applier boundary with the real double-buffer build/swap, D-SLRD-1 interim-writer precedent; **D-AGW-2** orphaned-`applying` jobs auto-recover in the startup sweep via `mark_failed("worker restarted mid-apply")` + the existing retry path (`failed→queued`, re-dispatch) — zero new state-machine edges, bounded by `attempts`; A-AGW-1..8 — AD-026). First M4 feature = the **consumer half** of M1 apply-status (executed `a4b1ffd..de47b5f`, gate satisfied): long-running worker process (`python -m app.worker`, control-plane package, colocated on gateway node A-AGW-1), `BRPOP apply:jobs` + startup/periodic **DB-ledger reconcile** (fulfils outbox promise A-APLY-1/APLY-27/36; Redis down → DB-poll degrade ≤60 s bound), handler registry keyed by `JobType` (`SERVICE_UPDATE`-only v1, A-AGW-4; no-handler → `mark_failed`), identity-only jobs (config read from PG at apply time, A-AGW-5), all transitions via executed `mark_*` (APLY-03; first concurrent exerciser of the no-stale-over-new guards), ≤5 s nominal propagation asserted with v1 applier (A-AGW-7), SIGTERM grace + kill-9 crash-consistency, structured per-job logs + existing `GET /jobs` (no new endpoints). Pure control-plane — executable independently of M3 fairness Execute. **Design drafted** (2026-07-10): design.md (**AD-027**: new `app/worker/` package — `__main__.py` (`python -m app.worker`, cli.py precedent) → `Worker` runtime (BRPOP loop + startup/periodic reconcile + SIGTERM grace + Redis/DB bounded-backoff degrade) → loop-free testable core `process_job`/`reconcile_once`/`recover_orphan` → `HANDLERS` registry → `handle_service_update` → injected `Applier` boundary (v1 `PlaceholderApplier` succeeds, M4#2 swaps the impl not the boundary); **crux = two transactions per job** — `mark_applying` commits & releases the `ProtectedService` FOR UPDATE lock *before* the applier runs, terminal `mark_active/failed` re-takes it, so a mid-apply `bump_version→N+1` is observable and caught by the executed `_superseded` guard (first concurrent exerciser of no-stale-over-new); worker re-reads `job.status` after `mark_applying` to branch (executed `apply.py` byte-untouched); orphan recovery = one txn `mark_failed`+`retry(actor=None)` (system-actor audit already supported → D-AGW-2 attribution flag resolved, zero new `LEGAL_APPLY` edges); new shared `session_scope` UoW in `db/session.py` mirrors `get_db`'s commit+`run_post_commit_callbacks` so `retry`'s post-commit re-dispatch fires; `Settings` extended with `WORKER_*` knobs (poll 2 s / reconcile 15 s / backoff 0.5→30 s / grace 10 s); **no new models, no migration, no endpoints**) + 2 rendered diagrams (`diagrams/worker-architecture.{mmd,svg}`, `diagrams/process-job-sequence.{mmd,svg}`). 1 load-bearing external API web-verified (redis-py async `brpop([key],timeout)` → `(key,value)` str under `decode_responses=True` else `None`). 5 open flags for Tasks (no-handler test mechanism, config-snapshot depth, `session_scope` placement, degraded-Redis test scope, stdlib structured-log format). **Tasks APPROVED** (2026-07-10): tasks.md (T1–T6, all 30 reqs mapped; baseline **B=209** static — 29 unit + 180 integration, pin exact `pytest -q` total live at Execute; control-plane gates quick=`ruff+mypy+pytest -m unit`, full=`ruff+mypy+pytest` on `compose.test.yml`): **T1** `session_scope` UoW in `db/session.py` (integration) · **T2** applier boundary+`ServiceConfig`+`PlaceholderApplier`+`load_service_config` in `app/worker/applier.py` (integration) · **T3** `HANDLERS` registry+`handle_service_update`+`NoHandlerError` (integration) · **T4** processor `process_job`/`reconcile_once`/`recover_orphan` + truncation-isolation fixture — the crux (two-txn guard, supersede-under-churn `BarrierApplier`, reconcile, orphan recovery) (integration) · **T5** `Worker` runtime+`WORKER_*` settings+`__main__` entrypoint (integration + unit backoff) · **T6** docs `[P]` (`docs-writer`). Only T6 `[P]` — all integration tasks serial (shared `compose.test.yml` infra, TESTING.md). 5 design flags resolved: monkeypatch `HANDLERS` for no-handler test; full `ServiceConfig` snapshot now; `session_scope` in `db/session.py` (no `get_db` refactor); gated `WORKER_REDIS_DOWN_TEST` degrade case; stdlib `logging` `extra={}`. **`apply.py` stays byte-for-byte unmodified** (AGW-04); no new models/migration/endpoints. Tools: `coding-guidelines` T1–T5, `docs-writer` T6. Next session: **Execute** (T1 first; T4 supersede test = the fail-fast proof the lock-release enables supersession).
**Prior planning (fairness-bandwidth):** M3 → **Fairness & bandwidth reservation (§8.4)** — **spec drafted** (2026-07-10): `.specs/features/fairness-bandwidth/` spec.md (FAIR-01..27; A-FAIR-1..8; 2 gray areas open GA-1/GA-2) — the final M3 feature, fills both remaining seams: the admit ladder at **ARL-24** (`admit_clean()`: committed exact global+`bpf_spin_lock` → burst per-CPU `ceiling−committed` dual-drawing node headroom `capacity−Σcommitted` → drops `service_ceiling_drop`/`congestion_drop`) and the ingress-cost cap at **WLV-24 seam A** (`k×ceiling` raw pps/bps pre-whitelist, destination-keyed spoof-immune, `ingress_cap_drop`); wires the last 3 frozen ABI indices **11/12/13** (all 16 §9.2 reasons live after this); per-service rates via a new slotted config map from `ServicePlan` (M4 build contract; `service_val` pad bytes exhausted → new map, A-FAIR-2); 3 runtime maps unslotted; VIP never enters the ladder (§8.4.6 structural) but **is** subject to the upstream cap (FAIR-18 documented precedence); deterministic fairness scenario = the M3 milestone gate (FAIR-24); spin-lock-in-XDP = the one novel kernel claim, de-risked fail-fast with fallback (FAIR-22/A-FAIR-8); default seed keeps post-BLK baseline verdict-identical (FAIR-23). **Spec APPROVED + context complete** (2026-07-10): context.md (**D-FAIR-1** dual-dimension ingress cap — bps budget `k×ceiling` + derived pps budget `k×ceiling÷ref_pkt_size` (~512 B, node-tunable), **k=3** default, AD-019 rodata posture; **D-FAIR-2** `node_clean_capacity` = env-driven seed value with documented **40 Gbps §15 default** when unset, loader never fails on it, headroom = `capacity−Σcommitted` floor 0 recomputed by seed/M4 worker — AD-024; A-FAIR-1..8). Requires blacklist-filters executed first (A-FAIR-1). **Design drafted** (2026-07-10): design.md (**AD-025**: new `src/fairness.h` with both stages; committed = unslotted top-level `HASH` + BTF `bpf_spin_lock` value (`now` captured **before** lock, pure-ALU critical section — kernel forbids helper calls in CS; verified vs introduction patch: XDP allowed, HASH/ARRAY only, **never map-in-map inner**, one lock/value); fallback ladder spin_lock → `__sync` atomics → per-CPU split; burst/node/cap reuse `rl_bucket`+helpers verbatim; `fair_config_map` ARRAY_OF_MAPS[2]/HASH 40 B row with **all budgets precomputed userspace-side** (k/ref-size/capacity = env, kernel does zero derivation); `fair_node_config` ARRAY[2] slot-keyed (gbl_meta precedent); burst dual-draw service-then-node, no refund (A-FAIR-6); **pkt_meta first deliberate growth 32→40 B** (`fair_state` + 7 spare — zero pad left, A-FAIR-7 void); **`FAIR_RATE_MAX` 16e9 B/s builder clamp** from the `elapsed×rate` u64 overflow bound (~147 Gbps), generous default 100 Gbps inside it; `admit_clean(ctx,meta,slot)` signature change internal) + 2 rendered diagrams (`diagrams/fairness-{ladder-flow,map-layout}.{mmd,svg}`). 3 kernel semantics web-verified (spin_lock program types/map homes/CS rules). Design **APPROVED** (2026-07-10) + **tasks.md drafted** (T1–T6, all 27 reqs mapped; baseline **B=91** pinned live — blacklist-filters executed, A-FAIR-1 gate satisfied): **T1** contracts+6 maps+pkt_meta 32→40+spin-lock de-risk rung selection (build+quick, 92) · **T2** ingress-cap stage+seam A wire+harness generous fair seed (dp-unit, ≥99) · **T3** committed/burst/node ladder at `admit_clean` (dp-unit, ≥107) · **T4** deterministic fairness scenario = M3 milestone gate (dp-unit, ≥110) · **T5** loader env seed+fairness smoke variant (full gate, privileged) · **T6** docs `[P]`. Only T6 parallel (T1–T4 share `fairness.h`/`test_parse.c`; T5 smoke not parallel-safe). Tasks **APPROVED** (2026-07-10) → next session: **Execute** (T1 first — the spin-lock rung selection is the fail-fast moment).
**Prior planning (blacklist-filters):** M3 → **Blacklist (bloom + LPM)** — **spec drafted** (2026-07-09): `.specs/features/blacklist-filters/` spec.md (BLK-01..26; A-BLK-1..8) — deny-filter stage at WLV-24 **seam B** (whitelist-miss path) in §8.2 order: hardcoded UDP amp ports → bogon → dynamic blocked-port bitmap → global blacklist (bloom→LPM) → service blacklist (bloom→LPM, key incl. `service_id`, BL-02 posture) → rule stage; whitelist hit bypasses the whole stage (§6.5). Wires frozen ABI **4/7/8** (`bogon_drop`/`udp_amplification_drop`/`blacklist_drop`, dpstat drop rows zero-change); global maps sized to the **1M-entry envelope** (gated bulk-load check, A-BLK-6); `bloom_hit_lpm_miss` = exact per-CPU counter **outside** `counter_map` (pure-ABI per AD-017) covering whitelist + both blacklist blooms (WLV deferred FP counting here; dpstat gains a new surface); 5 slotted config maps = M4 build contract (service pair reuses AD-021 patterns, A-BLK-2); default seed leaves deny maps empty (baseline/smoke unchanged); **A-BLK-1 bogon set (IANA incl. RFC 5737) forces a deliberate documented dp-unit source migration** (BLK-24). **Spec APPROVED + context complete** (2026-07-09): context.md (**D-BLK-1** full hardcoded amp-port set incl. 53/123 — 17/19/53/111/123/137/161/389/520/1900/5353/11211, resolver/NTP tenants whitelist upstreams; **D-BLK-2** bitmap seed-only writer in v1, D-SLRD-1 posture, control-plane writer deferred — AD-022; A-BLK-1..8) + **design drafted** (2026-07-09): design.md (**AD-023**: /24 global bloom buckets + 16..23 expansion band + slot-level `GBL_F_HAS_BROAD` escape + builder fill invariant; 1M LPM footprint measured at new gated `make blbulk`; `service_val.bl_flags` + `pkt_meta.bl_state` pad-byte gates, sizes frozen; per-stage `bloom_stats` PERCPU_ARRAY[3] + dpstat counters section, bump only when bloom consulted; pure-code amp/bogon checks; seam-B rewire with `whitelist_stage` taking `const struct service_val *` + its deferred FP bump; loader env seeds `XDPGW_SEED_{GBL_CIDR,SBL_CIDR,BLOCKED_PORT}` default-empty) + 2 rendered diagrams (`diagrams/deny-{stage-flow,map-layout}.{mmd,svg}`). 3 kernel semantics web-verified (bloom 7/5 bitset sizing → 2 MiB/slot @2M×5; LPM NO_PREALLOC kmalloc-per-node, ~670 ns/lookup @1M per Cloudflare → bloom guard load-bearing; ARRAY inner standard). Design **APPROVED** (2026-07-09) + **tasks.md drafted** (T1–T8, all 26 reqs mapped; baseline **B=68** re-verified live — WLV executed, A-BLK-5 gate satisfied): T1 contracts+maps+1M/ARRAY-inner load de-risk (build+quick, 68 unchanged) · T2 `[P]` bogon-space suite migration, verdict-neutral pre-wire (quick, 68 unchanged) · T3 amp/bogon/bitmap filters + seam-B wire (dp-unit, ≥78) · T4 global+service bloom→LPM bands + exact per-stage `bloom_stats` incl. deferred whitelist bump (dp-unit, ≥88) · T5 loader pins+env deny seed+smoke incl. inverse blacklisted-source smoke (full gate) · T6 dpstat bloom-FP section (build+manual) · T7 gated `make blbulk` 1M bulk-load + measured footprint (privileged) · T8 docs (needs T7's number). Only T2 `[P]` (disjoint files from T1); T5/T7 privileged not parallel-safe. Tasks **APPROVED** (2026-07-09) → **Executed / VERIFIED** (commits `692ebf5..1421daa`, confirmed 2026-07-10 during fairness Tasks session): T1–T8 landed (contracts+1M de-risk, bogon-space suite migration, seam-B filters, bloom→LPM bands+`bloom_stats`, env deny seed, dpstat FP section, gated `blbulk`, docs); `make test` → **91 passed** re-verified live 2026-07-10.
**Prior planning (whitelist-vip):** M3 → **Whitelist/VIP (scoped) & VIP ceiling** — **spec APPROVED + context + design drafted** (2026-07-09): `.specs/features/whitelist-vip/` spec.md (WLV-01..25) + context.md (**D-WLV-1**: NULL `vip_pps`+`vip_bps` = whitelist **inactive** — AD-020; A-WLV-1..8) + design.md (**AD-021**: composite scoped LPM key prefixlen≥32, /24 bloom buckets + `WL_F_HAS_BROAD` escape, `service_val.wl_flags` zero-cost D-WLV-1 gate, slotted `vip_config_map` + `rl_bucket` reuse with `rules.h` untouched, VIP admit → `redirect_out()` not `admit_clean()`, bloom replace-only M4 contract, de-risk ladder BTF-static→loader-created→LPM-only) + 2 rendered diagrams (`diagrams/whitelist-{stage-flow,map-layout}.{mmd,svg}`). Scoped bloom→LPM bypass (key incl. `service_id`, BL-01/02) inserted between service-enabled hit and ARL's rule stage; hit → VIP ceiling (aggregate per-service, per-CPU, unslotted `vip_ceiling_state`, D-ARL-2 lazy reset + AD-019 rate÷nCPU precedent) → redirect (skips 8.4 admit ladder); over-ceiling = terminal `vip_ceiling_drop` (frozen ABI **index 14**, dpstat zero-change); miss → rule stage unchanged (post-ARL baseline passes as-is). Slotted whitelist maps = M4 contract; seed-helper interim writer; marked seams for M3#3 (miss path) & M3#4 (ingress cap). Design **APPROVED** (2026-07-09) + **tasks.md drafted** (T1–T5, all 25 reqs mapped; baseline **B=50** — ARL executed, A-WLV-8 gate satisfied): T1 contracts+maps+bloom de-risk ladder (build+quick, 51) · T2 scoped match stage+wire+seams (dp-unit, ≥60) · T3 VIP ceiling bucket idx 14 (dp-unit, ≥66) · T4 loader env seed+smoke (full gate) · T5 docs `[P]`. Only T5 parallel. Tasks **APPROVED** (2026-07-09) → next session: **Execute** (T1 first). Requires ARL executed first (A-WLV-8; shared hot-path files).
**Whitelist/VIP Execute Progress:** T1 complete/verified (2026-07-09): primary static `BLOOM_FILTER` inner-in-`ARRAY_OF_MAPS` rung loaded successfully; de-risk dp-unit push/lookup/peek case passed; gate `cd data-plane && make bpf skel loader dpstat && make test` → **51 passed**.
**Execute track:** M3 → **Allow-rule matching & rate-limit** — spec approved + context + **design drafted** (2026-07-09): `.specs/features/allow-rule-ratelimit/` spec.md (ARL-01..25) + context.md (D-ARL-1 strict `any`; D-ARL-2 reset-on-swap — AD-018) + design.md (AD-019: pre-sorted `rule_block_map` ARRAY_OF_MAPS[2]/HASH, lazy version-reset `PERCPU_HASH` buckets, rate÷nCPU split, `rules.h` = M4 contract) + 2 rendered diagrams (design APPROVED) + **tasks.md drafted** (T1–T5, all 25 reqs mapped): T1 contracts+maps+verifier de-risk (build+quick, 34 unchanged) · T2 match engine+wire+suite migration (dp-unit, ≥42) · T3 buckets+deterministic mode (dp-unit, ≥49) · T4 loader seed+`rl_ncpus`+live smoke (full gate) · T5 docs `[P]`. Only T5 parallel (T1–T3 share files; T4 smoke not parallel-safe). Tasks APPROVED → **Executed / VERIFIED** (2026-07-09): T1–T5 committed (`d220628`..`4d87f4b`); final gates passed — `make test` → **50 passed** (re-verified during whitelist-vip Tasks session), rule-stage conventions + tunnel-traffic note documented in TESTING.md/README. A-WLV-8's execute gate is satisfied.
**Packet Parse Execute Status:** Complete and verified (2026-07-08). T1-T8 are committed; final gates passed: `cd data-plane && make bpf skel loader` and `cd data-plane && make test` -> 21 passed. B-002 remains resolved in this environment.
**Prior M2 work (drop-reason counters):** **VERIFIED / executed** (2026-07-08). T1-T6 are committed; final gates passed: `cd data-plane && make test` -> 34 passed and `cd data-plane && make bpf skel loader dpstat` -> passed. `./build/dpstat counters` without pinned maps returns the expected gateway-not-loaded error. `BPF_PROG_TEST_RUN` -> ringbuf delivery succeeded in the de-risk case, so no stats-only fallback was needed.
**M2 Execute Status:** Packet parse, service lookup & transparent redirect, and drop-reason counters are verified. Resume = M3 policy enforcement feature planning/execution.
**Prior M2 work (service lookup):** **Service lookup & transparent redirect** — **VERIFIED / executed** (`.specs/features/service-lookup-redirect/`, SLRD-01..26 -> T1-T7). Final gates: `make test && sudo make smoke` -> 29 dp-unit tests + live-veth TTL/checksum unchanged.
**Prior M2 work (packet parse):** **Packet parse & fail-fast** — **VERIFIED / executed** (`.specs/features/packet-parse/`, PKT-01..24 -> T1-T8). Final gates: build=`make bpf skel loader`; quick=`make test` -> 21 passed.
**Prior M1 work — Executed / landed** (verified via git 2026-07-10: full M1 chain committed through `de47b5f` "wire service mutations to queued jobs"; `app/core/applystate.py`, `app/services/apply.py` (`APPLY_QUEUE_KEY="apply:jobs"`, `mark_applying/active/failed`, `retry`, `ApplyDispatcher`), `agent_job` model + integration tests all present — the M4 agent-worker consumes these as executed code): **Apply-status state machine** — spec + context + design + **tasks** complete (`.specs/features/apply-status/`, APLY-01..40 → T1–T7; D-APLY-1..3; A-APLY-1..6): `pending→queued→applying→active|failed` behind one guard, API auto-enqueue (real Redis + version-idempotent `AgentJob` ledger via transactional outbox), version-guarded worker-facing `mark_*`, per-service apply-status read API + admin job-list; `agent_job` table + `core/applystate.py` guard + `services/apply.py`; modifies service-rule-list services + `bump_version` (worker loop = M4). **Service, rule & list management (API)** — spec + context + design complete (`.specs/features/service-rule-list/`, SRL-01..44; D-SRL-1..4) → Tasks. **Tenant & CIDR allocation** — spec + design + tasks complete (`.specs/features/tenant-cidr/`, TCA-01..32 → T1–T7) → Execute. **Auth & RBAC** complete (T1–T12, AUTH-01..39) → Execute.

---

## Recent Decisions (Last 60 days)

### AD-040: B2 design — the service blacklist was never enforced; `scope` kept, `bl_flags` → `reserved0`, wire v4 (2026-07-23)

**Finding (design.md §0) — reframes the whole feature:** `bl_flags` is **hardcoded to `0`** at [applier.py:279](../../control-plane/app/worker/applier.py#L279), the sole writer of that wire field, since `6c6a532` ("feat(worker): DoubleBufferApplier build/swap via xdpgw-apply"). `deny_filter_stage`'s service branch is gated on `bl_flags & BL_F_ACTIVE` ([blacklist.h:457](../../data-plane/src/blacklist.h#L457)) ⇒ **it has never executed in production.** Non-zero `bl_flags` exists only in dp-unit (`set_service_bl_flags`, direct map write) and the loader's test-only `XDPGW_SEED_SBL_CIDR`; the only blacklist bench case (`bench_dp.c` S2) seeds the **global** map. Meanwhile rows *do* traverse the full pipeline — serialized (`applier.py:315`), parsed, and written into the sbl bloom+LPM on every apply ([xdpgw-apply.c:760-785](../../data-plane/tools/xdpgw-apply.c#L760)) — then never read.
**Consequences:** (a) **B2's per-packet saving is zero**; the bench will show noise, and §8.6's "~15–25% (A4+B1/B2)" is carried by A4 and B1 alone. (b) The real defect is **correctness, not perf**: a tenant can POST a blacklist entry that is validated, stored, audited, mapped into the kernel, and shown in the UI as active policy — and never enforced. Removal converts a silent no-op into an explicit 404. (c) **D2's risk evaporates** — deleting service-scoped rows is verdict-neutral by construction (the spec's "sources become unblocked" edge case was wrong and is amended). (d) Genuine savings are apply-time: two fresh 65536-entry inners created + populated per service on every config swap, under attack. Spec amended in five places (Goals, SBR-25/P3, edge case, success criteria, D2 row) with an amendment banner at the top.
**Design decisions:** **D-SBR-1 (GA-3 closed) = keep `scope`, narrow to `global`** — Python enum loses `service`, PG enum keeps both labels (no `DROP VALUE`; recreating the type is disproportionate), plus a new `ck_blacklist_scope_global_only` CHECK. Rejected dropping the column: `feed_reconcile.py` filters `scope = 'global'` in 8 raw-SQL sites and `uq_blacklist_global_source_cidr` is a **partial** index on it — pure risk on the one pipeline that must not move; logged as deferred cleanup. **D-SBR-2 (GA-4 closed) = keep the byte as `reserved0`, readers reject non-zero** — `service_val` is 8 B with a size assert and `bl_flags` sits beside `_pad`, so removal would only grow padding; keeping it holds A4's flag slot, and `APPLY_SNAPSHOT_SERVICE_FIXED_SIZE` stays **67**. Fail-closed on non-zero so a future writer can't arm an unimplemented semantic (A4 bumps to v5 when it defines meaning). **D-SBR-3** = schema v3→4, delta is only the dropped trailing `sbl_count`/`sbl[]`; the existing header guard at `xdpgw-apply.c:745` already rejects wholesale before `apply_read_active`. **D-SBR-4** = either half of a partial rollout fails closed (apply job → `failed`, retried, previous snapshot keeps serving) — never a half-written map. **D-SBR-5** = `enum apply_service_outer` 8→6 is a process-local index, safe to renumber. **D-SBR-6** = `BLOOM_STAT_MAX` 3→2 with indices 0/1 unmoved ⇒ no dpstat remap, no telemetry migration. **D-SBR-7** = **keep** `service_blacklist` in the UI's `BLOOM_LABELS` as a legacy label; `BloomFpPanel` already iterates `Object.entries` with a `?? name` fallback (so SBR-14 is satisfied by existing code), and deleting the label would only degrade historical rows to a raw key.
**Trade-off:** one one-valued column and one reserved byte survive as deliberate debt; a `404` stays indistinguishable from an unknown `service_id` (D4); downgrade restores schema but not rows. Risk flagged: someone later "fixing" tenant blocking by re-adding the scope — §0 and this entry record that the correct project would be to **build** it (set `bl_flags` from the blacklist, mirroring `wl_flags`), and that call is the user's.
**Impact:** ~30 files across 5 languages; DP core + wire + CP + FE are **not independently shippable** (the version bump couples them) and must land in one change set. Component-level edit map with line anchors in design.md §4; 2 rendered diagrams (`deny-stage-b2`, `removal-surface`). Verification plan: DP unit ~137→~136 (3 cases deleted, 1 rewritten as global-attribution, 2 added for v3-rejection and `reserved0`≠0), bench ±1%, CP suite green minus the 6 pre-existing reds, `alembic upgrade head`→`downgrade -1` on a both-scopes DB, FE gate, plus a residue grep. **Tasks `tasks.md` drafted (T1–T12; all 25 reqs mapped, 0 unmapped; baseline `B` = measured dp-unit count, docs disagree 130 vs 137 and T11 reconciles them):** phase 1 DP **T1** delete branch (−3 dp-unit) · **T2** remove writers (+`applybulk`) · **T3** delete map defs · **T4** `bl_flags`→`reserved0`; phase 2 **T5** ⚠️ wire v4 — **atomic by necessity**, `apply_snapshot_golden.bin` binds the C parser (`make apply`) *and* the Python serializer (`test_snapshot_serialize.py`), so version+layout+fixture+both sides move in one commit (+2 new cases: v3 rejected, `reserved0`≠0 rejected); phase 3 CP **T6** routes+service layer → 404 · **T7** applier stops loading rows · **T8** migration 0014 (delete **before** CHECK, or it fails on existing data); phase 4 `[P]` **T9** bloom-FP 3→2 + telemetry golden · **T10** SPA tab; phase 5 **T11** docs · **T12** end-to-end verify. Only T9/T10 parallel (T9's CP half is `unit`, T10's `fe` gate is compose-independent); T6/T7/T8 strictly serial on shared `compose.test.yml`. **Key sequencing insight:** the v3→v4 bump is *deferred* to T5 — T2 stops programming the sbl maps while still parsing and discarding `sbl[]` at v3 — so T1–T4 each stay green and the risky contract change lands alone and bisects cleanly. Next: **Execute**.

### AD-039: B2 — remove service-scoped blacklist entirely; global blacklist is the only scope (2026-07-23)

**Decision (spec.md, SBR-01..25, 4 gray areas closed pre-spec):** perf item **B2** from `docs/danh-gia-hieu-nang-data-plane.md` §8.3 is executed as a **full-stack removal**, not a runtime gate. (a) **D1 = full-stack**: the `sbl` bloom+LPM pair, `struct sbl_lpm_key`/`sbl_bloom_key`, `BL_STATE_SERVICE_HIT`, `BL_F_ACTIVE`/`BL_F_HAS_BROAD`, `BLOOM_FP_SERVICE`, the four inner maps and the `service_blacklist:` branch of `deny_filter_stage` ([blacklist.h:456-481](../../data-plane/src/blacklist.h#L456)) all leave the tree — reversible only by revert. (b) **D2 = delete existing service-scoped rows** in migration `0014` (no promotion to global, no dedupe): sources blocked *only* by a service-scoped entry become unblocked at the next apply, deleted `(service_id, source_cidr)` pairs logged at INFO for manual re-add. (c) **D3 = no tenant-facing replacement** — blocking becomes admin-only by design; this is a **product capability reduction**, since `POST /services/{id}/blacklist` is tenant self-service (`load_service_for_principal`) while `/blacklist` is `require_admin`. Tenants retain allow-rules, service rate-limits, whitelist/VIP. (d) **D4 = routes removed entirely (404)**, no 410 deprecation window. (e) **Wire contract bumps to v4** (`APPLY_SNAPSHOT_SCHEMA_VERSION` 3→4): `sbl_count`/`sbl[]` is a *trailing variable-length* section of every service record, so an unbumped version would let a v3 writer's bytes be parsed as a v4 record and corrupt the whole service table — mixed-version apply must reject wholesale, before any map write.
**Reason:** the service branch costs an outer-map lookup + bloom peek + LPM lookup per whitelist-miss packet on top of the ~15–20 map ops the accept path already pays (§8.1), for a scope carrying disproportionate cross-stack machinery (4 inner maps, a 12-byte LPM key, a service-map flag byte, a wire section, a bloom-FP index, a DB scope enum + check constraint + partial index, 3 routes, a UI tab). Threat feeds write **global-only** ([feed_reconcile.py:306](../../control-plane/app/services/feed_reconcile.py#L306)), so the feed pipeline is untouched — that fact is what makes full removal viable rather than merely gating.
**Trade-off:** tenants lose self-service blocking (every block becomes an admin ticket **and** applies to all tenants globally); 404 is indistinguishable from an unknown `service_id` for existing clients; deleted rows are unrecoverable on downgrade. Two design questions left open: **GA-3** does the `scope` column survive (recommendation: keep it narrowed to `global`, since `feed_reconcile.py` filters `scope = 'global'` in ~10 raw-SQL sites and `uq_blacklist_global_source_cidr` is a partial index on it; dropping it = deferred cleanup), and **GA-4** is `bl_flags` dropped from the wire or kept as `reserved0` (recommendation: keep the byte for A4's forthcoming "feature active" bitmask, since the version bump is happening anyway).
**Impact:** **Large** — spans `data-plane/src/{blacklist.h,service.h,whitelist.h,apply_snapshot.h}`, `tools/{xdpgw-apply.c,dpstat.c}`, `loader/loader.c` (incl. `XDPGW_SEED_SBL_CIDR`), `tests/{test_snapshot.c,apply_smoke.py,fixtures/*}`, CP `db/models.py` + migration `0014`, `services/lists.py`, `api/routers/lists.py`, and FE `BlacklistTab.tsx` + `ServiceDetailPage.tsx` + `BloomFpPanel` (`BLOOM_STAT_MAX` 3→2, legacy 3-key `node_health.bloom_stats` rows must still render). PRD §6.5/§6.6 and the tenant capability lines (72, 117, 123, 136, 149) change; blacklist-filters BLK-03/BLK-04 and the BL-02 scoping posture become **superseded**. Frozen drop-reason ABI unchanged (`blacklist_drop` = 8, now global-only). Spec at `.specs/features/service-blacklist-removal/spec.md`. Next: **Design** (GA-3 + GA-4 must be settled there).

### AD-038: Promote UDP reflection & amplification out of the DDoS Protection page into its own admin tab (2026-07-23)

**Decision (spec.md Amendment A2, AMP-26..29):** the A1 unified blocked-source-port section becomes its **own admin-only tab**. (a) New `control-plane/frontend/src/features/config/ddos/AmplificationPage.tsx` at **`/admin/amplification`** (registered inside the existing `ProtectedRoute allowedRoles={['admin']}` block in `App.tsx`) owns the whole surface moved verbatim from `DdosProtectionPage`: `REFLECTOR_LABELS`, the `BlockedPortRow` discriminated union, the unified `DataTable` (locked built-in rows / removable dynamic rows / empty-dynamic hint = AMP-22..24 behaviour intact), the Add `Dialog`+`BlockedPortForm` and the remove `ConfirmDialog`; its `PageHeader` title is the full "UDP Reflection & Amplification" and the table's h2 is "Dynamic blocked source ports". (b) **D-AMP-6 = `/admin/ddos` survives as the coverage overview** (chosen over collapsing it onto the shared tenant `DdosCoveragePage`, and over dropping the admin nav entry): it renders `ProtectionCoverage` **only** plus a new cross-link Card "Tune UDP reflection & amplification" → `<Link to="/admin/amplification">Manage blocked source ports →</Link>`. (c) **D-AMP-7 = sidebar label verbatim "UDP Reflection & Amplification"** under *Manage*, directly after "DDoS Protection", reusing the ProtectionCoverage lightning-bolt glyph. ~30 chars against an 8–16 char cohort so it **wraps to two lines** — verified safe with **no CSS change**: `.navLink` is a flex row with no `white-space: nowrap` and `.navIcon` already carries `flex-shrink: 0`, so the icon cannot be squashed. (d) **D-AMP-5 still binds** — `ProtectionCoverage.tsx` and `DdosCoveragePage.tsx` byte-unchanged (AMP-29), so the tenant `/ddos` view is untouched.
**Reason:** A1 made the port section the page's centre of gravity, leaving a **read-only coverage overview carrying the node's only tunable deny-list**. The two have different audiences and change rates — coverage is documentation read once, the port list is config edited under attack — so burying a live control under a documentation grid costs a scroll at exactly the wrong moment.
**Trade-off:** one more admin nav item (now 11 under *Manage*) and a wrapping two-line label; `/admin/ddos` is left thin (coverage grid + link) and its content overlaps the tenant page, which is accepted rather than merging the two audiences' routes. Discoverability of the port list now depends on the nav entry plus the cross-link card.
**Impact:** **presentation-only, FE-only** — new `AmplificationPage.tsx` + `AmplificationPage.test.tsx` (5 specs moved/rewritten from the old page spec); `DdosProtectionPage.tsx` reduced to coverage + link and its spec rewritten to 2 specs (coverage renders / port management absent + link href); `App.tsx` route; `Sidebar.tsx` NavLink; `AppShell.test.tsx` +2 nav assertions (admin sees both entries, tenant sees neither). **No** API/schema change, no worker change, no data-plane change, no migration. **fe gate green:** lint 0 warnings, typecheck clean, **225 tests / 52 files** (223→225), build ok. Builds on AD-037 (A1).

### AD-037: UDP amplification tab — unify built-in + dynamic ports into one "UDP reflection & amplification" table (2026-07-23)

**Decision (spec.md Amendment A1, AMP-22..25):** the DDoS Protection admin page merges its two blocked-source-port blocks — the read-only "Built-in blocked source ports (always on)" chip Card and the "Dynamic blocked source ports" `DataTable` — into **one section** headed `UDP reflection & amplification` (h2) / subtitled "Dynamic blocked source ports", backed by a **single `DataTable<BlockedPortRow>`**. (a) **D-AMP-4 = one table, built-ins as locked rows** (chosen over chips-above-table and a source-column + filter toggle): a discriminated `BlockedPortRow = {kind:'builtin',port} | {kind:'dynamic',port,entry}`, built-ins sorted first and rendered with a `Badge variant="success">Built-in` + a reflector label from a new page-local `REFLECTOR_LABELS` map (17 QOTD … 11211 memcached, unknown → "Well-known reflector"), `Blocked At` = "Always on", and `rowActions` returning **`null`** so they carry no Remove; dynamic rows keep note / timestamp / Remove. (b) Because built-in rows are always present the `DataTable` `emptyState` can never fire, so the `EmptyState` import is dropped and replaced by an **inline hint** rendered when `!isLoading && !error && dynamic_ports.length === 0` (AMP-24) — the page-header "Add Blocked Port" button remains the affordance. (c) **D-AMP-5 = `ProtectionCoverage` stays byte-unchanged** (AMP-25): it is shared with the tenant-facing `DdosCoveragePage`, so hiding or retitling its amplification card to de-duplicate the admin page would change what tenants see; the residual overlap is accepted as summary-then-detail. Its card title is an **h3** of the same name, so the merged section's heading assertion must query `{ level: 2 }` — noted in the spec.
**Reason:** post-delivery UX review — AMP-15's two blocks plus the coverage card described the same vector in three places and forced the admin to mentally union two lists to answer "which UDP source ports are blocked right now?". One table = one mental model. The filter-toggle variant was rejected as extra state and test surface for a 12-row compile-time constant.
**Trade-off:** the built-in rows inflate the table by 12 constant rows before the (usually short) admin list, and the coverage card still repeats the vector name one screen above. Accepted: the ports are the answer to the operator's question, and preserving the shared component keeps the tenant page correct and the diff to one file.
**Impact:** **presentation-only** — `control-plane/frontend/src/features/config/ddos/DdosProtectionPage.tsx` + `DdosProtectionPage.test.tsx` (+1 spec: built-ins-with-no-dynamic-ports hint; existing render spec rewritten for the merged table, 12 `Built-in` badges, exactly 1 Remove). **No** API/schema/`AmplificationConfigResponse` change, no worker/reconcile-lane change, no data-plane change, no migration. **fe gate green:** lint 0 warnings, typecheck clean, **223 tests / 51 files** (220→223), build ok. Supersedes AMP-15's layout only; AMP-01..19 enforcement/propagation unaffected.

### AD-028: Double-buffer map build/swap — design (C apply-helper, fresh-inner replacement, full-node rebuild + feed carry-forward, structural-verify abort-before-flip) (2026-07-10)

**Decision (design.md):** (a) **`DoubleBufferApplier`** (new class in `app/worker/applier.py`) replaces `PlaceholderApplier` **only at the `__main__` DI site** — the AD-027 boundary `apply(config: ServiceConfig)` and `process_job` are byte-unchanged (D-DBS-1/A-DBS-1). It does **no BPF in Python**: loads a consistent full-node snapshot from PG (`load_node_config`, all enabled services + plan/rules/wl/service-bl, reusing AD-027 `ServiceConfig`), serializes to a temp file in the new **`apply_snapshot.h`** versioned explicit-LE binary wire format (first CP↔DP data crossing; `schema_version` + golden-fixture round-trip), and `asyncio.create_subprocess_exec`s the **new C helper `data-plane/tools/xdpgw-apply.c`** with a timeout, mapping exit 0→`mark_active` / nonzero→`ApplyError(stderr)`→`mark_failed` (D-DBS-1). (b) **Helper = build→verify→single-write swap:** read `active_config{A,V}` fresh (never cached, DBS-24) → `inactive=1-A` → for each **service-scoped** outer (`service_map`, `rule_block_map`, `whitelist_bloom`+`lpm`, `vip_config_map`, `service_blacklist_bloom`+`lpm`, `fair_config_map`) **create a fresh inner meta-equal to the active slot's inner** (`bpf_map_get_info_by_fd`→`bpf_map_create`, `btf_fd=0`), populate from the snapshot (loader leaf-writer idioms + shared fair math + env node knobs), `bpf_map_update_elem(outer,inactive,fresh_fd)` install → **carry-forward feed-owned deny maps** (`global_blacklist_bloom`/`lpm`, `udp_blocked_port_bitmap` = pointer-copy the active inner fd into inactive; `gbl_meta` = row copy — O(1), the 1M global blacklist is NEVER rebuilt per job, D-DBS-2/A-DBS-4) → **structural read-back `verify_slot(inactive)`** (every enabled svc present, `rule_block` version/count match, inner fds installed — D-DBS-3) → **single `bpf_map_update_elem(active_config,0,{inactive,V+1})` COMMIT** (DBS-03/19). (c) **Abort-before-flip is the entire rollback:** only the inactive slot is ever written; any build/verify failure returns before the commit (live slot untouched — "flip back" never needed); the flip is one aligned `__u32` (`active_slot`) so the hot path (reads `active_slot` once/packet, indexes all outers with it) never straddles a swap; crash/kill mid-build → live slot unchanged, next apply overwrites the half-built inactive slot (DBS-11/13/14). (d) **Uniform fresh-inner replacement** for all `ARRAY_OF_MAPS` service-scoped outers (blooms *cannot* be cleared per the header contract ⇒ replacement mandatory there; using it everywhere dodges stale-service-key deletion; fresh inner starts empty). (e) **Loader change (additive):** pin the 14 config maps (`service_map`/`rule_block_map`/`whitelist_bloom`+`lpm`/`vip_config_map`/`global_blacklist_bloom`+`lpm`/`service_blacklist_bloom`+`lpm`/`udp_blocked_port_bitmap`/`fair_config_map`/`fair_node_config`/`gbl_meta`/`active_config`) mirroring the observability pin/unpin/rollback; extract `clamp_fair_rate`/`fair_rate_product`/budget derivation to shared `fair_budget.h`; loader stays running = map-lifetime owner (helper opens by pin, never unpins). Static inner_0/_1, `tx_devmap`, runtime-state maps NOT pinned (§8.3, DBS-05). (f) **dpstat** +`active_config` slot/version section (DBS-27); `Settings` +`worker_apply_binary_path`/`worker_apply_timeout_seconds` (5 s); **no CP schema change/migration, no new `JobType`** (A-AGW-4). Node knobs (capacity/k/ref_pkt) = loader-env inherited by the helper (D-FAIR-2); per-service committed/ceiling from the snapshot.
**Reason (verified 2026-07-10):** grounded in the live data-plane — outers are `ARRAY_OF_MAPS[2]` with static inner_0/inner_1 (BTF `.values`), only observability maps pinned, and `whitelist.h`/`blacklist.h` **already document** the M4 contract ("build fresh inners and swap them into the inactive slot; keep value_size/max_entries/map_extra equal"). Reusable in `loader.c`: `seed_rule_block_fd`/`seed_wl_slot`/`seed_*_blacklist_*`/`seed_fair_config_slot` leaf writers + `clamp_fair_rate`/`fair_rate_product`/budget math; struct sizes confirmed (`rule_block` 520 B, `fair_config` 40 B, `fair_node_config` 16 B, `gbl_meta` 4 B). One load-bearing new fact **web-verified** (kernel map_of_maps.html + eBPF-docs ARRAY_OF_MAPS + libbpf#834): userspace `bpf_map_create`+`bpf_map_update_elem(outer,&slot,&inner_fd)` runtime inner replacement; inner must match template meta (type/key/value/max_entries/flags/map_extra = `bpf_map_meta_equal`; BTF NOT compared → `btf_fd=0` fine); a BPF program cannot modify outer entries (userspace-only) — exactly our model. `active_config` single-`__u32`-slot write atomicity from the hot-path read (`lookup(active_config,0)→active_slot` only; `version` observability). Context7 unavailable.
**Trade-off:** a process boundary (serialize node config in → exit-code+stderr out) instead of an in-process call; full-node rebuild is O(all active services) per single-service job (redundant but simple at the ≤1000-service envelope; the 1M blacklist is excluded via carry-forward, keeping ≤5 s); the two original static inners leak for loader lifetime (skel holds their fds after replacement — bounded, one-time); `active_config.version` monotonic node-global map version distinct from per-service `active_version`; feed carry-forward converges both slots' global-deny inners to a shared object (M4 #3 feed sync must be slot-aware — its problem, out of scope here). The one novel composition (separate-process fresh-inner install into a **pinned** outer) is **de-risked fail-fast at the first Execute gate** with a 3-rung fallback (pin fresh inners / loader pre-pins static inners → loader hosts apply via IPC), all verdict-identical.
**Impact:** M4 Double-buffer map build/swap (DBS-01..28) — new `app/worker/applier.py::DoubleBufferApplier`+`load_node_config`+`serialize_node_snapshot`, `__main__` DI swap, `Settings` fields; new `data-plane/tools/xdpgw-apply.c` + `src/apply_snapshot.h` (wire contract) + shared `fair_budget.h`; loader config-map pins; dpstat `active_config` section; `make apply`/`make applybulk` targets; TESTING.md `xdpgw-apply` section. Reuses frozen contracts AD-015/019/021/023/025 + pin pattern AD-017; D-SLRD-1 loader env seed downgrades to initial-slot bootstrap. 2 rendered diagrams. **Requires agent-worker executed first** (Execute gated). 6 flags for Tasks (loader-refactor depth; binary-vs-JSON snapshot; ≤5 s test placement; fresh-inner de-risk rung; helper-computed headroom; apply timeout default/attempts). Next: **Tasks**.

### AD-027: Agent worker & job pipeline — control-plane design (two-transaction version guard, loop-free testable core, placeholder applier boundary) (2026-07-10)

**Decision (design.md):** (a) **New `app/worker/` package**, colocated process `python -m app.worker` (`cli.py` asyncio.run precedent): `__main__.py` → `Worker` runtime (BRPOP consume + startup/periodic `reconcile_once` + SIGTERM/SIGINT grace + Redis/DB bounded-backoff degrade) → **loop-free, injectable core** `process_job` / `reconcile_once` / `recover_orphan` (plain coroutines, directly testable without loops/signals) → `HANDLERS: {JobType→handler}` registry (no-handler → `mark_failed`, AGW-08 forward-compat) → `handle_service_update` (reads full config from PG at apply time) → injected **`Applier` protocol** (v1 `PlaceholderApplier` logs+succeeds, **M4#2 swaps the impl behind the boundary, not the boundary**; never touches bpffs so startup does no unsolicited swap). (b) **Crux — two transactions per job:** `mark_applying` commits and **releases** the `ProtectedService` FOR UPDATE lock *before* the applier runs; terminal `mark_active`/`mark_failed` **re-takes** it — the only way a mid-apply `bump_version→N+1` is observable, then caught by the executed `_superseded(service,job)` check (version N → `superseded` no-op, N+1 advances once). Holding one lock across the applier would serialize mutations behind the apply and defeat the guard. The worker **re-reads `job.status` after `mark_applying`** to decide proceed/skip (executed `apply.py` left byte-for-byte untouched, AGW-04); `db.get` pre-check avoids depending on apply.py's `HTTPException(404)` in a non-HTTP process. (c) **Orphan recovery (D-AGW-2) = one transaction, existing edges only:** `mark_failed(job,"worker restarted mid-apply")` + `retry(db, service, actor=None)` — `retry`'s audit already records `actor_username="system"` (resolves the flagged attribution question with **no change to `retry`**); durable evidence = the `apply.retry` system audit event + incremented `attempts`; `failed` state transient on the reused job row. **Startup-only** orphan sweep (single worker ⇒ a runtime `applying` row is the live job); periodic sweep = `queued & dispatched_at IS NULL` only. (d) **New shared `session_scope` UoW** in `db/session.py` (additive) reproduces `get_db`'s `commit()` + `run_post_commit_callbacks()` so `retry`/`enqueue` post-commit re-dispatch actually LPUSHes from the worker. (e) `Settings` extended with `CONTROL_PLANE_WORKER_*` (poll 2 s / reconcile 15 s / backoff 0.5→30 s / grace 10 s); **no new models, no migration, no endpoints** — reads/queries `AgentJob`+`ProtectedService`, mutates only via executed `apply.*`.
**Reason (verified 2026-07-10):** grounded entirely in the executed M1 surface read live — `mark_*` flush-not-commit + silent no-op-on-superseded/terminal, `retry` NotFailedError guard + system-actor audit, `add_post_commit_callback`/`run_post_commit_callbacks` fire only when the caller runs them (API's `get_db` does; the worker must), `LPUSH apply:jobs` ⇒ FIFO consumer `BRPOP apply:jobs`, `expire_on_commit=False` makes the post-`mark_applying` status re-read free. One load-bearing external API web-verified (redis-py async `brpop([key],timeout)` → `(key,value)` str under `decode_responses=True`, `None` on timeout — redis.readthedocs.io + BRPOP ref); asyncio `add_signal_handler` stdlib. Context7 unavailable.
**Trade-off:** two `fair_config`-style service-row lock acquisitions per job (needed for the guard); a runtime orphan from a transient failure that the worker survives leaves a **cosmetic** `applying` ledger row (never wedges the service — version < service.version, next mutation supersedes it; healed at restart); Redis-down degrade drops to ≤ reconcile-interval latency (≤60 s) instead of ≤5 s; the placeholder's `active` overstates reality until M4#2 (D-AGW-1, documented at the boundary).
**Impact:** M4 Agent worker (AGW-01..30) — new `app/worker/{__main__,worker,processor,handlers,applier}.py` + `session_scope` added to `db/session.py` + `WORKER_*` fields on `Settings`. Establishes the `Applier` boundary + `HANDLERS` registry + `process_job`/`reconcile_once` orchestration that M4#2 (`DoubleBufferApplier`, `MAP_REBUILD`/`ACTIVE_SLOT_SWAP`), M4#3 (`FEED_SYNC`), M5 (`TELEMETRY_AGGREGATE`) plug into. Tests: unit (registry/placeholder/backoff/UUID) quick + integration (happy ≤5 s, no-handler, handler-fail+retry, reconcile/Redis-lost, orphan recovery, **BarrierApplier supersede-under-churn crux**, duplicate delivery, shutdown) on `compose.test.yml`. 2 rendered diagrams. 5 flags for Tasks. Next: **Tasks**.

### AD-026: Agent worker & job pipeline — 2 gray areas (v1 placeholder applier; orphaned-`applying` auto-recovery via existing retry) (2026-07-10)

**Decision:** (a, **D-AGW-1**) the v1 `SERVICE_UPDATE` applier is a **succeeding placeholder** — it reads the target's full config from PostgreSQL, logs what it would build, and succeeds, so the whole `queued→applying→active` lifecycle (reconcile, churn, crash recovery, idempotency) runs and is integration-tested end-to-end in this feature; `active`/`active_version` mean "**acknowledged by worker**" until M4#2 (*Double-buffer map build/swap*) replaces the applier **implementation** (never the boundary) and re-validates the ≤5 s bound with real map builds (A-AGW-7). (b, **D-AGW-2**) jobs orphaned at `applying` by a worker crash **auto-recover in the startup sweep** via `mark_failed("worker restarted mid-apply")` + the **existing** retry path (`failed→queued` + re-dispatch) — zero new state-machine edges in M1's frozen `LEGAL_APPLY`, a visible `failed` blip in job history, bounded by `attempts`; a wedged-but-alive worker is out of scope (M6 stuck-job alerting). Spec approved as drafted (AGW-01..30). Full context in `.specs/features/agent-worker/context.md` (D-AGW-1..2, A-AGW-1..8).
**Reason:** the placeholder mirrors the D-SLRD-1 interim-writer precedent (feature-testable now, authoritative writer next feature) — the alternatives leave nothing demoable (non-terminal jobs) or drag double-buffer design into the wrong feature (minimal real map write); auto-recovery keeps the ≤5 s propagation promise approximately intact across routine restarts with no human in the loop and no new machinery, where manual-only retry strands every in-flight change and lease/heartbeat reclaim is multi-worker machinery v1 doesn't need (A-AGW-2 single worker).
**Trade-off:** between this feature's Execute and M4#2's, the UI's `active` overstates reality (no data-plane change occurred — pilot-internal, documented at the boundary; the loader env seed remains the data-plane's writer); each crash-recovery costs one visible `failed` + one `attempts` increment per orphaned job.
**Impact:** M4 Agent worker & job pipeline (AGW-01..30) — first M4 feature, the consumer half of executed M1 apply-status: worker process + `BRPOP apply:jobs` + DB-ledger reconcile (outbox promise A-APLY-1/APLY-27/36 fulfilled), handler registry + applier boundary (= M4#2's fill-in point), all transitions via executed `mark_*` (APLY-03). `JobType` stays `SERVICE_UPDATE`-only (A-AGW-4); jobs carry identity not payload (A-AGW-5); no new endpoints. Agent discretion at Design: package layout/entrypoint, loop/backoff shapes, applier boundary signature, system-actor audit attribution for the auto-retry, interval defaults, fake-applier test mechanics. Pure control-plane (independent of M3 fairness Execute). Next: **Design**.

### AD-025: Fairness & bandwidth reservation — data-plane design (spin-locked exact committed bucket, precomputed budgets, pkt_meta first growth) (2026-07-10)

**Decision (design.md):** (a) **One header:** new `src/fairness.h` = map contract + `ingress_cap_stage(ctx, meta, slot)` (WLV-24 seam A) + `fair_admit_stage(ctx, meta, slot)` (replaces `admit_clean()`'s ARL-24 seam body; signature → `admit_clean(ctx, meta, slot)`, internal). (b) **Committed bucket = unslotted top-level `HASH`** (1024, prealloc) with BTF value `{bpf_spin_lock lock; cfg_version; tokens; last_ns}` — `bpf_ktime_get_ns()` captured **before** the lock, critical section = pure inlined ALU (version-check/reset → refill → consume), kernel-lazy element creation (`BPF_NOEXIST` + re-lookup) before locking. Fallback ladder (FAIR-22, proven at first load gate): BTF spin_lock → `__sync` atomics global bucket (bounded refill skew) → per-CPU rate÷nCPU split (ARL-14-style bound); all verdict-preserving. (c) **Burst/node/cap buckets reuse `rl_bucket` + `rl_*` helpers verbatim** (WLV precedent, `rules.h` untouched except `admit_clean`): `svc_burst_state` PERCPU_HASH, `node_burst_state` PERCPU_ARRAY[1], `service_ingress_cap_state` PERCPU_HASH dual pps+bps; D-ARL-2 lazy version reset everywhere; burst dual-draw = **service-then-node, no refund on node-miss** (A-FAIR-6; reason attribution: burst empty → 11, node empty → 12). (d) **Config = `fair_config_map`** ARRAY_OF_MAPS[2] of HASH (`service_id → {version, committed_bps, burst_bps, cap_bps, cap_pps}`, 40 B) + `fair_node_config` plain ARRAY[2] slot-keyed (gbl_meta precedent) — **all budgets precomputed by seed/M4 worker**; k (default 3), ref-pkt (512 B), capacity (default 40 Gbps) are **userspace env only**, kernel does zero derivation (AD-019 dumb-copier). Missing row for a matched enabled service = `DR_MAP_ERROR` (builder invariant). (e) **`pkt_meta` first deliberate growth 32→40 B** (`fair_state` outcome enum + 7 spare bytes; zero pad remained — A-FAIR-7 void; assert migrated, header-driven recompile). (f) **`FAIR_RATE_MAX` = 16e9 B/s (128 Gbps) builder clamp** — `elapsed×rate` in reused `rl_refill_dim` overflows u64 at ~147 Gbps; generous default seed = 100 Gbps committed=ceiling (burst 0, baseline verdict-identical, FAIR-23).
**Reason (verified 2026-07-10, bpf_spin_lock introduction patch + eBPF docs, Context7 unavailable):** spin_lock available to all program types **except tracing/socket-filter** (XDP in-spec, ≥5.1, target kernel 6.8); HASH/ARRAY values only, **never map-in-map inners** (our committed map is top-level unslotted — composes); mandatory BTF, one lock per value, **no helper/function calls inside the critical section** (shapes the now-before-lock flow); map_update ignores the lock field (lazy create + userspace seeding safe). Overflow bound derived from codebase (`rl_refill_dim` math).
**Trade-off:** two `fair_config` HASH lookups per clean packet (one per stage) instead of threading a pointer through 3 mid-flight stage signatures (localized, documented); a node-miss burns one already-drawn service-burst token (per-CPU burst approximate by charter); `pkt_meta` growth is a one-time ABI churn the static assert was designed to force; §8.3's single `service_agg_rate_state` row is physically two maps (global-locked + per-CPU) — mapping documented.
**Impact:** M3 Fairness (FAIR-01..27) — new `fairness.h`; surgical seam edits in `xdp_gateway.bpf.c` + `rules.h`; `pkt_meta.h` growth; loader env family (`XDPGW_FAIR_{COMMITTED,CEILING}_BPS`, `XDPGW_NODE_CLEAN_CAPACITY_BPS`, `XDPGW_FAIR_K`, `XDPGW_FAIR_REF_PKT`) + fair rows in seed; deterministic fairness scenario = M3 gate (small-packet flood exercises the D-FAIR-1 pps dimension); dpstat zero-change. 2 rendered diagrams (`diagrams/fairness-{ladder-flow,map-layout}.{mmd,svg}`). Next: **Tasks**.

### AD-024: Fairness & bandwidth reservation — 2 gray areas (dual-dimension ingress cap k=3; env node capacity with 40 Gbps default) (2026-07-10)

**Decision:** (a, **D-FAIR-1**) the per-service ingress-cost cap is **dual-dimension**: byte budget = `k × ceiling_clean_gbps` directly; packet budget = `k × ceiling ÷ reference_packet_size` (documented, node-tunable, ~512 B — exact default at Design); default **k = 3**, node-tunable at load (AD-019 rodata posture), never per-service in v1; reuses ARL's dual `pps/bps` bucket shape. (b, **D-FAIR-2**) `node_clean_capacity` is delivered as an **env-driven seed/loader value** (D-SLRD-1 posture) with a **documented 40 Gbps default** (§15 node target) when unset — the loader never fails on it; headroom rate = `capacity − Σ committed`, floored at 0, recomputed by the seed (v1) / M4 worker (authoritative) on plan changes. Spec approved as drafted (FAIR-01..27). Full context in `.specs/features/fairness-bandwidth/context.md` (D-FAIR-1..2, A-FAIR-1..8).
**Reason:** pps, not bps, is the CPU cost dimension the cap exists to bound — a bps-only cap admits ~44 Mpps of 64 B classification work at 10 Gbps×k=3, more than the whole node's 20 Mpps target; and the capacity value's mis-set blast radius is **burst QoS only** (committed skips the node bucket; §7.2 is control-plane-warned via SRL-36), so the dev/baseline-friendly default beats a fail-loud env for the lowest-stakes knob in the feature.
**Trade-off:** the reference packet size is a heuristic — legitimately tiny-packet services hit the pps cap before `k×ceiling` bytes (mitigable per node via k/ref-size knobs, documented); a node whose real capacity ≠ 40 Gbps with a forgotten env gets mis-sized burst admission until set (committed unaffected, observable via `congestion_drop`).
**Impact:** M3 Fairness & bandwidth reservation (FAIR-01..27) — final M3 feature; fills the ARL-24 (`admit_clean()` ladder) and WLV-24 seam A (ingress cap) seams; wires frozen ABI **11/12/13** (all 16 §9.2 reasons live); new slotted rate-config map from `ServicePlan` = M4 build contract (A-FAIR-2, `service_val` pads exhausted); deterministic fairness scenario = the M3 milestone gate (FAIR-24); spin-lock-in-XDP de-risked fail-fast with verdict-preserving fallback (FAIR-22/A-FAIR-8). Agent discretion at Design: rate-config map layout, committed-bucket critical section + fallback ladder, burst dual-draw ordering (A-FAIR-6), ref-size default/delivery, env names, `pkt_meta` outcome encoding, fairness-scenario mechanics. Requires blacklist-filters executed first. Next: **Design**.

### AD-023: Blacklist & deny filters — data-plane design (/24 global bloom + expansion band + slot escape, 1M LPM measured-not-asserted, pad-byte flag gates) (2026-07-09)

**Decision (design.md):** (a) **One stage:** new `src/blacklist.h` `deny_filter_stage(ctx, meta, slot, bl_flags)` replaces WLV's seam-B comment inside `whitelist_miss()`; §8.2 order amp→bogon→bitmap→global→service→`allow_rule_stage()`; amp ports (D-BLK-1 12-port switch) + bogon set (A-BLK-1) are **pure compile-time code** (no maps, no rodata tables). (b) **Global bloom granularity:** /24 bucket keys (`__be32 src&/24`) + **builder expansion band** for prefixes 16..23 (≤256 keys/entry) + slot-level `gbl_meta.GBL_F_HAS_BROAD` escape only for </16 entries + **fill invariant** (computed keys > 2M ⇒ escape, never over-fill); `GBL_F_ACTIVE` skips the whole band when empty. Rationale: /16 buckets saturate at the 1M envelope (65,536 buckets exist); AD-021's per-service escape has no global analog. (c) **Global LPM 1M:** `max_entries` 1M `NO_PREALLOC`; footprint **measured at a new gated `make blbulk` bulk-load check** (~≤128 MiB/slot estimate, kmalloc-per-node — deliberately not asserted a priori); Cloudflare bench (~670 ns/lookup @1M) baked in as "only bloom-hit traffic touches the trie". (d) **Service blacklist = AD-021 verbatim** (`sbl_lpm_key` prefixlen≥32, 8-byte scoped bloom keys, 64K); per-service gate in `service_val`'s **last pad byte → `bl_flags`** (`BL_F_ACTIVE`/`BL_F_HAS_BROAD`, size 8 frozen); flags threaded by `whitelist_stage` taking `const struct service_val *` (internal signature, no M4-contract change in whitelist.h). (e) **Bitmap** = `ARRAY_OF_MAPS[2]` of `ARRAY` 1024×u64 (8 KiB/slot, host-order port bit index); `gbl_meta` = plain slot-keyed `ARRAY[2]` (§8.3 slot-in-key sanctioned). (f) **`bloom_hit_lpm_miss`** = unslotted `PERCPU_ARRAY[3]` `bloom_stats` (whitelist/global/service per-stage — M6 alert must localize), pinned, new dpstat `counters` section; **bump only when the bloom was actually consulted** (HAS_BROAD skips must not fabricate FPs); whitelist.h gains its deferred one-line FP bump. (g) `pkt_meta._pad` → `bl_state` (7 outcomes, size 32 frozen); BLK-24 migration via named non-bogon source constants in `pkt_build.h` (45.45/16, 185/8 pool).
**Reason (verified 2026-07-09, kernel docs/source + Cloudflare production data, Context7 unavailable):** bloom bitset = `max_entries × nr_hashes × 7/5` bits pow2-rounded → 2M×5 ≈ 2 MiB/slot; LPM trie = mandatory `NO_PREALLOC`, kmalloc-per-node, ~1.5M single-core ops/s @1M entries (dcache/dTLB bound) → the bloom guard is load-bearing, not an optimization; bloom-inner-in-`ARRAY_OF_MAPS` already **proven at load** by WLV T1; `ARRAY` inner = standard (AD-015 "any inner except PROG_ARRAY"), fallback = slot-in-key singles.
**Trade-off:** a </16 global entry degrades the whole global list to always-LPM (observable, rare, builder-alertable); 1M footprint is measured not guaranteed (gated check is the evidence); global+service blacklist share drop reason 8 (frozen §10.2 ABI) — `bl_state` disambiguates for tests/debug only.
**Impact:** M3 Blacklist (BLK-01..26) — new `blacklist.h` + surgical edits to `whitelist.h` (seam call, signature, FP bump), pad-byte fields in `service.h`/`pkt_meta.h`, loader pins+env seeds (`XDPGW_SEED_GBL_CIDR`/`SBL_CIDR`/`BLOCKED_PORT`, default empty), dpstat bloom section, `make blbulk` gate, BLK-24 source migration. 2 rendered diagrams (`diagrams/deny-{stage-flow,map-layout}.{mmd,svg}`). Next: **Tasks**.

### AD-022: Blacklist & deny filters — 2 gray areas (full amp-port set incl. 53/123; bitmap seed-only in v1) (2026-07-09)

**Decision:** (a, **D-BLK-1**) the hardcoded UDP amplification source-port set = the **full reflection set including 53/123**: 17, 19, 53, 111, 123, 137, 161, 389, 520, 1900, 5353, 11211 — compile-time constants (rebuild to change). Tenants legitimately receiving UDP from these source ports (resolver/NTP upstream responses) must whitelist those upstream sources (§6.5 VIP exception covers the filter by pipeline position; ceiling required per D-WLV-1) — onboarding guidance documented (BLK-26). (b, **D-BLK-2**) the `udp_blocked_port_bitmap` ships **enforcement + slotted map contract with the seed helper as its only v1 writer** (D-SLRD-1 posture); no control-plane model/CRUD/M4 job created — the authoritative writer (minimal admin CRUD or GA auto-response OP-02) is a deferred idea. Spec approved as drafted (BLK-01..26). Full context in `.specs/features/blacklist-filters/context.md` (D-BLK-1..2, A-BLK-1..8).
**Reason:** DNS/NTP are the two most common reflection vectors — excluding them guts the default posture, and the principled escape hatch (scoped, ceiling-capped whitelist) already exists; inventing a blocked-port control-plane surface would be speculative modeling (§7.1 deliberately has no entity) for a map whose natural writer is OP-02/M4.
**Trade-off:** resolver/NTP-style tenants get a mandatory onboarding whitelist step (forgetting it = visible `udp_amplification_drop`); "dynamic" bitmap is operator-dynamic (node map write), not API-dynamic, until a writer feature lands.
**Impact:** M3 Blacklist (BLK-01..26) — third M3 feature, fills WLV-24 seam B entirely (amp→bogon→bitmap→global bl→service bl→rule stage); wires frozen ABI 4/7/8; owns `bloom_hit_lpm_miss` (outside `counter_map`, covers whitelist + blacklist blooms); global maps sized to the 1M envelope; A-BLK-1 bogon set forces the documented dp-unit migration off RFC 5737 sources. Agent discretion at Design: global bloom granularity/FP target at 1M, bitmap representation, FP-counter home + dpstat surface, FP-induction test mechanism, map-free bogon check implementation, suite-migration mechanics. Next: **Design**.

### AD-021: Whitelist/VIP — data-plane design (composite scoped LPM key, /24 bloom buckets, zero-cost D-WLV-1 gate, VIP bucket reuse) (2026-07-09)

**Decision (design.md):** (a) **Scoping by key construction:** `whitelist_lpm` = `ARRAY_OF_MAPS`[2] of `LPM_TRIE` inners with composite key `{prefixlen, service_id(be32), src(be32)}` (max_prefixlen 64); every entry carries `prefixlen = 32 + cidr_len ≥ 32` so all `service_id` bits are always fully matched — BL-02 needs no runtime check. (b) **Bloom = /24 buckets:** `whitelist_bloom` = `ARRAY_OF_MAPS`[2] of `BLOOM_FILTER` inners (value = 8-byte `{svc_be, src&/24}`, 65536 entries, 5 hashes); entries with cidr ≥ /24 contribute exactly one key; broader entries set per-service `WL_F_HAS_BROAD` → skip bloom, always LPM-confirm (no 2^k key blow-up; degradation scoped to the owning service). Blooms are **replace-only** (no kernel clear/delete) → M4 builds a fresh inner per swap; meta-equal params (value_size/max_entries/map_extra) pinned in the contract. (c) **Zero-cost D-WLV-1 gate:** `service_val` gains `wl_flags` in a pad byte (`WL_F_ACTIVE`/`WL_F_HAS_BROAD`, value stays 8 B) — the already-fetched service lookup answers "whitelist active?" with no extra map access; builder sets ACTIVE only when entries exist AND ≥1 ceiling dim set. (d) **VIP ceiling:** slotted `vip_config_map` (HASH svc → {version, flags, pps, bps(bytes/s)}) consulted only on LPM hit; `vip_ceiling_state` = unslotted `PERCPU_HASH` svc → `struct rl_bucket` **reused verbatim** from `rules.h` along with the rate-parametric helpers and the shared `test_no_refill` knob — `rules.h` itself untouched (ARL mid-execution). VIP admit calls `redirect_out()` **directly, not `admit_clean()`** — §8.4.6's "VIP never enters the fairness ladder" becomes structural. `pkt_meta` gains `wl_state` (pad byte; none/miss/hit-admit/hit-drop).
**Reason (verified 2026-07-09, kernel docs + eBPF docs, Context7 unavailable):** bloom map (≥5.16) push/peek from XDP, false negatives impossible, "may be used as an inner map" (`.map_meta_equal` registered); LPM data matched MSB-first big-endian with prefix 8..2048 bits → 64-bit composite scoped key is in-spec; per-CPU-hash/lazy-reset semantics carry over from AD-019 unchanged. The one undocumented *composition* (static bloom inners in BTF `__array(values,…)`) gets a fail-fast de-risk ladder: BTF static → loader-created inners (the M4 flow anyway) → LPM-only fallback (verdict-identical; bloom is cost-only by WLV-04).
**Trade-off:** broad (</24) entries force LPM cost on their whole service; a second LPM walk (8-byte key) on the whitelist path for services with active whitelists; `vip_config` as a separate map costs one hash lookup on the rare hit path (keeps SLRD's 8-byte `service_val` ABI frozen).
**Impact:** M3 Whitelist/VIP (WLV-01..25) — new `src/whitelist.h` (3 slotted maps + 1 runtime map + stage); one-line rewire of `service_lookup_redirect`'s enabled branch (seam A comment for M3#4); seam B (miss path) hosts M3#3; `service.h`/`pkt_meta.h` pad-byte extensions (sizes unchanged, zero test churn); loader default seed whitelist-free (baseline + smoke untouched, WLV-22), env-driven VIP seed for demos. 2 rendered diagrams. Next: **Tasks**.

### AD-020: Whitelist/VIP — NULL VIP ceiling = whitelist inactive (fail-safe) (2026-07-09)

**Decision (D-WLV-1, context.md):** a `ProtectedService` whose `vip_pps` AND `vip_bps` are **both NULL** grants **no whitelist bypass** — the data-plane treats its whitelist as empty (behaves exactly as a clean miss, WLV-06) until at least one ceiling dimension is set. One set dimension governs alone (the NULL dimension is unlimited); **`0` = explicit block** per dimension (whitelisted traffic always drops `vip_ceiling_drop` on it — A-ARL-3 consistent). Spec approved as drafted (WLV-01..25).
**Reason:** PRD §6.5 + risk register call the aggregate VIP ceiling **mandatory** (BL-08 whitelist-spoofing mitigation); NULL=unlimited would make that mitigation opt-in — an uncapped bypass of every defence. Fail-safe option (b) makes an uncapped bypass impossible **by construction**, with no control-plane schema change (fields stay nullable).
**Trade-off:** a whitelist "silently does nothing" until a ceiling is set — UX mitigation (SRL warning when entries exist on a NULL/NULL service + UI banner) captured as a deferred idea, out of this feature's scope. How "inactive" is enforced (builder emits nothing vs kernel-side check) = Design call, verdict identical either way.
**Impact:** M3 Whitelist/VIP feature (WLV-01..25, realizes WLV-13) — second M3 feature; wires frozen ABI index 14 (`vip_ceiling_drop`). Stage inserts between service-enabled hit and ARL's rule stage (hit bypasses rules + future M3#3 filters, subject to VIP ceiling; never the 8.4 admit ladder). Requires ARL executed first (shared hot-path files, A-WLV-8). Agent discretion at Design: bloom granularity over CIDRs (no false negatives), map layout (= M4 contract), `BPF_MAP_TYPE_BLOOM_FILTER` de-risk with LPM-only fallback, VIP bucket key shape, `pkt_meta` outcome field. Onboarding language: "whitelist requires a VIP ceiling to take effect". Next: **Design**.

### AD-019: Allow-rule matching & rate-limit — data-plane design (pre-sorted blocks, lazy version-reset buckets, rate÷nCPU split) (2026-07-09)

**Decision (design.md):** (a) **`rule_block_map` = `ARRAY_OF_MAPS`[2] of `HASH` inners** (`rule_block_0/1`, 1024 entries, `service_id → struct rule_block{version, rule_count, rule_entry[16]}`, 520 B) — same double-buffer shape as AD-015's `service_map`; **rules pre-sorted by ascending `priority` at build time**, so position = match order and `priority` never ships to the kernel (the DB's `UNIQUE(service_id,priority)` guarantees the total order); disabled rows included with `RULE_F_ENABLED` unset (M4 builder stays a dumb row-copier, ARL-05 stays kernel behavior). (b) **D-ARL-2 reset realized lazily**: `rate_limit_state` = preallocated `PERCPU_HASH` `(service_id, rule_idx) → {cfg_version, last_ns, pps_tokens, bps_tokens}`; every touch compares `cfg_version` vs `block.version`, mismatch → reinit to full burst — a slot flip self-resets buckets with **zero worker plumbing**, and the kernel's zero-fill of other CPUs' values on BPF-side element creation funnels first-touch through the same path. (c) **A-ARL-5 resolved: per-CPU rate = configured rate ÷ nCPU** (via `const volatile` rodata `rl_ncpus` set pre-load) with remainder-preserving ns-granular refill (`last_ns` advances by granted time, so quotas < nCPU still admit at the correct long-run rate); documented ARL-14 bound = node admit ∈ [rate/nCPU, rate], **never above configured** (enforcement inverts AD-017's full-budget-per-CPU sampling choice — fail-closed vs don't-starve-observability). (d) map `bps` field = **bytes/sec** (builder converts); `rl_config.test_no_refill` knob + runner CPU-pinning = deterministic dp-unit buckets (quota value = exact admit count).
**Reason (verified 2026-07-09, web + kernel docs, Context7 unavailable):** BPF-side `bpf_map_update_elem`/`lookup` on `PERCPU_HASH` access only the current CPU's slot (race-free under XDP, no atomics); `pcpu_init_value` zero-fills other CPUs' values on creation without `onallcpus` — the two facts that make the lazy-reset scheme sound. Bounded loops (≥5.3) + hash-inner map-in-map confirmed at the first build/load gate (fail-fast, not assumed; SLRD proved the harder LPM-inner case).
**Trade-off:** prealloc `rate_limit_state` costs ~32 MiB at 64 CPUs upfront (accepted: no allocator pressure under flood); rate÷nCPU undershoots when matched traffic concentrates on few RSS queues (documented; aggregate quotas under distributed flood spread well); 2 u64 divisions on quota'd-rule packets only.
**Impact:** M3 Allow-rule feature (ARL-01..25) — new `src/rules.h` (stage + 4 maps) + one-line insert at `service_lookup_redirect`'s enabled hit; `pkt_meta` gains `rule_idx` (pad byte, size unchanged); `admit_clean()` = the marked ARL-24 seam M3#4 replaces; loader seeds a match-all block per service (smoke unchanged); `rules.h` block layout **is** the M4 build contract. 2 rendered diagrams. Next: **Tasks**.

### AD-018: Allow-rule matching & rate-limit — 2 gray areas (strict `any`, buckets reset on swap) (2026-07-09)

**Decision:** (a, **D-ARL-1**) **`protocol = any` matches exactly {tcp, udp, icmp}** (strict PRD §6.4 reading); non-TCP/UDP/ICMP IPv4 protocols (GRE, ESP, …) are unmatchable by any rule → always `not_allowed` — v1 does not carry tunnel/IPsec traffic (documented product statement; explicit `gre`/`esp` protocol values captured as a deferred idea). (b, **D-ARL-2**) **rate-limit buckets reset on config swap** — keyed positionally/per-version, not by stable rule id; every apply of a service's rule/list edits re-grants full burst to that service's rules (bounded, brief). Spec approved as drafted (ARL-01..25). Full context in `.specs/features/allow-rule-ratelimit/context.md` (D-ARL-1..2, A-ARL-1..8).
**Reason:** Strict `any` is the strongest default-deny and matches "ANY trong phạm vi được hỗ trợ"; sustained `not_allowed` from tunnel traffic becomes *expected*, documented behavior. Reset-on-swap needs no rule-identity plumbing through the M4 build contract and keeps the bucket keyspace minimal; the one-extra-burst-per-apply cost is bounded by burst size.
**Trade-off:** Tunnel/IPsec tenants unsupported in v1 (product statement, not a bug); a flood matching a quota'd rule gets one extra burst per config apply during churn.
**Impact:** M3 Allow-rule matching & rate-limit (ARL-01..25) — first M3 feature; turns enabled services **default-deny** (zero rules = drop all, `not_allowed`). Wires frozen ABI indices 9/10 (AD-016/17); rule stage reads a new slotted `rule_block_map` via the SLRD pinned slot (AD-005); `rate_limit_state` unslotted runtime. Agent discretion at Design: per-CPU budget split + deviation bound (A-ARL-5), block map layout, deterministic-bucket test mode, `pkt_meta` matched-rule field. Migrates the 34-case dp-unit suite (enabled-service cases now need a seeded rule). Next: **Design**.

### AD-017: Drop-reason counters — data-plane design (ringbuf sampling, per-CPU token bucket, pinned maps, dpstat) (2026-07-08)

**Decision (design.md):** (a) **sampling channel = `BPF_MAP_TYPE_RINGBUF`** (256 KiB, power-of-2/page-aligned ≈ 8K events of a fixed 32-byte `drop_event`), not perf event array — single shared MPSC buffer, ordering, non-blocking `bpf_ringbuf_reserve` (NULL on full → `SAMPLE_LOST++`, never stalls); (b) **rate limit = per-CPU token bucket** (`sample_bucket` PERCPU_ARRAY) with knobs in a 1-entry `sample_config` ARRAY map (runtime-tunable, no reload; defaults 256 events/s + burst 64 **per CPU**, node bound = rate × CPUs); dp-unit determinism via `rate=0, burst=B`; (c) **`record_drop(meta, reason)` fuses exact counting + best-effort sampling** in one helper (DRC-15) and clamps out-of-range reasons to `DR_MAP_ERROR` (fail-closed); suppressed/lost accounting lives in a separate `sample_stats` map so `counter_map` stays a pure drop-reason ABI; (d) **loader pins** counter/ringbuf/config/stats maps under `/sys/fs/bpf/xdp_gateway/` (fail-loud on existing pins) and a new **`tools/dpstat`** CLI (counters [-w] / tail / rate) consumes pinned paths, decoupled from the loader process.
**Reason (kernel semantics web-verified 2026-07-08, Context7 unavailable):** ringbuf is XDP-compatible (≥5.8), reserve is NULL-on-full by construction, `bpf_ktime_get_ns` available in XDP; §11.1 mandates sampled+rate-limited event emission; per-CPU bucket keeps the drop path free of shared-cacheline atomics (same posture as `counter_map`).
**Trade-off:** budget is per-CPU (node total varies with CPU count — documented); **`BPF_PROG_TEST_RUN`→ringbuf consumability is high-confidence but not explicitly documented** → proven by a fail-fast de-risk case first in the test task, with a documented fallback (dp-unit asserts `sample_stats` accounting only; event content asserted in the gated `make smoke`); pin-path lifecycle assumes one gateway per node.
**Impact:** M2 Drop-reason counters executed T1–T6 (all 17 DRC reqs verified) with 2 rendered diagrams. New `src/drop_event.h` + `src/sample.h` + `tools/dpstat.c`; rewrites `drop_reason.h` (frozen 0..15 ABI per AD-016 + `drop_reason_name[]` + static-assert headroom); extends `loader.c` (pin+seed), `test_parse.c` (symbol-based migration + sampling cases), Makefile. Establishes the bpffs-pinning pattern the M4 worker will reuse for map access.

### AD-016: Drop-reason index ABI — §9.2 doc order, frozen, header name-table as source of truth (2026-07-08)

**Decision (D-DRC-1, context.md):** (a) canonical `enum drop_reason` numbering = **TDD §9.2 document order, indices 0..15** — a one-move migration (`map_error` 4→15, `bogon_drop` takes 4; fail-fast 0–3 and SLRD's `service_miss`/`service_disabled` 5/6 already coincide); (b) **`drop_reason_name[]` string table in `drop_reason.h` is the single source of truth** — tests, the P3 CLI, and the M4 worker decode from it; doc tables reference the header; (c) **append-only growth after 15** within `DROP_REASON_CAP=32`, indices frozen forever after this feature (renumber legal only inside it, existing suite migrated in-place); (d) **SLRD executes first** (ROADMAP order) — no edits to its approved tasks.
**Reason:** M3 adds nine drop paths and M4/M5 consume indices as ABI — last cheap moment to make code and §9.2 agree permanently; doc-order costs only one moved index; a code-adjacent name table can't drift from the enum.
**Trade-off:** `map_error`'s index changes once (its tests + SLRD's `DR_MAP_ERROR` expectations migrate in this feature); adding a GA reason means the doc listing order and index order diverge for reasons >15 (append order wins there).
**Impact:** M2 Drop-reason counters (DRC-01..17): spec + context complete. Closes AD-015's deferred "final §10.2 numbering". Sampling mechanism (ringbuf vs perf), rate-limit policy, and CLI shape = agent discretion at Design. Next: **Design**.

### AD-015: Service lookup & redirect — data-plane design (map-in-map slot, fail-closed redirect, decision-via-test-hook) (2026-07-08)

**Decision (design.md):** (a) **`service_map` = `BPF_MAP_TYPE_ARRAY_OF_MAPS`[2] of `LPM_TRIE` inners** (`service_inner_0/1`, `BPF_F_NO_PREALLOC`), the inner selected by `active_config.active_slot` — the concrete double-buffer for A-SLRD-4; the M4 worker flips one field (`active_slot`) to swap all config atomically. (b) **Redirect is fail-closed by construction:** `bpf_redirect_map(&tx_devmap, 0, XDP_DROP)` returns `XDP_REDIRECT` on a populated `OUT` else `XDP_DROP` (low 2 bits of `flags` = miss action) — no extra hot-path guard. (c) **The redirect decision is observed device-free via `pkt_meta.verdict` in `test_meta_map`** (under `-DPKT_TEST_HOOKS`); real `XDP_REDIRECT` + TTL/checksum only in the gated `make smoke` two-veth path. (d) hot path replaces both packet-parse seams: slot-pin → inner-LPM lookup on `dst_ip` (prefixlen=32, network-order) → `DR_SERVICE_MISS`/`DR_SERVICE_DISABLED`/`DR_MAP_ERROR` or redirect; ARP → same redirect. New `src/service.h`; `drop_reason.h` **appends** 2 reasons (indices stable); `pkt_meta.h` adds `service_id`/`active_slot`/`verdict`; loader gains `OUT`+seed. Full detail in `.specs/features/service-lookup-redirect/design.md`.
**Reason (3 kernel semantics web-verified 2026-07-08, Context7 unavailable):** map-in-map accepts any inner except `PROG_ARRAY` (LPM inner valid); `bpf_redirect_map` uses `flags` low-2-bits as the miss action; **`BPF_PROG_TEST_RUN` does not process `XDP_REDIRECT`** — so redirect forwarding is not unit-testable and the decision must be observable without a device. These are reusable patterns for every M3 config map + redirect path and M4's swap.
**Trade-off:** map-in-map+LPM-inner has no explicit doc example → **fallback** documented (two named top-level LPM maps + slot branch, same external contract) and confirmed at build/load (fail-fast, not assumed); the decision-via-hook means unit tests never assert `retval==XDP_REDIRECT` (only the gated smoke does); appending drop reasons keeps counter indices stable but leaves final §10.2 numbering to *Drop-reason counters* (M2#3).
**Impact:** M2 Service lookup & redirect executed T1–T7 (all 26 SLRD reqs verified). Modifies the executed `data-plane/` (`xdp_gateway.bpf.c`, `pkt_meta.h`, `drop_reason.h`, `loader.c`, `Makefile`, `test_parse.c`) and **migrates the 21 packet-parse tests** whose `XDP_PASS` terminal changed. Establishes the config-map/slot-pin/`tx_devmap` patterns + the first `dp-integration` (`make smoke`) convention (extends `TESTING.md`, A-SLRD-8).

### AD-014: Service lookup & transparent redirect — 3 gray areas (first config maps + slot pin) (2026-07-08)

**Decision:** (a, D-SLRD-1) **this feature owns the config-map read/pin side + a userspace seed helper** — a slot-aware `service_map` (LPM by dst IPv4), an `active_config` map (`active_slot`+`version`), the ingress `active_slot` snapshot/pin into `pkt_meta`, and `tx_devmap`; a loader/test seed helper fills a slot so it's independently loadable/testable. The **authoritative Postgres→map build, verify, and atomic `active_slot` swap + rollback stay M4** (no worker/Redis/DB here). (b, D-SLRD-2) **redirect verified by unit decision + a gated live smoke** — `BPF_PROG_TEST_RUN` asserts the verdict (`XDP_REDIRECT`/`service_miss`/`service_disabled`/`map_error`) in the parallel-safe suite; a separately-gated live two-veth (`IN↔OUT`) smoke asserts real forwarding + **TTL/checksum unchanged** (first dp-integration test). (c, D-SLRD-3) **ARP switches to `XDP_REDIRECT IN→OUT`** via the same `tx_devmap` (true transparent bridge; closes packet-parse's ARP seam D-PKT-2), never mis-counted/dropped; replies return via the asymmetric/DSR path (CM-09). Full context in `.specs/features/service-lookup-redirect/context.md` (D-SLRD-1..3).
**Reason:** The ROADMAP assigns the `active_slot` pin here and it's the invariant every M3 config lookup depends on, so it must be established now with a stable contract M4 can fill without touching the hot path; `BPF_PROG_TEST_RUN` proves the decision but only a live path proves xmit + header preservation, and splitting keeps the everyday suite NIC-free/parallel-safe; a transparent inbound bridge with no useful IP on `IN` should redirect ARP `IN→OUT`, not pass it to a host stack (PRD §8.2 explicitly left ARP "pass/redirect" open).
**Trade-off:** slot machinery whose only writer until M4 is a throwaway seed helper (double-buffer's atomic swap unexercised until M4); header preservation proven in the gated/privileged path, not the quick suite; ARP now depends on the `OUT` driver's `ndo_xdp_xmit` + the asymmetric return path (CM-09).
**Impact:** M2 Service lookup & redirect feature (SLRD-01..26). Introduces the first **config maps** (`service_map`, `active_config`) + `tx_devmap` and the per-packet slot-pin pattern reused by all of M3; replaces packet-parse's service-lookup + ARP seams (PKT-15, PKT-23/24). Adds `DR_SERVICE_MISS`/`DR_SERVICE_DISABLED` to `enum drop_reason` within `DROP_REASON_CAP=32` headroom (A-SLRD-2); full §10.2 set + sampling still *Drop-reason counters* (M2#3). Extends `.specs/codebase/TESTING.md` data-plane section with the redirect/dp-integration convention (A-SLRD-8). No control-plane change. Flagged: A-SLRD-1 `service_map` value = `service_id`+`enabled` (disabled services present in map), A-SLRD-3 loader gains `OUT` arg, A-SLRD-4 slotting mechanism (map-in-map vs slot-in-key) = Design call, A-SLRD-7 map/devmap errors fail closed (`map_error`).

### AD-013: Packet parse & fail-fast — 4 gray areas (first data-plane feature) (2026-07-08)

**Decision:** (a) **this feature bootstraps `data-plane/`** — `clang -target bpf` + libbpf-skeleton build, a userspace loader that attaches to `IN` in **native/DRV mode** (fail-loud on non-native, **no silent generic fallback**; generic-mode alerting deferred to M6), and a `BPF_PROG_TEST_RUN` test harness — with parse + fail-fast as the payload (mirrors auth-rbac bootstrapping the control-plane); (b) **ARP = classify + `XDP_PASS`** (non-destructive), with a marked seam to switch to `XDP_REDIRECT IN→OUT` when the redirect feature lands (this feature has no `tx_devmap` yet); (c) **valid IPv4 exits at a marked `XDP_PASS` service-lookup seam** with `pkt_meta` fully populated (next feature drops in the service call; tests assert PASS + `pkt_meta` values); (d) **data-plane verified via `BPF_PROG_TEST_RUN` synthetic packets** (tests the real verifier-approved program; no NIC), establishing the data-plane `TESTING.md` pattern. Full context in `.specs/features/packet-parse/context.md` (D-PKT-1..4).
**Reason:** Nothing downstream can compile/load/test without the scaffold; fail-loud-on-non-native keeps the mandatory-native constraint honest; `XDP_PASS` seams keep clean-traffic pass-through demoable without stealing the redirect feature's ownership; `BPF_PROG_TEST_RUN` exercises the loaded object (not a host-compiled mirror) with per-verdict assertions.
**Trade-off:** Large/Complex feature (scaffold + parse in one); ARP/clean-IPv4 only `XDP_PASS` until the redirect feature ships (not a shippable end state alone); `BPF_PROG_TEST_RUN` needs a BPF-capable CI kernel and does not exercise the NIC's native-XDP path (a live-veth smoke gate is a later candidate).
**Impact:** M2 Packet parse feature (PKT-01..24). Creates `data-plane/`; ships shared `enum drop_reason` (4 fail-fast reasons + `map_error`) + a **minimal** per-CPU counter for test observability — full §10.2 coverage + ringbuf sampling + bloom-FP counters remain *Drop-reason counters* (M2#3, A-PKT-3). Adds a data-plane section to `.specs/codebase/TESTING.md` (A-PKT-2). No control-plane change. Assumptions flagged: A-PKT-1 VLAN/QinQ depth = 2, A-PKT-4 L4-truncation = `malformed_ipv4`, A-PKT-5 non-TCP/UDP/ICMP IPv4 continues, A-PKT-7 fragment = MF∨offset.

### AD-012: Apply-status state machine policy — 3 gray areas (2026-07-08)

**Decision:** (a) **auto-enqueue** — every committed service/rule/list mutation immediately creates a job and moves the service `pending→queued`, returning **202** `{apply_status, version, active_version}` (TDD 4.5/4.6); no explicit apply/publish action in v1; (b) **M1 owns machine + guard + real Redis enqueue + `AgentJob` ledger + worker-facing `mark_applying/active/failed` (version-guarded)** — the full machine is unit+integration testable in M1 without a data-plane; M4 adds only the worker loop that calls the mark_* functions; (c) **per-service** apply targets in v1 (status/version/active_version on `ProtectedService`; scoped rule/list edits roll up to the parent service); global-blacklist/feed apply-status deferred to M4 (no generic `ApplyTarget`). Full context in `.specs/features/apply-status/context.md` (D-APLY-1..3).
**Reason:** Auto-enqueue meets ≤5s propagation by construction (idempotent-by-version collapses rapid edits); owning the whole machine now maximises what's verifiable in M1 and hands M4 a clean tested interface; per-service reuses the columns service-rule-list already added — no speculative modeling for targets with no data-plane consumer yet.
**Trade-off:** Redis becomes an enqueue-only dependency one milestone before its consumer; mark_* ship "callable, only called by tests" until M4; N rapid edits enqueue N jobs (superseded via the version guard, not cancelled); a global-blacklist edit gets no own apply-status until M4.
**Impact:** M1 Apply-status feature (APLY-01..40). **Reads** service-rule-list's `version` (A-SRL-3) and **modifies** its service/rule/list services to enqueue (mirrors the tenant-cidr `revoke` modification pattern). New `AgentJob` model + Alembic revision + enqueue-only Redis client. Flagged: A-APLY-1 Redis outage = graceful-degrade via ledger (transactional outbox), A-APLY-3 version guard is the only concurrency control (no job cancellation), A-APLY-6 retry-failed P2 / rollback (OP-05) deferred.

### AD-011: Service/rule/list management policy — 4 gray areas (2026-07-07)

**Decision:** (a) whitelist/blacklist **source** CIDRs are **arbitrary IPv4** (external allowed) — only a service's **destination** `cidr_or_ip` is scoped to `AllocatedCIDR` (AUTH-14); lists are "scoped" = attached to `service_id` (AD-003); (b) **delete service = disable-first, then cascade** its own rules/whitelist/blacklist (dangerous + audited); delete of an `enabled` service → 409; (c) service destination `cidr_or_ip` **must not overlap** another active service — enforced **globally** via partial GiST exclusion (mirrors `AllocatedCIDR`, one dst IP → one service); (d) **manual** global blacklist CRUD ships in this feature (admin-only), **feed** auto-population deferred to M4 (`source` field discriminates `manual`/`feed`). Full context in `.specs/features/service-rule-list/context.md` (D-SRL-1..4).
**Reason:** Whitelisting/blacklisting external sources is the whole point; children are composed by the service (cascade natural) while the CIDR↔service scoping relationship blocks (D-TCA-2); global service-destination no-overlap = deterministic `service_map` + unambiguous ownership; manual global-deny is plain list mgmt, the feed is its own M4 machinery.
**Trade-off:** Delete needs the explicit disable→delete sequence (no one-call live cut); services can't nest destination ranges; plan `committed`/`ceiling` are admin-only in v1 (A-SRL-1, flagged).
**Impact:** M1 Service/rule/list feature (SRL-01..44). Wires auth-rbac `AUTH-14`; realizes tenant-cidr `TCA-16` (revoke-in-use) via the dependency-count hook it stubbed. New `protected_service_active_dest_no_overlap` GiST exclusion + `(service_id,priority)` unique + ≤16-rule cap. Flagged: A-SRL-1 plan authority, A-SRL-3 apply-status handoff (stops at `pending`).

### AD-009: Tenant & CIDR allocation policy — 3 gray areas (2026-07-07)

**Decision:** (a) `AllocatedCIDR` non-overlap enforced **globally** (no two `active` allocations overlap, even within one tenant) via Postgres GiST exclusion constraint partial on `status='active'`; (b) revoking a CIDR still holding services/list entries is **blocked** (409, fail-closed) — not cascade/soft; (c) deleting a tenant with any user or active CIDR is **blocked** until emptied — `suspend` is the reversible off-switch. Full context in `.specs/features/tenant-cidr/context.md` (D-TCA-1..3).
**Reason:** Global no-overlap = one DB constraint + unambiguous scope checks (superset of PRD 7.2). Block-on-in-use / block-on-delete match the product's fail-closed posture; no orphaned users (closes AUTH-36) or silently-unprotected resources.
**Trade-off:** Admin must do explicit multi-step cleanup before revoke/delete (no one-click cascade); global overlap forbids a tenant holding nested ranges of its own.
**Impact:** M1 Tenant & CIDR feature (TCA-01..32). Resolves auth-rbac `AUTH-36`; supplies data + CIDR-scope primitive behind `AUTH-14`. Assumptions flagged: non-canonical CIDR rejected via `cidr` type; `0.0.0.0/0` rejected.

### AD-010: CIDR non-overlap = DB-level partial GiST exclusion (2026-07-07)

**Decision:** `AllocatedCIDR.cidr` uses Postgres `CIDR` type; non-overlap enforced by `EXCLUDE USING gist (cidr inet_ops WITH &&) WHERE (status='active')`. Scope containment (AUTH-14 primitive) via `cidr >>= :target`. API-layer CIDR validation via Python `ipaddress.ip_network(strict=True)` (reject IPv6/host-bits/`0.0.0.0/0`).
**Reason (verified vs PostgreSQL docs, 2026-07-07):** `inet_ops` is a **core built-in** GiST opclass (supports `&&`,`>>`,`>>=`); **no `btree_gist`/extension** needed for a single-column `&&` constraint. Must be **named explicitly** (`ops={'cidr':'inet_ops'}` in SQLAlchemy `ExcludeConstraint`) — it isn't the default opclass until PG 19. Partial `WHERE active` makes soft-revoke free the space (re-allocatable) and is race-proof (concurrent overlaps → one `ExclusionViolation` → 409).
**Trade-off:** requires the named opclass (a known SQLAlchemy gotcha); DB error must be mapped to a friendly 409.
**Impact:** M1 Tenant & CIDR `design.md`; the `cidr_in_tenant_allocation` primitive + `AllocatedCIDR` model are reused by Service/Whitelist/Blacklist (M1/M3). `User.tenant_id` FK to be pinned `ON DELETE RESTRICT`.

### AD-001: Stack for control-plane, DB, dashboard (2026-07-07)

**Decision:** API = Python + FastAPI; DB = PostgreSQL; dashboard = React (SPA). Data-plane (C/XDP/eBPF), worker (Python), queue (Redis) are PRD-fixed.
**Reason:** FastAPI shares Python with the sync worker; Postgres has native inet/cidr for CIDR allocation/overlap; React suits ≤2s realtime dashboards.
**Trade-off:** Two languages (C + Python) across the stack; React adds a build/SPA layer vs server-rendered.
**Impact:** M1 API and M5 dashboards target these; Postgres constraints back `AllocatedCIDR` non-overlap (7.2).

### AD-008: Control-plane testing/runtime conventions (2026-07-07)

**Decision:** Control-plane is **async** (asyncpg + SQLAlchemy 2.0 `AsyncSession`, `redis.asyncio`, httpx `AsyncClient`). Tests use **pytest** with `unit`/`integration` markers; integration tests run against a **docker-compose test stack** (`compose.test.yml` PG+Redis). Quick gate = **ruff + mypy + unit**; full gate adds integration. Conventions in `.specs/codebase/TESTING.md`.
**Reason:** Async fits later realtime dashboards/worker; real PG needed for citext/JSONB/CHECK fidelity; ruff+mypy modern default.
**Trade-off:** Integration tests not parallel-safe (shared compose stack) → mostly sequential execution.
**Impact:** All control-plane code (M1–M6) follows async idioms; only unit-tested tasks can be `[P]`.

### AD-002: Service `disabled` = drop-all (D1 / BL-03) (2026-07-07)

**Decision:** Disabling a service drops all its traffic with reason `service_disabled` (distinct from `service_miss`), requires UI confirm + audit. NOT pass-through.
**Reason:** Inline inbound-only bridge; disable is an intentional protection cut, not a bypass.
**Impact:** M2 pipeline + M1 UI confirm/audit. Refs 6.3, 8.2, 10.2, 12.2.

### AD-003: Whitelist bypass is service-scoped (D2 / BL-01, BL-02) (2026-07-07)

**Decision:** Whitelist/VIP bypass keyed by `service_id`+source CIDR; never edits the global blacklist/feed map. Whitelisting a feed IP raises alert+audit; admin flag can forbid it.
**Reason:** Preserve tenant isolation (5.2) — tenant A must not remove global protection for B/C.
**Impact:** `whitelist_lpm` key includes `service_id` (M3). Refs 6.5, 6.7, 8.3, 12.3.

### AD-004: Allow-rule = first-match by priority, terminal (D3 / BL-05) (2026-07-07)

**Decision:** First enabled rule matching by ascending `priority` decides the verdict; if it is out of quota → `rate_limit_drop`, no fall-through to looser rules.
**Reason:** Fall-through would empty per-rule limits (traffic spills to a looser rule).
**Impact:** M3 rule loop; UI warns on overlapping rules. Refs 6.4, 8.2, 12.2.

### AD-005: Atomic config swap via double-buffer `active_slot` (BL-06) (2026-07-07)

**Decision:** Config maps versioned into 2 slots; worker builds/verifies the inactive slot, then flips `active_slot` in one write. Data-plane pins the slot at ingress. Runtime-state maps are unslotted. Rollback = flip back.
**Reason:** Avoid hybrid new-rule/old-service windows; enables instant rollback (OP-05).
**Impact:** M4 worker + M2 ingress pin. Refs 8.1, 8.3.

### AD-006: Chargeback by p95 clean Gbps (D5 / CM-03, BL-09) (2026-07-07)

**Decision:** Internal chargeback metered on clean (redirected) bandwidth; `billed_gbps = max(committed_clean_gbps, p95_clean_gbps)`; `ServicePlan`/`BillingUsage` model it. Billing bytes from exact per-CPU counters, separate from rate-limited event sampling.
**Reason:** p95 is the bandwidth-billing industry norm; sampling can drop events and must not be trusted for money.
**Impact:** M5 metering + M3 ceiling enforcement (`service_ceiling_drop`). Refs 7.1, 10.3, 8.2/8.3, 12.6.

### AD-007: SLA option (ii) — Availability excluded at Pilot (D6 / CM-01, CM-04) (2026-07-07)

**Decision:** Per-tenant SLA level = high on latency/accuracy/propagation/fairness; Availability deliberately **excluded** from the Pilot SLA (best-effort + maintenance window + bypass in the OLA). HA is the GA condition for an Availability commitment.
**Reason:** Single-node fail-closed inline = SPOF; committed clean bandwidth is guaranteed in hardware terms, availability is not.
**Impact:** M6 bypass/OLA; M7 HA. Refs 3.2, 11.4, 8.4, 14.

---

## Resolved Blockers

### B-002: Local user could not load BPF programs for `BPF_PROG_TEST_RUN` — Resolved

**Discovered:** 2026-07-08 (Packet parse T3 execution)
**Resolved:** 2026-07-08 — `cd data-plane && make test` now loads the test XDP program and prints `1 passed`.
**Impact:** Previously blocked T3+ verification because `make test` needs to load the XDP program for `BPF_PROG_TEST_RUN`; per plan, unverified task work was not committed or marked complete.
**Evidence:** `make test` compiles the test runner, then libbpf fails to load `xdp_gateway_test_bpf` with `Operation not permitted`; `kernel.unprivileged_bpf_disabled=2`; current shell has no effective capabilities; `sudo -n make test` fails with `sudo: a password is required`.
**Workaround:** Run the gate in a privileged shell (`sudo make test`) or grant an appropriate development environment with BPF load permissions.
**Resolution:** Verified in the current shell with `cd data-plane && make test` → `1 passed`; T3 marked verified and execution continues.

---

## Active Blockers

### B-001: No HA / single-node SPOF (CM-01) — GA Blocker

**Discovered:** 2026-07-07 (PRD BA review)
**Impact:** Blocks a production Availability SLA; does NOT block Pilot or development.
**Workaround (Pilot):** OLA documents maintenance window + bypass procedure (OP-03, M6); Availability excluded from Pilot SLA (AD-007).
**Resolution:** Active/passive HA + link bypass (fail-to-wire) at GA — M7.

---

## Lessons Learned

### L-001: Staged work is not task completion (2026-07-12)

**Context:** Telemetry T5 was initially interrupted with model, migration, and
test changes partially staged; an audit commit included the staged subset.
**Problem:** A staged diff or partial commit can look complete without a passing gate and a complete test set.
**Solution:** Record it as partial and keep dependent tasks blocked until
verification and the remaining test work finish. T5 later completed in
`657cd3d` after the full gate passed.
**Prevents:** Incorrect dependency advancement and misleading requirement traceability.

---

## Quick Tasks Completed

| # | Description | Date | Commit | Status |
| --- | --- | --- | --- | --- |
| 1 | Tenant Create-Service modal: hide Mode/VIP-PPS/VIP-BPS fields; fix `mode=allow-rule-only`, default new services `vip_pps=5000` / `vip_bps=1_000_000_000` (1 Gbps bps); existing values preserved on edit (`ServiceForm.tsx` + `ServicesPage.test.tsx`, 6 fe tests green, tsc clean). Admin create/edit forms untouched (separate surface). | 2026-07-22 | _uncommitted_ | Done |

---

## Deferred Ideas

Ideas for future features/phases (mostly PRD-tracked GA/Backlog findings):

- [ ] Auto-response / one-click mitigate for fast attack reaction (OP-02, GA) — Captured during: init
- [ ] Monitor/count-only rule mode before enforcing (OP-04, GA) — Captured during: init
- [ ] Sampled per-tenant drop-flow records for self-service debug (OP-06, GA) — Captured during: init
- [ ] `expires_at` reconciliation sweep for whitelist/blacklist (BL-07, GA) — Captured during: init
- [ ] Stateless SYN-cookie / scan detection to back SYN-flood/port-scan claims (BL-04, GA) — Captured during: init
- [ ] PII retention/anonymization for `top_src` (CM-08, GA) — Captured during: init
- [ ] Multi-admin & separation of duties (OP-07, GA) — Captured during: init
- [ ] Guided onboarding + learning mode (OP-08, Backlog) — Captured during: init
- [ ] SSO/IdP + MFA for admin (CM-10, Backlog) — Captured during: init
- [ ] Explicit tunnel protocol values (`gre`, `esp`, …) in `AllowRule.protocol` so tunnels become allowlistable without widening `any` (D-ARL-1 follow-on, GA) — Captured during: allow-rule-ratelimit discuss (2026-07-09)
- [ ] Control-plane warning/validation + UI banner when whitelist entries exist on a service with both `vip_pps`/`vip_bps` NULL (whitelist inert per D-WLV-1/AD-020; SRL UX follow-up, pairs with M6 VIP-ceiling-hit alert) — Captured during: whitelist-vip discuss (2026-07-09)
- [~] Control-plane writer for `udp_blocked_port_bitmap` (minimal admin CRUD + build path, or fold into GA auto-response OP-02) — seed-only in v1 per D-BLK-2/AD-022 — Captured during: blacklist-filters discuss (2026-07-09) → **NOW BEING SPECIFIED** as `udp-amplification-config` (AMP-01..21, spec+context drafted 2026-07-21, D-AMP-2 = reconcile lane + `dpstat set-blocked-ports`, not fold-into-OP-02)
- [ ] Redis Stream/consumer-group upgrade behind `ApplyDispatcher` (multi-worker, replay) — list+ledger suffices v1 per A-AGW-3 — Captured during: agent-worker discuss (2026-07-10)
- [ ] Lease/heartbeat stuck-job reclaim for a wedged-but-alive worker (pairs with M6 stuck-job alerting; crash recovery = D-AGW-2 startup sweep) — Captured during: agent-worker discuss (2026-07-10)
- [ ] Per-service queue coalescing (skip straight to newest queued version instead of superseding one job at a time) — optimization under sustained churn — Captured during: agent-worker discuss (2026-07-10)

---

## Todos

Open before Pilot (non-engineering, non-blocking — Product/Legal owned):

- [ ] CM-02: IPv6 hard-drop blackhole warning + checklist in onboarding
- [ ] CM-06: capacity positioning (single 40G node = small/mid scrubber; absorption depends on upstream)
- [ ] CM-07: review threat-feed licenses for commercial/internal-paid use
- [x] Complete telemetry P1 execution: T7 → T9 → (T8 ∥ T11) → T12 → T17;
  P2/P3 telemetry tasks remain deferred — verified 2026-07-13

---

## Preferences

**Model Guidance Shown:** 2026-07-08 (mentioned for spec-pipeline state/doc updates)
**Execute tooling:** Skill `coding-guidelines` on C/XDP code tasks; no MCPs configured; data-plane gates via `make` (build/quick/full).
